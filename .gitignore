# 憑證：永不進 git。交易金鑰只存在跑引擎的那台機器上。
# 用 .env* 而不是 .env：記事本會把 ".env" 存成 ".env.txt"，差三個字元
# 就等於把密鑰提交上去。
.env*
!.env.example
*.key
engine/config.yaml

# 執行時產物：logs 是 320M 的分鐘/掃描 CSV
engine/logs/
*.log

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
