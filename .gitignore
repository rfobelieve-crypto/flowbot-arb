# 憑證：永不進 git。交易金鑰只存在跑引擎的那台機器上。
# 用 .env* 而不是 .env：記事本會把 ".env" 存成 ".env.txt"，差三個字元
# 就等於把密鑰提交上去。
.env*
!.env.example
*.key
engine/config.yaml

# 執行時產物：logs 是 320M 的分鐘/掃描 CSV
engine/logs/
*.log

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# 第三方參考原始碼（使用者放進來當範例，2026-09-05）：讀它、不提交它。
# 它們有自己的 LICENSE 與自己的上游，混進本 repo 的歷史會讓「哪些程式是
# 我們的」這個問題永遠答不清楚——而那正是 PEER_INFRA 的紀律要保住的東西。
entropy-arb-main/
perp-dex-tools-main/
