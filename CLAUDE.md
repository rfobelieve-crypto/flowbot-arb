# CLAUDE.md — 這個 repo 的硬規則

> 第四條線：**兩場館套利**。不預測任何東西，賺的是同一個東西在兩個地方的價差。
> 這份檔案是**每個 session 開頭都會讀的規則**，所以只寫「不照做會出事」的，
> 細節一律指向 `docs/`。
>
> **新 session 的閱讀順序**：本檔 → `docs/NEXT_SESSION.md`（現況與待辦）→
> 手上那件事對應的 `docs/` 檔。

---

## 1. 隔離規則（違反了要回退，不是討論）

- **本 repo 永不 import、永不讀 `flow_system`／`jarvis`／任何交易路徑的模組**，
  永不碰它們的 MySQL。對外只有兩座唯讀顯示橋，且是對方主動來讀。
- **資金完全獨立**：獨立錢包／子帳戶，只放這條線的錢，盈虧不進 V7 的權益快照。
  （共用帳戶已經付過兩次代價：06-05、07-27 手動爆倉。）
- **金鑰不進 git、不進 Railway。** 引擎在 VPS，儀表板不碰金鑰。
  `.gitignore` 用 `.env*` 而不是 `.env`——記事本會把 `.env` 存成 `.env.txt`，
  差三個字元就等於把密鑰提交上去。
- **儀表板一顆按鈕都沒有。** 唯讀。要動引擎走控制通道（`entropy_arb/control.py`）。

## 2. 碰錢的程式碼（下單、撤單、對帳、對沖、平倉）

**這一節的每一條都有事故當背書。**

1. **下單程式碼要逐行審。** 新寫的執行路徑，寫完要一份 B1 等級的審查文件
   （範本：`docs/B1_ENGINE_AUDIT.md`、`docs/B3_MAKER_AUDIT.md`）。
   審查要明寫**沒跑過的部分是哪些**。
2. **不確定時不假設沒成交。** 逾時／5xx／沉默一律回 `unresolved`，
   上層去**對帳**，不是補單。「以為沒成交所以再送一次」是那對兄弟賠掉
   $1.1M 的病灶。
3. **本地狀態只能被交易所的回報清除，不能被我們自己的動作清除。**
   送出撤單 ≠ 訂單消失（Hummingbot XEMM 就是在這裡把對沖腿弄丟的）。
4. **對帳只採信真相，不下修正單。** 鏈上讀數是完整的；採信之後不准再把
   本地的 delta 加上去。
5. **每張單都要有我們自己產生的唯一 ID**（HL cloid／Lighter client order
   index），而且**在網路呼叫之前就配好**——送單逾時的那張單也必須撤得掉。
6. **破壞性操作執行前先核對真實外部狀態。** `/okx-admin/heal` 曾經是 GET，
   被連結預覽帶著 `confirm=YES` 預抓，把一筆活著的真錢部位在 DB 裡歸零造出
   孤兒倉（mistake.md 2026-06-07，第二次）。所以 `flat` 先對帳再平倉。
7. **風控出錯要 fail-close。** 沒武裝的風控開關 → 實盤拒絕啟動
   （`_require_armed_risk_block`）。「開關存在」與「開關武裝」是兩件事，
   而運行中的系統看不出差別。
8. **kill switch 寫死在引擎裡，不是儀表板上的開關。** 等人按按鈕的守衛等於
   沒有守衛——兄弟那台機器人自己失控了 15–30 分鐘。
9. **HALT 是單向的**，要重啟才能解除，因為重啟才會用 `strict=True` 重讀真實
   部位。控制通道**不准**解除 HALT。只有**波動熔斷**是 PAUSE 會自動恢復。
10. **停機不等於不平倉。** 開的時候不敢開、關的時候也不敢關，是兩頭最差。

## 3. 這條線已經用量測決定「不做」的事

**不要重新引入。要推翻請先拿出新的量測，不是拿一份通用最佳實踐。**
（背景資料：`docs/HFT_INDUSTRY_GUIDE.md` 是一份行業通用高頻指南，
它的前提跟這條線的 regime 不同。）

| 不做 | 量到的理由 |
|---|---|
| **微秒級延遲工程**（C++/Rust 熱路徑、機房同置、CPU 綁核、kernel bypass） | 機會壽命中位 **5–15 分鐘**、掃描週期 180 秒、本機到場館 RTT **32–95 ms**。進場慢 100 ms 不會錯過一個 5 分鐘的機會。**在延遲上投資是解一個我們還沒有的問題**（`HOTPATH_AUDIT.md` 1、8；`PEER_INFRA.md` §5） |
| **多邊／三角套利**（Bellman-Ford、CPLEX） | 問題從來不是「找不到路徑」，是**成本吃掉價差**（七桶成本，量到 43%，全家族在所有情境下皆為負）。換更會找路徑的演算法不改變費率結構 |
| **把執行迴圈放進 web 應用（jarvis）** | 沒有任何 Railway 區同時靠近兩條腿；部署重啟有 1–2 分鐘盲區；jarvis 上有其他人的錢 |
| **在儀表板放一鍵下單** | 見 §1 最後一條 |
| **自動加碼、多配對併發** | `LIVE_50U_SPEC` §5 凍結：單配對單倉 |

**唯一真正會咬人的延遲在撤單**（掛單被吃之前撤掉）。那是 B3 上線後要量的第一件事
（M3 逆選擇），到那時再談延遲工程也不遲。

## 4. 研究紀律

- **判準在看到資料之前寫死，之後不放寬。** 已凍結的判準不要重寫。
- **沒凍結的一律先預註冊再跑。**
- **只寫親自查證過的東西。** 別人整理的清單當**待查名單**，不當事實；
  文件裡要明講哪些是沒查的、哪些是估計不是量到的。
- **時鐘到期才讀**（`docs/NEXT_SESSION.md` §C），不要提前下結論。

## 5. 工作方式

- **測試**：`cd engine && python -m pytest -q`（目前 **84 passed**，anaconda 3.9）。
  碰錢的改動要有**故意觸發它的已知答案測試**，包含失敗路徑
  （斷線、逾時、拒單、撤單輸掉競速）。
- **測試不准污染 `engine/logs/`**——那裡是錄價器的家。測試設定寫到暫存目錄。
- **改設定檔的預設值＝改所有既有配對的行為。** 新增旗標一律預設成舊行為
  （例：`execution.mode` 預設 `taker`），並驗證九個既有設定檔載入結果不變。
- **費率**：沒對過帳單的費率**不是折扣，是猜測**。`maker_fee_bps` 預設等於
  `taker_fee_bps`，不預設 0。M1 拿到成交回執之前不改 `mode: maker`。
- **看引擎活著**：`engine/logs/*/minutes.csv` 的 `os.stat` mtime
  （PowerShell 目錄列表的 mtime 對長期開著的檔案會延遲，不準）。

## 6. 檔案地圖

| 東西 | 位置 |
|---|---|
| 現況與待辦交接 | `docs/NEXT_SESSION.md` ← **每次先讀這個** |
| 引擎（下單/對帳/對沖/風控） | `engine/entropy_arb/` |
| 掛單狀態機（三條「必須」住在這） | `engine/entropy_arb/maker.py` |
| 波動熔斷 | `engine/entropy_arb/volatility.py` |
| 控制通道（四動詞） | `engine/entropy_arb/control.py` |
| 下單路徑逐行審 | `docs/B1_ENGINE_AUDIT.md`、`docs/B3_MAKER_AUDIT.md` |
| 風控數字怎麼來的 | `docs/RISK_NUMBERS.md` |
| $50 實盤週的凍結規格 | `docs/LIVE_50U_SPEC.md` |
| 別人的基礎設施對照 | `docs/PEER_INFRA.md` |
| 熱路徑稽核 | `docs/HOTPATH_AUDIT.md` |
| 場館三層清單 | `docs/VENUES.md` |
| 成本名冊（七桶） | `docs/COST_INVENTORY.md` |

## 7. 現在還開著的

- **強平偵測**——只能靠對帳事後發現。
- **掛單啟動殘單掃描兩邊不對稱**——Lighter 查不到就拒絕啟動，HL 只記 warning
  （HL `openOrders` 帶 `dex` 在 HIP-3 上還沒實測）。第一次實盤前補成對稱的。
- **簽章那一層沒跑過**——兩個 SDK 本機沒裝，程式是照官方原始碼寫的。
  第一次實盤前要人工確認 `docs/B3_MAKER_AUDIT.md` §5 的三件事。
- **波動熔斷門檻要用實盤第一週的真實觸發次數重調**（現在的 1.6 倍換算是估計）。
- 從通用指南裡挑出來、確實該補但還沒做的三項：**故障注入測試**、
  **shadow 模式**（只算不下單）、**延遲看 P99 與抖動而不是平均**。
