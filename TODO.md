# 套利線 TODO（flowbot-arb）

2026-09-04 從 `flow_system/TODO.md` 整段搬過來——內容一字未改，只是換了家。
節號沿用原本的編號（§0.75 是這條線的起點，§1.xx 是後續戰場），因為所有
commit、判決檔與文件都在引用它們。

**讀法**：§0.75 在最下面（它最早），§1.xx 由新到舊。開工前先讀
`README.md` 與 `docs/NEXT_SESSION.md`。


---

### 0.75 兩場館永續套利（entropy-arb 型）——第四條策略線，錄資料階段（2026-08-28 登記）

> **2026-09-03 修正：可捕獲金額原本高估一到兩個數量級**（使用者問「那 30
> 美金一天是怎麼來的」）。`fires` 數的是**分鐘**不是交易——帶是 p90，
> 所以依定義每天 144 分鐘開火，但一段一小時的偏離是**一筆**交易含 60 個
> 開火分鐘；而收斂定義是回到**半個帶**，所以每筆抓半個帶。改成
> 事件數/天 × 半個帶 × 深度後：**SNDK 賣 $0.84 / 買 $0.07、NBIS 賣 $2.42、
> ANTH 賣 $0.05 / 買 $0.17**（原為 $8.4 / $29.5 / $13.0 / $14.4 / $25.9）。
> 舊值保留為 `_ceiling` 可稽核。**判準未動**。09-04 判決要照新數字讀。
> 詳見 mistake.md 同日第二則。

> **本線的家在 `research/arb/`（2026-08-28 使用者要求與交易策略分開）**：
> README 載明隔離規則（永不 import 交易路徑、資金獨立、只共用兩個顯示
> 看板）、五條件偵察表、戰場帳本。判決程式 = `research/arb/premium_verdict.py`。
> 本節保留為登記與風險清單的原始紀錄。

**來源**：使用者研究了 `your-quantguy/entropy-arb`（兩場館 perp 套利：
Entropy/Hyperliquid vs Lighter/trade.xyz，量 `premium_bps = (A/B − 1)×1e4`
偏離中線超過門檻就雙腿對鎖，等收斂平倉）。使用者：「我是想做一個這個」。

**為什麼值得排**：與現有三條線**完全不同的棋格**——結構性 edge 不是統計性,
機會存在性直接可觀測,**驗證週期是天不是月**（錄一兩週掛單簿 → 扣費後的
溢價分佈就是答案）。不需要預註冊/CI/樣本時鐘那一套。

**風險清單（先寫,免得到時候忘）**
- 斷腿 = 主要事故源：一腿成交一腿被拒 → 裸倉在動的市場裡
- **中線漂移 = 2026-08-08 解碼漂移的同款病**：參考分佈不是被判斷的分佈,
  每筆小虧且看起來像正常交易
- **零費率是獲客補貼,有內建過期日**：Lighter taker 0 bps 不是均衡態
- 撿到的是做市商不要的：簿太薄或風險他們算過不值得
- 該 repo 一次 commit / 零 issue：**下單程式碼要逐行審**（滑價符號 bug 的
  先例——「看起來扣了成本」騙過所有人）
- **「沒有倉位也會爆倉」（2026-08-30 使用者轉來的 Binance 案例）**：統一
  帳戶裡留著平倉後的 USDT 負債，抵押品（RLUSD 折 90%、SOL 折 80%）用
  「最大」轉出後，折算抵押只比負債多 0.6u，任何一個 tick 就強平；查倉位
  歷史看不到任何倉。對本線的規格含義：(1) 對帳要讀**餘額與負債**，不只讀
  倉位——「查無倉位」對負債是瞎的（同 mistake 07-28「DB 只記自己下的單」）；
  (2) **任何資金劃轉都是風險事件**：轉出前自己算轉出後的折算保證金率
  （含各資產折價），留緩衝，不信交易所的「允許轉出」；永不點「最大」；
  (3) 保證金一律用場館的報價穩定幣（USDC/USDG），不用計息的替代穩定幣
  當抵押；(4) 產品端跟單用戶同樣適用——「0 倉位 ≠ 0 風險」要進風控規格。
  **通則（08-31 補，作者的機制拆解證實）**：uniMMR 的分母只算持倉維持保證金
  ＋利息 5%，沒倉位時塌到 0.137u，「轉出後 ≥1.2」的守衛實際只留 0.16u 緩衝
  去對 23,000u 抵押品——**守衛量錯了分母**（同族：08-08 解碼參考分佈≠被判斷
  分佈）。**任何比值型安全門檻，先問分母會不會在某個正常狀態下塌到零**；會的
  話該門檻在那個狀態等於不存在。本線接兩個保證金帳戶時，「淨曝險／保證金」
  在一腿平掉後分母同樣會塌，設計要照此寫。

**錄製已上線（2026-08-28 18:30 起）＋判準先凍結**

- **場館發現**：Entropy 不是 crypto 所——上市 6 個標的全是**股票/私募永續**
  （io:OAI＝OpenAI、io:ANTH＝Anthropic、SNDK、IONQ、NBIS、EWY）。
  「盤後預言機制度不同」是溢價主要來源；OAI/ANTH 連公開市價的錨都沒有
- **首錄配對**：SNDK vs lighter-rh（README 自己的例子；兩邊 taker 0 bps
  **當場驗證為真**）。首兩分鐘：溢價 mean −1.4 bps、std 0.6、buy 側瞬間
  可成交空間最高 +1.38 bps——**中線不在零**，README 的頭號警告當場應驗
- **耐久化**：`run_recorder.bat`（30 秒重啟迴圈）＋使用者啟動資料夾
  （schtasks 要管理員權限，改走 Startup）＋ freshness board 監控
  （1h 上限——**靜默斷錄 = 一週資料白等**）
- **本機補丁**（clone 內，未推遠端）：6 處 `open()` 補 `encoding="utf-8"`
  （cp950 家族）、Windows 無 `add_signal_handler` 的 fallback
- **判準（跑滿 7 天之前凍結，不看資料調）**：錄滿 ≥7 個完整天後，
  存在一個扣費後往返 ≥1 bps 的門檻帶、平均每天觸發 ≥10 次、且
  **前後兩半皆成立** → 進工程閘門討論；否則本線關閉。
  帶寬不掃描——用 analyze.py 的 p90 建議值起步

**判準修訂 v2（2026-08-28 22:40，已看 230 分鐘＝窗口的 3%；只加嚴不放寬）**：
使用者問「有沒有機會」時發現 buy 側 94% 的分鐘都有帳面肉——這不是好消息，
是**中線偏移**的症狀（永遠在的價差賺不到，要收斂才落袋）。而原判準只量
「帶存在＋開火頻率」，量不到「會不會收斂」，也量不到「肥的那刻盤口有沒有
量／是不是卡價」。三項修法，全部在判決程式裡寫死：
1. **加第四關「收斂」**：溢價偏離滾動中線（6h 中位數）超過帶之後，
   ≥70% 的事件要在 240 分鐘內收斂回帶的一半以內——過不了 = 帳面肉是
   結構性偏移，不論多肥都關線
2. **錄價程式補儀器**（本機補丁 v2）：新增 8 欄——最肥時刻兩腿頂檔
   名目的較小值（能吃到多少錢）＋該刻較舊一邊的報價年齡（>5s = 卡價
   嫌疑）。舊 230 分鐘無此欄位，只從量/卡價統計中排除，不從判準排除
3. 深度/卡價統計**列報告不進判準**（供工程閘門評估）——數字是這次才
   開始錄的，拿它當關卡等於在 3% 資料上發明門檻

**階段閘門（依 CLAUDE.md 既有紀律）**
1. **現在可做**：`--record-only` 錄兩邊掛單簿（零憑證、零錢、零風險）
   → `analyze.py` 量扣費後溢價分佈 → 有肉/沒肉,一兩週結案
2. **接錢包之前**：走統一風控框架的門（兩層 kill/中央曝險帳本——
   「第二條策略上線必須先有統一風控」對第四條同樣適用）,且下單路徑逐行審
3. 排序：**在 §0.59/Gate F/地形扳機結算之前不投入工程**,只允許錄資料
   （錄資料越早越好,因為分析需要的就是時間長度）

**錄製家族（2026-08-30 登記；使用者：「有很多標的為什麼只錄一個」）**

08-28 只錄 SNDK 是因為對 Entropy 六個標的做**同名比對**只對到它；重查發現
Lighter 的代號不同（`OAI`→`OPENAI`、`ANTH`→`ANTHROPIC`），加上 HL 主網
本身可當一腿（加密標的）。錄價程式補了兩個本機補丁（`hedge.symbol`
別名、`entropy.dex=""` 走 HL core），每配對獨立設定檔／log 目錄／
30 秒重啟迴圈／啟動資料夾項目，freshness board 逐條 1h 上限。

| 配對 | 腿 A | 腿 B | 定位 | 起錄 |
|---|---|---|---|---|
| SNDK | Entropy io:SNDK | lighter-rh SNDK | 原始 | 08-28 18:28 |
| NBIS | Entropy io:NBIS | lighter NBIS | 股票永續 | 08-30 ~23:00 |
| ANTH | Entropy io:ANTH | lighter-rh ANTHROPIC | 私募永續、無現貨錨 | 08-30 ~23:00 |
| **BTC** | HL BTC | lighter-rh BTC | **對照組**：兩邊深簿零費率，帶應≈0；它一肥就是儀器壞了（mistake 07-29） | 08-30 ~23:00 |
| ZEC | HL ZEC | lighter-rh ZEC | 薄加密 | 08-30 ~23:00 |
| NEAR | HL NEAR | lighter-rh NEAR | 薄加密 | 08-30 ~23:00 |

**io:OAI、io:EWY 首次連線即 `isDelisted`**（meta 仍列但已下市）——登記
當晚就掉兩個，**下市風險是這類場館的實際風險**，記入風險清單。io:IONQ
兩邊都沒有對沖腿。

**紀律**：(1) **同一套凍結判準（四關）逐配對套用，全部報告、不挑**——
六個裡挑最好看的上，就是變體 C/D；(2) 各配對從自己的首列起算 7 天，
SNDK 照舊 09-04；(3) 判決程式（`premium_verdict.py`）已改逐配對，新增
**「可捕獲美元／天」報告欄**＝次數／天 × 帶寬 × 最肥時刻深度——**只報告
不進判準**，用途是「過閘之後值不值得投工程」（SNDK 期中 ≈ $12–16/天，
就是為此登記的）；(4) BTC 若過閘 → 判「對照組亮了」，先查儀器。

**§0.75b 掃描器（2026-08-30 登記，使用者：「新幣／熱門迷因機會大，但需要
搜尋篩選機制」）**：獨立於錄價程式的 REST 掃描（`../entropy-arb/tools/
scanner.py`，每 ~2 分鐘一輪）掃 HL 主網 ∩ Lighter 主網／RH 的全部配對：
兩邊最佳買賣、頂檔美元深度、24h 量、上市日；輸出 `logs/scan/scan_*.csv`
與新上市／下市事件 `listings.csv`。**排名指標先凍結**（`research/arb/
scan_rank.py`）：逐配對逐側 band=p90 正向可成交空間、fires=≥band 的次數
／天、depth=那些時刻 min(兩邊頂檔) 的中位 → **可捕獲美元／天 = fires ×
band × depth**，取兩側較大者排名。掃描滿 ≥3 天且 ≥500 樣本才出名單；
上市 <48h 不列；前 3 名升格到完整錄價，**升格之後的資料才進判準**
（選擇用一段資料、驗證用另一段——防「掃描器變成生產漂亮假象的機器」）。
BTC 永遠在名單當對照。

**與主體的關係（使用者 2026-08-30 定調）**：「除了流動性獵取跟 V7，其他
都是附加價值，這兩個才是主體」——本線維持「只錄不做、判決日看數字」，
不佔研究線的預註冊／統計資源；掃描器與錄製家族是零風險的資料累積，
不是投入。

---

### 1.00 掃描器擴編到非加密（2026-09-03 使用者：「掃描看看其他鏈上的股票期貨黃金」）——瓶頸是深度，而深度在別的場館

**為什麼是這件事**：§0.75 的期中數字顯示這條線的天花板不是價差、是
**對手簿的深度**——SNDK 最肥時刻的頂檔中位只有 **$264 / $288**，
帶再寬也只換得到零錢。所以擴編掃描不是「找更多機會」，是**去有深度的
地方找同一個機會**。

**查到的事實（2026-09-03，`perpDexs` + `metaAndAssetCtxs`）**：
Hyperliquid 上有 **11 個 perp dex**，掃描器只掃了其中一個（Entropy `io`，
5 個標的、24h $57M）。真正大的那個是 **`xyz`：24h $2.24B**，掛著
**黃金、白銀、銅、鉑、鈀、WTI（CL）、布蘭特、天然氣、鈾、SP500、
JP225、KR200、EUR/GBP/JPY**，加上約 90 檔個股。另外 `para`（$5.9M，
含 2Y/10Y/30Y 殖利率）、`mkts`（$8.1M，指數）也有量。

**同時查到 5 個 dex 的 24h 量是 $0、簿口全空**（flx / vntl / km / abcd /
cash）——它們掛著 GOLD、USA500、WTI 這些名字，但**沒有任何一筆報價**。
這正是「名單上有 ≠ 交易得到」，所以擴編的第一道守衛就是量。

**改了什麼（`../arb/engine/tools/scanner.py` v3，純收集端）**
1. 場館 = HL 主場 + **每一個 builder dex** + 兩條 Lighter 鏈
2. 配對 = 任何被 **≥2 個場館**掛出的同一標的，**包含 dex 對 dex**
   （同一條鏈、不用跨鏈搬錢——執行難度比跨場館低一個等級）
3. 兩道便宜的儀器守衛（廣度會放大假數字的種類）：
   - **24h 量為 0 的標的不當腿**
   - **尺度守衛**：兩腿中價差距超過 2 倍就跳過並記一筆——那是
     單位不同（指數 vs ETF，如 SP500 vs SPY），不是價差。
     ETF 對指數的別名（SPY/SP500、QQQ/XYZ100、USO/CL、SLV/SILVER）
     **刻意不建別名**，只認同一標的（GOLD↔XAU、SILVER↔XAG 除外）

**結果：配對從 113 → 255，其中 93 個碰到股票／商品場館。** 首輪快照
（單一 cycle，只是形狀不是判決）：

| 配對 | 兩側最佳 | 頂檔深度（美元） |
|---|---|---|
| GOLD@xyz-lighter | 4.65 bps | **a $35,079 / b $12,489** |
| SNDK@xyz-lighter | 5.18 bps | **a $37,428** / b $5,949 |
| SILVER@xyz-lighter | 8.61 bps | a $3,744 / b $819 |
| NATGAS@xyz-lighter | 20.41 bps | a $1,478 / b $14,997 |
| BRENTOIL@xyz-lighter | 1.48 bps | a $1,976 / b $5,006 |
| NVDA@xyz-lighter-rh | 8.42 bps | a $1,208 / b $5,807 |

**深度比原配對高一到兩個數量級**（$264 → $1k–37k）。可捕獲金額
= 次數 × 帶寬 × 深度，所以這是唯一能把「每天幾十塊」變成別的量級的
那個因子。

**紀律（沒有任何一條被放寬）**
- 升格指標（`arb/arblib/scan_rank.py`：次數／天 × 帶寬 × 深度）
  **2026-08-30 凍結，這次一個字沒動**；新配對照樣要 ≥3 天跨度、
  ≥500 筆才進名單，BTC 對照組的帶寬仍是雜訊底（2×）
- 升格 → 進錄製家族 → **自己的 7 天時鐘**，而且**收斂關**照跑：
  BTC 對照組就是靠它擋下來的（$97.8/天但 episodes=0＝永遠不回來）。
  首輪快照裡 `xyz-para` 那批兩側都是 −20~−46 bps 的持續偏移，
  **形狀跟 BTC 一模一樣**，八成會死在收斂關——這是預測，寫在這裡等打臉
- 掃描期的資料**不進判決**（選擇窗 ≠ 驗證窗）

**還沒查、進場前必須查的**：HIP-3 builder dex 的**手續費**。現有家族
的零費率是 Entropy/Lighter 的獲客補貼（§0.75 已註明有內建過期日），
**不可假設 `xyz`/`para` 也是 0**——費率是分子的直接扣項，沒查證前
任何「可捕獲美元」都只是上限。

- [x] 掃描器 v3 上線、smoke 一輪 236/255 配對成功、35s/cycle（限額內）
- [x] 舊程序停掉、新程序接手（`run_scanner.bat`）
- [x] **上牆**（2026-09-03，使用者：「上線到我的 flowbot 網站方便管理」）：
      `arb_publish.py` 加 `scan` 區塊 → `/public/arb-status` → 網站
      `/charts/arb` 的「戰場掃描」盤。排序用的可捕獲美元不出公開面，
      只有它產生的順序出；深度照舊只給分級
- [x] **五階梯寫在板上**（使用者：「回測驗證完還要上實盤驗證後才算真」）：
      掃描（本板）→ 錄製 7 天 → 收斂關 → 費率查證 → 小額實盤。
      後四關全部標「未做」——板上任何一列只過了第一關
> **掃描帶 vs 錄製帶：同一個配對差到 11 倍（2026-09-03 量測，寫下來以免
> 有人拿掃描數字當收益）**。掃描盤的帶是「**只取正的那些樣本**」的 p90
> （08-30 註冊時就這樣寫），錄製端的帶是**全部分鐘**的 p90。某一側越少
> 為正，兩者差越多：BTC 買側只有 **12.6%** 的樣本為正 → p90(全部) **0.449**
> vs p90(只取正) **5.105 bps**。把修正算式套到掃描盤上會得到「合計
> $333/天」這種數字，**它不能當收益讀**——升格後用錄製端的定義重量一次
> 多半會大幅縮水。這不是掃描盤壞了（它是排名不是判決），是階梯存在的理由。
> 不因此修改凍結的升格指標；偏誤方向已記錄，判決端本來就更嚴。

- [ ] 3 天後（~09-06）跑 `scan_rank.py`，看非加密配對能不能擠進前段
- [x] **費率查證完成（2026-09-03）——這一關把整條線的答案改了**：
      HL 官方文件 taker **0.045%（4.5 bps）**、maker 0.015%；HIP-3 部署者
      可再加 0–300% 的費用分成（growth mode 0–100%）。查 `meta` 的
      `deployerFeeScale`：**io / xyz / para / mkts 全部是 1.0 + growth
      mode** → **交易者付的是 HL 標準費率，builder dex 不是零費**。
      Lighter 相反：標準帳戶**排程寫死 0 maker / 0 taker**。
      所以 §0.75 家族的「兩邊 taker 0 bps」不是費率表，是 **Entropy 的
      推薦返佣 100%**（上游 README 自己寫 Tier 4 referral, 100% rebates）
      ——**促銷，不是結構**，而 §0.75 登記時就寫過它有內建過期日。
      後果：一趟來回在 HL 腿要付 **2×4.5 = 9 bps**，所以需要
      **帶 > 18 bps** 才有得賺；黃金的帶是 4.65 bps。**沒有返佣，深度
      100 倍的戰場一樣是負的。**
- [x] **CEX 兩腿接進掃描（2026-09-03，使用者：「我 CEX 都有 50% 返佣而且
      那些商品他們也有」）**。查證屬實：OKX 有 XAG/SPY/QQQ/NVDA/TSLA/AAPL/
      COIN/MSTR/SPX，Bitget 另有 **XAUT/PAXG（代幣化黃金）、COPPER、
      NDX100**。Bitget 的合約端點**自己公布費率**（XAUT maker 2 / taker
      6 bps），已存進 `universe.json`。
      **50% 返佣把門檻換了一格**：CEX 腿約 2.5–3 bps，配零費的 Lighter →
      需要帶 **>10–12 bps**（對照:未返佣的 HL 腿要 18 bps）。
      掃描器 v4：配對 **255 → 1,629**、9 個場館、每輪 66 秒（週期改 180 秒）。
      首輪非加密快照：SILVER@xyz-okx 16.1 bps（OKX 側 **$224k**）、
      SPY@lighter-okx 14.1 bps（Lighter 側 **$500k**）、GOLD@xyz-bitget
      20.0、COPPER@xyz-bitget 14.8。**深度比原家族高三個數量級。**
      同時修了一個靜默降級：某個場館的 universe 抓失敗會讓配對數從 1,629
      掉到 1,068 而只留一行 log → 改成沿用上一份好的（`_keep`）。
      **警告**：這些是單一快照不是 p90，而且 xyz↔CEX 之間的價差很可能有
      結構性成分（USDT vs USDC、預言機、股票收盤時段），跟 USDG 基差同型
      ——**收斂關是唯一能分辨的東西**，別拿快照當收益。
- [x] **讀了錄價程式作者自己的實戰紀錄（2026-09-03，使用者轉貼；edgeX 60 天）
      ——三件事直接打到我們的算式**：
      1. **他不吃單，他掛單**。文中列了三種執行法（掛 A 成交→吃 B；掛 A→
         試掛 B→不成交才吃；兩腿都掛→一邊成交立刻取消另一邊改吃）。
         我們一路假設「四次吃單」＝最保守的那一格。改成一腿掛單後
         **Lighter×Bitget 的門檻從 12 bps 掉到 4 bps**、xyz×Lighter 從 18
         到 6。重算 3.75 天掃描資料：存活從 **3 側／$6.40** 變成
         **58 側／$50.65 上限**。`fees.py` 已加 maker 費率與三種執行模式，
         **預設仍是四次吃單**（保守值不動，掛單是選項不是假設）。
      2. **他點名「底層資產必須完全一致」**，並舉例 USDT vs USDC 的價差會
         偽裝成跨所價差——**正是我們對 USDG（lighter-rh）與 XAUT／PAXG
         的警告**，獨立佐證。
      3. **他監控訂單簿的每次變化，不是 K 線**。我們是 2 分鐘快照／1 分鐘
         bar，差兩個數量級。升格的配對要開 websocket。
      其餘可抄的：每筆成交後自動對兩邊倉位、監控「預期成交價 vs 實際成交價」
      偏離、近 X 分鐘損益異常告警、IP 白名單＋金鑰登入、TG bot 一鍵減倉／
      暫停。伺服器建議東京 AWS（多數交易所在那裡）。
- [x] **掃描器 v5：累積深度（使用者核准）**。頂檔深度是唯一被記錄的規模，
      而它**嚴重低估且不均勻**：實測 3 bps 內是頂檔的 **4–5 倍**（中位），
      極端如 `BTC@lighter` 頂檔 **$25.6**、1 bps 內 **$37,194**（**1450 倍**）
      ——Lighter 的簿口在觸價只掛很小、量在後面。這會系統性地把 Lighter
      腿的配對排到後面去。現在每列多記 8 欄（兩腿×買賣×1bps/3bps）。
      寫進**新檔名** `scan_v5_YYYYMMDD.csv`（不在既有檔中途換 header——
      那會讓之後每一列錯位；`scan_*.csv` 的 glob 兩個都收得到，無資料損失）。
      **凍結的升格指標不動**，仍讀頂檔；新欄位只給容量估計用。
- [x] **全線復審（2026-09-03，使用者：「重新審視一下整個策略有沒有 bug」）
      ——找到一個真 bug，其餘是已知假設**：
      1. **真 bug：Bitget 的深度被除以 10,000**。掃描器把合約端點的
         `sizeMultiplier`（0.0001 for BTC、0.01 for XAUT）當成合約面值，
         但 Bitget 的 USDT 永續**簿口本來就以幣為單位**（實測 BTCUSDT
         頂檔 1.8754 = 1.8754 顆 = **$145,718**）。OKX 相反，它的量是
         **張數**、`ctVal` 才是每張幣數（實測 0.01，用法正確）。
         後果：每一個 Bitget 腿的簿口都被記成空的，而**深度是升格指標
         的乘數**——等於把剛加進來的 CEX 腿自己排到最後面，正是加它們
         的相反。修法 `mult=1.0`，並在 `scan_rank.load()` 丟掉修正前
         （ts < 1788432000）的 38,302 筆 Bitget 列——**這是儀器修正，
         凍結的指標算式一個字沒動**。修正後 XAUT 3 bps 內深度
         **$346k–563k**（原本會記成 $3.5k–5.6k）。
      1b. **last-good 場館清單改成落地檔＋抓取重試三次**，並**反向證明過**
         （讓 `lighter_universe` 必定拋例外：有快取時 1,632 配對、Lighter
         兩條鏈的 216+46 個標的照樣在；清空快取重跑掉到 945 配對、兩條鏈
         歸零 → 測試確實在測東西）。原本快取只在記憶體，而程序由 bat 重啟
         迴圈管理，重啟撞上 Lighter 報錯就整個 refresh 間隔少一個場館。
         注意：**現正在跑的那個程序還沒走過「從檔案載入」那條路**（它啟動
         時檔案還不存在），下一次重啟才會第一次執行到。
      2. **順手清掉重複的掃描程序**：`run_scanner.bat` 有重啟迴圈，
         08-30 與 09-03 各有一個 cmd 迴圈在跑 → 兩個掃描器同時打同一組
         公開 API（先前的限流就是這樣來的）。現在只剩一條。
      3. **不是 bug，是已知假設，但要看見**：掃描盤現在的前兩名是
         `OAI@IO-lighter*`，帶 **428/436 bps**、簿口 **$89/$61**。
         四位數 bps 的「價差」配上百元的簿口，形狀跟 BTC 對照組
         09-01 那次（$97.8/天、episodes=0）一模一樣——**pre-IPO 標的
         在不同場館根本是不同的標記，不是會回來的價差**。掃描盤沒有
         收斂關（階梯第三格寫著「未做」），所以它會、也應該把這種東西
         排在前面；擋它的是升格後的 7 天錄製與收斂關。**預測寫在這裡：
         OAI 兩列會死在收斂關。**
      4. **控制組再次亮燈，且這次是 CEX 腿**：`BTC@HL-okx` 帶 3.31 bps、
         簿口 $46k → 排名指標 $571/天，是全板最大。BTC 對照組**依定義
         不可升格**，但它的帶就是雜訊底：CEX 腿的雜訊底是 3–5 bps，
         所以 CEX 配對要 >16 bps（2× 全體 BTC 最大帶 8.0）才算數。
      5. **查過沒問題的**：OKX `ctVal` 用法、Bitget `limit=25`（它自己
         回 100 檔，`_cum` 反正在 bps 界線就停）、`_legs()` 對
         `lighter-rh` 這種帶連字號的場館名解析正確、公開面（`arb_publish`
         → 端點 → 網站）沒有任何美元數字、`scan_v5_*.csv` 有被 glob 收到、
         每小時班車三支都在跑（premium_verdict → scan_rank → arb_publish）。
      6. **仍是假設不是事實（不動，但別忘）**：`IO` 的 100% 返佣、OKX 的
         標準 taker 費率、掛單成交率、收斂關用的是「均價偏離自己的中線」
         而帶來自「可成交邊際」——後者比前者小，所以偏離次數偏樂觀。
- [x] **判決程式現在讀真實費率（2026-09-03，這一條在 09-04 判決之前必須做）**：
      凍結判準的括號寫著「**fees are 0+0 on this pair**, so net == raw
      executable room」——那句話在 09-03 被自己的費率查證推翻了。所以
      `premium_verdict.py` 改成 import `fees.py`（逐配對明確登記場館，
      不靠字串比對），每個配對印「需要帶 X bps（含返佣）／Y bps（只看
      費率表）」，每一側多四個欄位（兩種假設下的每筆淨值、`passed_at_fee`、
      未查證的腿）。**凍結的 1.0 bps 門檻一個字沒動**——判準沒被改，是
      它的前提被推翻了，所以把前提攤在旁邊，不是把門檻挪走。
      用已知答案強制走過判決路徑（記憶體內把 GATE_DAYS 改 1，不寫產物）：
      **SNDK 兩側都過凍結門檻，但那完全靠 IO 那條「100% 返佣」——照費率表
      每筆是 sell −7.99 / buy −5.08 bps**；BTC 對照組需要 18 bps 而帶只有
      6.65，**兩種假設下都是負的**。順手改正對照組的判決措辭（原文「兩邊
      深簿零費率」已是錯的：Lighter 是 0，HL 不是）。
      `arb_publish` 的錄製家族每一側也跟著帶費率現實上牆——先前只出帶寬，
      讀起來像「有空間」，而費率表可能早就吃掉它了。
- [x] **錄價全家死了 46 分鐘，看板抓到（2026-09-03 21:09→21:55 +0800）**：
      七個錄價程式與掃描器在 21:07~21:09 同時停止，`runner.log` 最後一行是
      **`^C`**——是中斷訊號，不是崩潰、不是網路。restart 迴圈連同 cmd 一起
      被帶走，所以自我修復沒有發生（迴圈本身死了）。**成因無法確定，而且
      不排除是本 session 自己的指令造成的**（同時段有多個前景／背景指令在
      同一個 console 跑；Windows 的 Ctrl+C 是送給整個 console 行程群組的）。
      照開機啟動項 `entropy-arb-record.bat` 原樣重啟（每支自己的 console
      視窗），驗證方式是產物：七支 minutes.csv 與 scan_v5 全部恢復每分鐘
      更新、看板九列全綠。**損失 46 分鐘資料，09-04 的 SNDK 判決照原判準
      讀（分鐘數少 46，占 7 天的 0.5%，不改判準）。**
      教訓兩條：(a) 30 秒 restart 迴圈只能救「子程序死」，救不了「整個
      console 被中斷」——耐久化的層級不夠；(b) **能抓到它的只有產物新鮮度
      看板**，而它的上限（錄價 1h、掃描 0.5h）剛好夠。這是 §0.55 那套
      守衛第一次在套利線上真的擋到東西。
- [x] **看門狗上線（2026-09-03）**：`arb/ops/arb_watchdog.ps1`
      ＋ Windows 排程 `EntropyArbWatchdog`（每 5 分鐘，與既有排程同款的
      互動模式）。它是 .bat 內 30 秒迴圈**上面那一層**：迴圈只能救「python
      子程序死」，救不了「整個 console 被帶走」（今天的死法）。規則：
      (a) 用**程序命令列**判活不用檔案 mtime——在活著的程序上再拉一份就是
      同日的雙掃描器限流 bug；(b) 重啟走與開機啟動項**同一支 .bat**，
      修復路徑＝開機路徑；(c) 每輪都寫一行 log（含「all 8 alive」），
      只在動作時才寫的看門狗在一切正常時看起來像死了。
      **反向證明**：殺掉 ZEC 的 cmd 迴圈＋python → 看門狗一輪只重啟 ZEC、
      八個成員各一份、再乾跑報 all alive；排程觸發一次 → log 落地、ZEC
      minutes.csv 恢復每分鐘寫。沒用「使用者未登入也執行」——那需要存密碼，
      而今天的死法是登入中的 console 中斷，不是登出。
- [x] **IO 返佣查證完畢——是 25%，不是 100%（2026-09-04，使用者 Entropy 推薦頁截圖）**：
      頁面寫 Benefit **25%**（自己交易的費率折扣）、Rebate 60%（推薦別人可分到的收入）。
      上游 README 的「Tier 4, 100% rebates」講的是**推薦人分到被推薦人手續費的比例**
      ——是收入不是折扣，兩個數字同一個名字。08-28 那句「兩邊 taker 0 bps 當場驗證
      為真」因此是錯的，來源要重查。`fees.py` IO 改 rebate 0.25、verified。後果：
      IO+Lighter 門檻吃單 **13.5** / 掛單 **4.5** bps；SNDK 開盤帶 6.34 → 吃單 −3.6 /
      掛單 **+0.9**；NBIS 14.5 → 吃單 +0.5 / 掛單 +5.0。**這家只剩掛單一條路**。
- [ ] **唯一能救這條線的問題**：xyz（或任何有深度的 builder dex）能不能
      拿到同等的返佣／做市商減免。拿得到 → 黃金/白銀/原油那批 $12k–35k
      的簿口值得錄；拿不到 → 這條線的天花板就是 io↔lighter 那幾個配對，
      每天幾塊錢
- [ ] 升格前查兩腿的**保證金是不是分開的帳戶**（HIP-3 dex 之間、
      以及 HL 主場 vs dex）——斷腿風險與資金效率都由這件事決定

### 1.13 預註冊：新聞/LLM 發現層 vs 掃描器（2026-09-04 登記，**未開工**）

全文 `arb/docs/DISCOVERY_VS_SCANNER.md`。使用者在評估「Grok bot 報全市場機會」
會不會比掃描器好、以及讀到錯誤資訊的風險。結論：**不是取代關係，而且風險大小完全
取決於接線位置**——接「發現」層錯了只花幾天無用錄製（下游接得住），接「執行」層
則繞過收斂關／深度關／成本七桶／凍結判準，而且**可被餵**（公開文字驅動下單＝任何人
都能貼餌）。**硬規則**：bot 輸出只能是問題不是答案、只能改掃描器的場館清單、
永不進下單路徑。

**兩週對照測試判準事前寫死**：D1 bot 報出而掃描器沒覆蓋的機會數（=0 → 不開）、
D2 其中經量測真的存在的比例（<50% → 不開）、D3 漏報數（記錄不當閘門）。
**開的條件 D1≥3 且 D2≥50%。** 預期 D1 不會是 0——Polymarket、rToken、HIP-3 oracle
都來自文字——但它們是「哪裡開了新戰場」不是「現在哪裡有 50 bps」。

### 1.12 HIP-3 與 Entropy oracle 設計對套利的意義（2026-09-04 使用者提問）

全文 `arb/docs/HIP3_ORACLE.md`。**HIP-3**＝任何人質押 **500k HYPE**（維持 183 天）
就能在 Hyperliquid 上部署自己的永續市場；前 3 個標的免拍賣、之後走荷蘭拍；
**部署者控制 oracle 價格、槓桿上限、合約規格與 haltTrading**，費用分成 0–300%。
**核心：撮合／清算／保證金引擎由 HL 提供，但「價格是什麼」由部署者定義**——
io／xyz／para／mkts 共用同一條鏈與風控，**各自有自己的 oracle**。

**使用者的觀察**（Entropy 依簿口深度動態調整「市場價 vs 外部價」的權重 → 流動性越好
市場定價權越高 → mark-oracle 偏離小 → funding 低）**方向對但證據薄**。實測各 dex
|funding| 中位：io **0.208**、mkts 0.000、xyz 0.500、HL core 1.000、para 5.124 bps/8h——
但 n=7 對 n=233、資產類別不同，不可比。乾淨檢定是**同標的跨 dex**（25 個重疊）：
io 唯一有 OI 的重疊標的 ANTH funding **+0.012**（極低），但可比樣本只有一個；
para（OI 也小）反而最高 → **funding 高低混著流動性與 OI，不只是 oracle 設計**。

**對套利的兩面**：
- **正面**：低 funding ＝ 持有型套利的第 4 桶成本低。**但對我們現在做的沒意義**——
  §1.07 已量過，5–15 分鐘的持有時間下 carry 進不了帳。要用它必須先把持有拉到以天計，
  那是另一本風險簿。
- **反面且更重要**：資金費率是**把 mark 拉回 oracle 的機制**。oracle 若貼著自己的簿口，
  價格偏離外部世界時**沒有力量把它拉回來**——**funding 低不是因為價格對，是因為它把
  自己的價格當成對的**。後果：**io 上的價差更可能是結構性偏移而非可收斂價差**。
  而我們的收斂關量的是「回到自己的 6h 中線」——**兩個場館一起漂時，收斂關會過但錢
  賺不到**。io 家族現在收斂 98–100% 看起來很好，但那是相對自己的中線。
  **要區分「真收斂」與「跟著漂」需要外部錨**（§1.10 那條腿）。

- [ ] 掃描器加錄逐 dex 的 funding 與 mark-oracle 偏離（純收集端，meta 本來就在讀）
- [ ] 外部錨——沒有它分不出真收斂與一起漂

### 1.11 Bitget rToken：同所現貨-永續（2026-09-04 使用者提問，當場量測）

全文 `arb/docs/RTOKEN.md`。**有幫助，但幫的地方不是「多一個場館」，是三個成本桶
同時消失**：每一檔 rToken 在 Bitget 都有同名股票永續（rNVDA ↔ NVDAUSDT），
**同場館現貨-永續**配對 → 第 5 桶轉移歸零（同一帳戶）、第 3 桶資金由 UTA 跨資產保證金
吸收（rToken 本身可當抵押、折算率最高 95%）、第 7 桶的 USDG 脫鉤歸零（同 USDT 計價）。
**第 3 桶那個「$300 緩衝隱含 size ≥$200」的兩難在這裡不存在**——持倉本身就是保證金。

⚠ **當場擋掉一個假數字**：ticker 說 rSPY 24h 量 **$29.6B**、rNVDA $23.6B，而同交易所
BTCUSDT 現貨只有 $424M——**一檔代幣化 ETF 做 BTC 的 70 倍不可能**，八成是做市激勵刷量。
成交量不可信，只看簿口。實測簿口：

| 標的 | 現貨價差 | 現貨 3bps 深度(買/賣) | 永續 3bps 深度 | 現貨vs永續中價差 |
|---|---|---|---|---|
| rSPY | 22.8 bps | $28k / $48k | $4.6M / $1.5M | **+8.7 bps** |
| rNVDA | 1.7 | **$0.5k** / $98k | $151k / $648k | +4.1 |
| rTSLA | 2.2 | **$0.4k** / $10k | $754k / $315k | +5.3 |
| rAAPL | 21.9 | $0.7k / $1.9k | $269k / $567k | −3.2 |
| rMSTR | 16.9 | **$19** / $1.8k | $42k / $249k | +11.6 |

**三個讀法**：(a) 中價差 3–12 bps 跟現有家族同量級，**沒有比較肥**；(b) **容量由
rToken 那側決定**，現貨深度比永續薄 2–3 個數量級且極度不對稱（rMSTR 買側 $19）；
(c) 現貨自己的買賣價差（rSPY 22.8、rAAPL 21.9）**大於中價差**——**只能掛單不能吃單**。

**收益其實是三層**：基差（要收斂才拿得到）＋資金費＋**股利**（扣 30% 稅後入帳，
加密永續沒有這層）。第三層是這個結構獨有、也最需要查證的：空永續那側要不要付
對應股利，決定它是不是結構性正 carry。

**風險是換形狀不是變小**：兩個場館的風險變成**單一交易所的集中風險**（Bitget／
Reality／UTA 規則一起中，機率低但沒有分散）；官方風險揭露自己寫「週末/盤後價格跳空
可能快速突破追保門檻」與「UTA 跨資產虧損可疊加」——**後者正是 09-03「500 萬 U 一夜
爆倉」的機制**。

- [ ] Bitget 現貨 rToken 接進掃描器（`bitget-spot`），與既有 Bitget 股票永續自動配對，
      走既有凍結指標與**收斂關**
- [ ] 查證：空永續那側要不要付股利（決定第三層收益是否存在）
- [ ] **預期先寫下來**：多半是**結構性偏移**而非可收斂價差（現貨貼著永續下方＝資金
      成本），那樣它就不是套利而是 cash-and-carry 持有型，判準要換一套（§1.07 教訓）

### 1.10 候選賽道：鏈上股票代幣 vs 真實股價（2026-09-04 使用者提案，登記不開工）

全文 `arb/docs/TOKENIZED_EQUITY.md`。**登記為候選、不預註冊、不開時鐘**，因為它有
一個結構性問題必須先回答：**你是不是 AP（授權參與者）**。是 AP（完成 Backed KYC、能按
NAV 申贖）→ 收斂是**合約義務**，那是真套利；不是 AP → 只是押注別人會把它套回去，
跟我們現在做的同一種形狀，而且多了 gas 與 AMM 滑價。**第一步不是寫程式，是問
Backed／Ondo／Dinari 散戶能不能申贖、門檻多少。**

已查證：xStocks 是 SPL 代幣、1:1 真股託管、可向 Backed 按 NAV 贖回但**限 onboarded
KYC 客戶**；流動性集中在 TSLAx／NVDAx／CRCLx，Raydium/Jupiter **建議滑價容忍
0.1–0.5%（10–50 bps）——已經比我們家族的帶寬一個數量級**；美股開盤時價差被壓緊，
**週末盤後才打開**（但那是跨週末持倉風險，不是套利，§1.01 在股票永續上量過同一件事）。

**不是 AP 的退路**：用**股票永續當對沖腿**（那 188 個 TradFi 永續已在掃描器裡），
兩條腿都 24/7、不必等開盤、收斂不依賴發行商。最小第一步是把 **Jupiter swap 報價**
當一個新場館接進掃描器（只讀），自動走既有凍結指標與收斂關，**不需要新判準**。
**先決條件**：`tokens.jup.ag` 今天從本機 DNS 解析失敗——跟 Polymarket 同款嫌疑，
要東京 VPS 才能確認。

- [ ] 問發行商：散戶申贖門檻（這一題的答案決定要不要投工程）
- [ ] Jupiter 報價接進掃描器（要 VPS 才測得了連通性）

### 1.09 未完成工作交接（2026-09-04 收工）

**全文 `arb/docs/NEXT_SESSION.md`**——分成四塊：**A 卡在使用者身上**（7 項：M1 下單、
HL 位址、CEX 唯讀 key、東京 VPS、邀請碼折扣、xyz 返佣談判、保證金是否分帳戶）、
**B 我可以做的 10 項依序**（B1 逐行審現有下單路徑 → B2 憑證缺口 → B3 補 maker 路徑 →
B4 五個 kill switch → B5 控制通道 → B6 儀表板 → B7 成本假設換量測 → B8 掃描器兩個
儀器缺陷 → B9 公告監看 → B10 §1.04 未排期項）、**C 自己會跑的時鐘 8 條**、
**D 已定案不要做的 5 條**。

**下一個 session 的第一件事**：讀 `NEXT_SESSION.md` 的 A 段——那幾件不做，B 的大部分
做不下去。B1（逐行審 `send_taker`，特別是未決成交那段）不需要任何憑證，隨時可開始。

### 1.08 套利·$50 一週小額實盤規格（2026-09-04 凍結，使用者「可以」）＋ 儀表板架構

規格全文 `arb/docs/LIVE_50U_SPEC.md`。**這一週的產出不是損益，是把成本名冊裡的
假設換成量測值**——七件事各一個事前判準（M1 帳單費率／M2 掛單成交率／M3 逆選擇／
M4 兩腿延遲／M5 資料過期就停／M6 存貨漂移／M7 劃轉成本），收工用量到的值重跑
`cost_model.py`，看「量到%」從 43% 升到多少。範圍鎖死：只做 NBIS 與 NVDA_LL 兩個
配對、每所 $50、總曝險 $300、**絕不重平衡**（$1 提幣費攤在 $50 上是 2%）、1× 槓桿
（清算那一桶消失）。最小下單查證：OKX 股票永續 $1.9–7.7、Bitget $5、Binance $5、
HL $10——**$50 開得了單**。

**速度：實測本機到各場館往返（各 5 次中位）**——Lighter 32ms、Lighter-RH 65、
Binance 65、HL 80、Bitget 88、OKX 95。三個結論：(a) 機會壽命是**分鐘**（收斂中位
5–15 分），進場慢 100ms 不會錯過；(b) **真正吃延遲的是撤單**——掛單模式下價格動了
要在被吃掉前撤掉，這正是 M3 要量的逆選擇；(c) **Railway 沒有東京區**（US/EU/新加坡），
而**沒有任何一區同時靠近兩條腿**（新加坡近 HL/OKX、遠 Lighter 系；美國相反）。
→ **執行引擎不放 Railway**，用 $8–20/月的東京 VPS，順便解 Polymarket 的 DNS 封鎖。

**儀表板放 Railway 沒問題，但只顯示不執行**，沿用本 repo 既有的單向流
（`agent-boundary.md`）：東京 VPS 引擎 → 每 30 秒寫 `arb_live_*` 表 → agent-mcp
`/public/arb-live` → jarvis 頁面。產品端一行交易程式都不寫，跟現在的
`/public/arb-status` 完全同款。頁面內容全部是引擎已經在算的東西（帶／扣七桶淨／
深度分級／兩腿時間差／掛單成交撤單／成交後漂移／各所餘額偏斜／守衛紅燈），
公開面規則照舊：只出百分比、方向、時間。

- [ ] 東京 VPS 開機（X1，$8–20/月）——執行引擎與 Polymarket 掃描都卡在這
- [x] **M1 的讀取端已就緒**（`research/arb/fee_receipts.py`，2026-09-04）：讀四個場館
      過去 N 小時的成交回執，算**帳單上實際付的 bps**（分 maker/taker），與 `fees.py`
      並列，差 >0.5 bps 就標「以回執為準改 fees.py」。**這支只讀，沒有任何下單或
      簽單程式碼**——下單是操作者的動作。`HL_ACCOUNT_ADDRESS` 一設就能讀（HL 的
      `userFills` 是公開的，不需要金鑰）；CEX 要唯讀 API key。
- [ ] **M1 執行（2026-09-04 使用者：「今晚我來搞全部交易所註冊帳號」）**：
      清單 `research/arb/M1_CHECKLIST.md`；**交易所端開通步驟 `research/arb/ONBOARDING.md`**
      （每家要錢包還是 API key、批 A 唯讀先做／批 B 下單再開、Lighter 兩鏈要兩個帳號）。
      **§API 隔離規格已凍結**（2026-09-04 使用者要求）：四個維度——帳戶（獨立錢包／
      子帳戶）、金鑰（一把 key 一個用途，**HL 必須用 Agent 錢包不是主私鑰**，永不發
      提幣權限）、程序（交易 key 只在 VPS，儀表板唯讀不碰金鑰，不進 git 不進 Railway）、
      網路（VPS 固定 IP 綁白名單）。附 8 項上線前檢查表，**任何一項打不了勾就不下
      第一筆真單**。——註冊要點（子帳戶、只勾交易不勾提幣、
      HL 用獨立錢包、**Lighter 兩條鏈各一組**）、四筆最小單（OKX $2.3／Bitget $5／
      Binance $5／HL $10）、要貼給我的唯讀憑證，以及**同一晚可以順便做完的四項查證**
      （M7 劃轉成本、第 3 桶保證金率、A7 保證金是否分帳戶、rToken 抵押率）。
      四項一起做會把 `cost_model.py` 的「量到%」從 **43% 推到 ~70%**。
      ⚠ 用邀請碼時記得 §1.11 的教訓：**要問「我自己交易的折扣」（Benefit），不是
      「推薦別人的分成」（Rebate）**——Entropy 那次就是這兩個數字同名，錯了五天。
- [x] **B2 完成（2026-09-04）：兩條 Lighter 鏈各自的憑證**（entropy-arb 本機倉）。
      `_lighter_creds(venue)` 依部署選前綴，`LIGHTER_RH_*` 覆寫、未設則回退到
      `LIGHTER_*`——**既有單腿設定逐位元不變**。已知答案測試三種情境全過：
      兩腿分開（111/222 不同 key）、回退（RH 清空 → 111）、mainnet 腿不受影響。
      `.env.example` 補上新變數與中英說明。
      附註：上游 3 個 `test_engine` 失敗是 **pytest 事件迴圈隔離問題**（單獨跑會過、
      未改動的樹上也失敗），與本次無關，已查證。
- [ ] **憑證缺口（剩下的）**：(a) HL 要用 **API wallet**——能下單撤單
      但**永遠不能提幣**，把 DEX 腿的金鑰風險拉到跟 CEX 一樣（官方文件明載）；
      (b) **兩條 Lighter 鏈現在共用同一組憑證**（`config.py` 兩處都讀同樣的
      `LIGHTER_*`），而 mainnet 與 RH 是不同鏈不同帳號——`NVDA_LL` 兩腿都是 Lighter，
      **上線前必修**，錄價階段看不出來因為它不簽單；(c) OKX／Bitget／Binance 的下單
      認證**尚未實作**（現在只有公開簿口讀取）
- [x] **B1 逐行審下單路徑完成（2026-09-04）**：`research/arb/B1_ENGINE_AUDIT.md`。
      **核心設計是對的**——未決不假設沒成交、對帳只採信鏈上真相**不下修正單**、
      對沖前檢查簿口新鮮度、`reduce_only` 寫死、連續錯誤 HALT、`RECONCILE_GRACE_SEC=5`
      防「剛成交就對帳」的幻影震盪（作者註解直接寫了 Lighter REST 落後 ws）。
      **沒有找到「以為沒成交就重送」的邏輯**——那正是兄弟 $1.1M 的病灶。
      四個缺口：**G1 沒有累積差額硬上限**（兄弟參數裡的 `maxDelta: 800` 沒有對應物，
      現有的 cap_usd／reduce_only／consec_errors 都是間接防護，**這是五個 kill switch
      裡最該先加的**）、**G2** `settle_timeout` 逾時回 `filled_base=0` 是猜測，而
      `_execute` 先用了它才檢查 `unresolved`（對帳會修回來，但那之前本地部位是錯的，
      而 `_scan` 可能下新單）、**G3** 沒有 maker 路徑、**G4** 兩條 Lighter 鏈共用憑證。
- [x] **B4 第一個 kill switch 完成（2026-09-04）：`max_net_base`**（entropy-arb 本機倉
      `5ba9347`）。B1 的 G1——`_hedge` 只縮當下的差額，**沒有任何地方限制差額能長到
      多大**。修法：`risk.max_net_base`（預設 3×`net_tolerance_base`），在
      `_maybe_hedge` **開頭**檢查（因為失效形狀是「對沖一直失敗而差額一直長大」，
      這種必須自己停，不能等人發現），超過即 `halted=True` + critical log，
      **不下任何單、不碰既有部位**；HALT 單向，重啟時 `strict=True` 會重讀真實部位。
      順帶修 **G2**：`unresolved` 時 `filled_base` 是猜測（逾時回 0.0），加 warning
      說明本地部位在對帳前可能是舊的。
      **五種情境已知答案測試全過**：超限 HALT 且零下單／上限內照常對沖（無誤觸）／
      `cap=0` 停用時行為與改動前相同／容差內不動作／預設值＝3×tol。
- [x] **熱路徑審視（2026-09-04 使用者給的八條原則逐條對帳）**：
      `research/arb/HOTPATH_AUDIT.md`。**照第 8 條先量再說**——
      錄價 `writerow+flush` 中位 **6 µs**／p95 12／最大 139；風控 `sum(2 腿)` **0.5 µs**；
      單次 `log.info` **14 µs**（執行路徑上 6+ 個，合計 40–90 µs）。
      **相對 32–95 ms 的網路往返是 0.1%，而我們的決策週期是分鐘**——
      **結論：不需要動大手術**（第 8 條）。
      校準：**我們不是高頻系統**（收斂中位 5–15 分、掃描 180 秒），所以第 1 條
      「更快下單」部分適用——早已量過，吃延遲的是**撤單不是進場**。
      第 5 條**已合格且是設計出來的**：所有守衛都是 O(1)、不做 I/O 不呼叫交易所。
      第 4 條的「延遲預算」我們其實已有，只是刻度是秒（`staleness_sec=10`／
      `settle_timeout`／`reconcile_sec`／`venue_probe_sec`）；maker 上線要新增
      **撤單預算**（作者 repo 用 5 秒）。
      第 6 條有實測反例：flush 中位 6 µs 但**最大 139 µs（23 倍）**、OKX 中位 95 ms
      **最慢 161 ms**——**這正是 `staleness_sec` 這種絕對時限比平均延遲更該存在的理由**。
      **新缺口 G5**：錄價器與引擎**共用事件迴圈**且失敗路徑未驗證——asyncio task
      的例外若無人 await 會靜默死亡，引擎會繼續交易並以為錄價還在跑
      （mistake.md 2026-04-22「進程活著、功能死亡」那一族）。修法：done-callback
      設存活旗標並印在狀態行，**不停交易**（旁路壞不該停熱路徑）但要看得見。
- [x] **G5 ＋ B4 日損上限完成（2026-09-04，entropy-arb `248ff6b`）**：
      **G5**（原則 7 降級）錄價器 done-callback → critical log ＋ `_recorder_dead`
      旗標印在狀態行；**不停交易**（旁路壞不該停熱路徑），關機的正常取消不誤報。
      **B4 日損上限** `risk.max_daily_loss_usd`：session MTM 跌破即 HALT。
      所有硬停改走統一的 `_risk_halt()`，訊息格式一致、都會觸發對帳。
      **原則 5 已實測驗證**：兩個守衛合計 **1.9 µs**（p95 2.5），只做兩腿加總，
      不查 DB／不寫檔／不呼叫交易所。
      **六種情境已知答案測試全過**：跌破日損 HALT 零下單／未跌破不誤觸／停用時不動作／
      差額上限回歸／錄價拋例外升旗但**交易續行**／關機取消不誤報。
- [x] **B4 完結（2026-09-04，entropy-arb `2eb5d54`）**：`max_gross_usd`（兩腿名目
      合計的絕對上限，兜底設定打錯）、`max_consecutive_stale`（連續 N 次盤口過期即停機
      ——沒有這條「資料源死掉」和「行情安靜」長得一模一樣）、**HALT 語意改正**：
      原本停機後連對沖也停，斷腿的裸露會留在原地，而 `max_net_base` 那條特別自相矛盾
      （因為兩腿分家而停，然後停止把它們合回去）。改成 **HALT 只擋開新套利、
      仍允許 reduce-only 平掉既有裸露**，次數上限 `halt_flatten_attempts`（預設 3）
      避免失敗的對沖無限迴圈；設 0 = 完全凍結（可選）。
      五種情境測試全過：名目超標即停（差額為 0 也擋）／正常不誤觸／停機後平裸露三次
      即止／`flatten=0` 完全凍結／差額上限回歸。
- [x] **極端行情與斷腿的應變寫成文件**：`research/arb/CONTINGENCY.md`——逐情境寫
      **程式現在的實際行為**（不是設計意圖）＋11 條守衛全表＋$50 規模下的最壞情況。
      **誠實標出最弱的一環：沒有波動熔斷**（價格劇烈跳動但兩邊 book 都新鮮時照常交易），
      在 $50、1× 槓桿下不致命，**但放大前必須補**。
- [ ] **引擎缺的那一半：maker 路徑**（2026-09-04 查證）——錄價程式的
      `venue_hl.send_taker` / `venue_lighter.send_taker` 是完整的（IOC、reduce_only、
      成交輪詢、簽章），但**只有吃單**。而成本分析的結論是掛單才活得下來，所以引擎
      要補 post-only 下單＋撤單＋5 秒逾時。這是新的、碰錢的程式碼 → 上線前逐行審
      （CLAUDE.md「下單程式碼要逐行審」）
- [ ] `arb_live_*` 表與 `/public/arb-live` 端點（引擎有資料之後才做，避免空殼）
- [ ] 儀表板頁（jarvis）——端點有資料之後；**唯讀、一顆按鈕都沒有**
- [ ] 控制通道：TG 四個動詞 `pause`／`resume`／`flat`／`size`，冪等、不經儀表板、
      儀表板掛掉仍可用（規格 §6；理由是 mistake.md 2026-06-07 的 GET heal 疤）

### 1.07 套利·持有收租 vs 回半帶就平（2026-09-04 預註冊，使用者裁決「#8 可以試試看」；先 commit 再跑）

凍結規則與三個預測寫在 `research/arb/hold_carry.py` 檔頭：進場同計分器；資金費差對我們
有利時**續抱**，直到溢價穿過中線（吃整個偏離而非一半）、或 carry 翻向、或 24h 上限；
損益＝抓到的偏離＋收到的 carry −一趟來回費用。P1 ≥4 個配對持有優於基準、P2 拿掉最好
的一個仍成立、P3 持有中位 <8h（否則它是基差交易，要另外登記）。選擇窗探索，不是判決。

**結果（2026-09-04）：三關都過，但過的原因不是登記的那個——不開時鐘。**

| 配對 | n | 持有觸發 | 基準 | 持有 | 差 | 持有中位 |
|---|---|---|---|---|---|---|
| SNDK 賣 | 213 | 29 | −5.78 | −5.63 | +0.14 | 0.10h |
| NBIS 賣 | 22 | 3 | −2.40 | −0.99 | +1.41 | 0.20h |
| ANTH 買 | 6 | 1 | −0.96 | +0.04 | +1.00 | 0.23h |
| HYPE 買 | 12 | 9 | −6.80 | −5.49 | +1.32 | 0.19h |
| ZEC 賣 | 56 | 13 | −6.98 | −6.38 | +0.61 | 0.23h |
| BTC／NEAR／NVDA_LL | — | — | — | — | +0.17～+0.74 | 0.08–0.10h |

8/8 配對持有優於基準、拿掉最好的仍 ≥4、持有中位 **0.08h**——三關全過。**但持有中位
5–14 分鐘，這段時間的資金費幾乎是零**，「收租」不可能是增益的來源。照 2026-08-02
的規矩（符合預期的漂亮結果更要查儀器）做消融：**carry 歸零、出場規則不變**——

| | 全規則 | carry 歸零 |
|---|---|---|
| SNDK | +0.14 | +0.14 |
| NBIS | +1.41 | +0.88 |
| ANTH | +1.00 | +0.97 |
| HYPE | +1.32 | +1.29 |
| ZEC | +0.61 | +0.60 |

**增益幾乎全部來自「出場改在穿過中線」（吃整個偏離而非一半），不是 carry**。
carry 只在 NBIS 貢獻了 0.5 bps。所以登記的機制（持有收租）**沒有被支持**；被支持的是
另一件事：出場目標從半帶改成中線，值 +0.1～+1.4 bps，而且**沒有改變任何配對的正負**
（基準 −6～−8 的還是負的）。另註：資金費欄位 09-01 才開始錄，之前的事件全走基準，
所以「兩半」那一格第一半全是 0，沒有資訊。

**處置**：不開「持有收租」的前瞻時鐘（機制不對）。「出場改中線」若要用，另外登記成
出場變體並從零起算——它是執行細節，不是收益軸。使用者的「#8 試試看」試完了：
**在現在的持有時間量級下，carry 進不了帳**；要讓 carry 有意義，持有要以天計，那就是
基差交易，風險簿完全不同，要另立一節才能談。

- [x] 跑（2026-09-04）：三關過但消融證明驅動是出場目標不是 carry → 不開時鐘
- [ ] 若要「出場改中線」：另登記出場變體，從零起算

**同日順帶：#3 的 $300 緩衝跟 size 的關係**（`cost_model.py`，掛單無返佣）：緩衝是
固定金額，所以 size 越小它佔的比例越大——$50 時小所緩衝＝6× 名目，第 7 桶每筆
21–56 bps；$200 時 5.6／5.0／3.8；$300 時 3.9／3.5／2.6。**$300 緩衝隱含每筆 size 至少
$200 才攤得開**，而 S4 說 size 不能超過薄的那一側的深度（家族小所頂檔 $19–420）。
兩個約束夾起來：這個緩衝只在 NBIS（$135–420）與 CEX 腿配對上用得起，SNDK/ANTH 用不起。
**這不是緩衝定錯，是它把「哪些配對值得做」講清楚了。**

### 1.06 套利·成本函數的七個桶（2026-09-04 使用者拆解，`research/arb/cost_model.py`）

**使用者的拆解，逐項變成算式**（每筆來回 bps；`fees.py` 只是第 1 桶）：

| 桶 | 算式 | 現在的輸入來源 |
|---|---|---|
| 1 顯性手續費 | maker/taker × 兩腿 × 進出 | `fees.py`（VERIFIED，除 binance 未查證） |
| 2 滑價 | size 走過頂檔→1bps 內→3bps 內三段深度；超過 3bps 深度＝吃掉整個帶（inf） | 掃描 v5 的累積深度（MEASURED）；錄製家族只有頂檔，×4.5（§1.00 v5 量到的中位比，ASSUMED） |
| 3 資金成本 | (兩腿保證金 + 兩邊閒置緩衝) × 門檻利率 × 持有時間 | 持有時間＝收斂中位分鐘（MEASURED）；保證金率、5% 門檻、1× 閒置緩衝（ASSUMED） |
| 4 持有成本 | −資金費率差 × 持有時間/8h | 錄價 `fund_diff`（MEASURED） |
| 5 轉移成本 | 每次重平衡（手續費 + σ√t 的裸腿風險費）× 每筆分攤次數 | 全部 ASSUMED（2 bps、20 分鐘、每 20 筆一次、σ 60%） |
| 6 營運成本 | P(任一腿失敗 ∪ 停機命中持有期) × 損失 | 失敗率 6%＝掃描器每輪 2975/3164 報得到價（MEASURED）；46 分鐘停機年化（MEASURED）；損失 5 bps（ASSUMED） |
| 7 尾部 | Σ p_i × L_i 年化拖累 × 鎖住資本 ÷ 每年交易次數 | 全部 ASSUMED；每年 <12 筆的配對不攤（會落在一筆上讀成 −800，改標「尾未攤」） |

**跑出來（帶＝凍結判決的 p90；毛＝帶÷2）**：

| 情境 | SNDK | NBIS | ANTH | 零費 GOLD_LL/NVDA_LL |
|---|---|---|---|---|
| 吃單 $200 含返佣：只扣費 → 扣七桶 | −2.8 → **−9.2** | +0.3 → **−5.9** | +1.1 → **−5.1** | +2.6/+3.4 → ≈0 |
| 掛單 $50 無返佣：只扣費 → 扣七桶 | +1.0 → −5.5 | +4.1 → **−2.1** | +4.8 → **−0.3** | 同上 |

**三件從表裡讀出來的事**：
1. **「只扣費」到「扣七桶」差 5–6 bps，而那 5–6 bps 幾乎全是假設**（第 5 桶 2.0、第 7 桶
   2.6–3.9、第 6 桶 0.6）。滑價、資金、持有在現在的規模與持有時間下都是 0——
   **不是它們不重要，是我們現在小到量不出來**（$200 低於頂檔、持有 5–15 分鐘）。
2. **第 7 桶的假設等於每年吃掉鎖住資本的 8.4%**（小所違約 5%×50% + USDG 脫鉤 10%×3%
   + 基差爆開 10%×10%）。這一個桶就決定家族正負，而它一個數字都沒量過。
   要動它只有兩條路：找到公開的違約／脫鉤基率，或降低鎖住資本（第 3 桶的閒置緩衝 1×
   是它的乘數）。
3. **量到的比例 43%（3/7 桶）**。每換掉一個假設就多量到一桶——優先序：第 5 桶（開一次
   真實劃轉就有數字）＞第 3 桶保證金率（各場館文件查得到）＞第 7 桶（最貴、最不可能
   量準，但影響最大）。

**這條線現在的誠實狀態**：以凍結的帶、七桶悲觀假設，**家族在任何情境下都不是正的**；
最接近零的是 ANTH 掛單無返佣（−0.3）與零費對照組（≈0，帶本身只有 5–7）。
把它翻正的不是再找一個桶，是**帶要變寬**——這把問題交回 §1.05（交叉上市窗口）
與 §1.03 的結論（有小所參與的配對才有帶）。

- [x] 七桶成本函數落地、家族逐配對報告（2026-09-04）
- [x] **成本名冊 `research/arb/COST_INVENTORY.md`**（2026-09-04，使用者：「把所有會用到的
      成本列出來，我來想解套方案」）：七桶＋固定成本共 **47 項**，每項標現值（量到／假設）、
      來源、性質代碼（帳／結／工／規／時／率／固）；每桶最大的一項與桶間抵換關係寫在末尾。
      這裡不解，只列——解法由使用者提。
- [ ] 第 5 桶：做一次真實劃轉（HL→Lighter、CEX→HL），記費用與到帳分鐘
- [ ] 第 3 桶：各場館初始保證金率查文件替換 ASSUMED
- [ ] 第 2 桶：錄製家族補錄 1bps/3bps 累積深度（現在只有頂檔，靠 ×4.5 假設）
- [ ] 判決程式加一行「扣七桶淨值」報告列（不動凍結判準）

### 1.05 套利·交叉上市窗口（2026-09-04 預註冊；使用者：「新交易所上了新商品而其他所本來就有，這時候就有套利機會」）

**為什麼是這個問題**：§1.03 殺掉「換大所」（1.07×）、§1.01 殺掉「換時段」——兩者
都在穩態市場裡找**常設**的邊，答案一致：穩態下價差被套到只剩交易成本。那對兄弟
的文章寫的正是他們的邊從來不在「哪裡」，在「**什麼時候**」——場館或市場剛上線的
前幾天、資本還沒到之前。CoinGecko 報告從另一側說同一件事：「後進場的交易所報價
偏高，因為套利資金流進去得少……每有新交易所上線，這種缺口就會在早期重現」。

**所以假設不是 where 是 when**：一個配對的帶是「該配對誕生（第二個場館掛上該標的）
以來的時間」的遞減函數。

**誠實的出處**：這個想法是看 listings 檔看出來的——PONS 08-31 hl_core → 09-01
lighter-rh → 09-02 lighter，然後它是全掃描裡帶最寬的零費配對（45.95 bps）。**看過
資料才有的假設**，所以本節的回測切片只是探索、只能決定要不要開前瞻時鐘；確認要靠
登記之後發生的上市。跟 §0.94 變體 M 同一條規矩。

**先修儀器（2026-09-04，entropy-arb `6a2936d`）**：2,023 筆「listed」事件裡 **2,006 筆
是假的**——某場館 universe 抓取失敗，下一輪整份清單回來，集合差把每一個都叫成新
上市。真的那 17 筆被埋在底下。偵測器改成：前一份快照沒有的場館一律 `seen_at_start`；
同場館同輪 ≥5 個同向變動標 `mass_reappear`／`mass_vanish`；不刪任何列。五個已知答案
測試通過；掃描器已重啟（看門狗順手又驗了一次）。

**凍結（`research/arb/listing_window.py`）**：P1 同一配對 <24h 的帶 ≥ 2× >72h 的帶
（配對內比較，跨標的差異做不出來）；P2 對照組（整段都在的配對，同一把尺）無此衰減
（<1.3×）；P3 ≥60% 的事件同向。全過才開前瞻時鐘＋上市監看，不授權交易。

**回測切片結果（2026-09-04）：跑不出來，不是被否證。** 17 筆真上市對應到 20 個配對，
**沒有任何一個同時有 <24h 與 >72h 的足夠樣本**——掃描窗只有 4 天、多數上市在最後
一天。另外對照組比值 0.55（後段比前段寬）暴露了第二個問題：**掃描器在窗口中途換
過版本**（09-03 v4 加 CEX 腿、v5 加累積深度），前後段不是同一把尺。所以這個窗口
**根本不能用來回答這題**；前瞻版才乾淨——從穩定的掃描器起算。

**公告領先市場出現的時間（同日量到，這是「事件捕捉」的核心數字）**：三家的公告端點
都是公開的（OKX `support/announcements`、Bitget `public/annoucements`、Binance cms），
而且抓到的正是我們偵測到的那批事件——

| 場館 | 標的 | 公告 | 市場出現 | **領先** |
|---|---|---|---|---|
| OKX | PYPL／DDOG／IONQ | 09-03 03:30Z | 09:05／09:29／09:31Z | **5.6–6.0 h** |
| Bitget | GPRO | 09-03 14:00Z | 14:24Z | 0.4 h |

公告不只早，還告訴你**哪個標的會出現在哪裡**——這是能預先把錄價／掛單準備好的
那個窗口。OKX 給六小時，Bitget 給二十幾分鐘。

**同日的實例（使用者轉來 yourQuantGuy 貼文）**：**Polymarket 09-04 上線永續**，
67 個市場（36 股票、24 加密、4 商品、3 指數；含三星、SK 海力士、美光、黃金、白銀、
WTI、布蘭特、S&P500、Nasdaq100）——**一次開出幾十個「新所有、別所也有」的配對**。
費率 taker 4 bps／maker 1.25 bps（30 天量階梯到 2 / 0.5 rebate），**掛單流動性獎勵
每天 $75k**；貼文作者說 BTC/ETH/GOLD/SP500 買一賣一貼著、各幾千美元。
Polymarket 掛單獎勵是「成本」那一軌的直接候選：它不是折扣，是**倒貼**。

- [x] 儀器：上市偵測修正（2026-09-04）
- [x] 回測切片：樣本不足＋儀器中途換版，不能判（2026-09-04）
- [ ] **前瞻時鐘**：從 2026-09-04 起每一筆真上市自動進本節四關；≥20 個事件各自
      有 >72h 追蹤時第一次讀（約 09-18）
- [ ] 公告監看：三家公告端點每 5 分鐘輪詢，公告→市場出現的領先時間逐筆記錄
- [x] **BWEnews（t.me/BWEnews）評估（2026-09-04，使用者：「我有一個新聞機器人消息超快」）**：
      它是交易所公告的**轉發器**（雙語標題＋來源 URL，時間戳 UTC），涵蓋 Binance／
      Upbit／Bithumb／Schwab 等。拿它對 Binance 期貨上市**逐筆對時**（Binance 的
      `onboardDate` 是合約開始交易的時刻）：

      | 事件 | 合約開始交易 | BWE 貼出 | BWE 相對開盤 |
      |---|---|---|---|
      | GPROUSDT | 09-03 13:45Z | 20:30Z | **晚 6.75h** |
      | 9/2 那批 TradFi（NVDL/TSLL/DDOG/…）| 10:00–10:30Z | 16:45Z | 晚 ~6h |
      | 8/28 那批（TEM/MRK/IONQ/MARA/PDD）| 09:00–09:20Z | 15:30Z | 晚 ~6h |

      **對「新所上了別所有的商品」這個用途，BWE 比直接輪詢交易所 API 慢六小時**——
      合約已經交易半天它才貼。它快的是**韓所現貨上幣**（Upbit／Bithumb KRW），那是
      「上幣拉盤」的交易，不是跨場館永續價差。沒查證的一點：Binance 自己的公告時刻
      抓不到（cms 端點回非 JSON），所以「BWE 相對 Binance 公告」的延遲未量。
      **結論**：事件源以交易所自己的公告端點（OKX 領先 6h、Bitget 24 分鐘）＋
      `exchangeInfo` 輪詢為主；BWE 當第三方對照，不當主要訊號。另查 Binance
      `PENDING_TRADING` 狀態：只有一筆陳舊殘留，**不能**當開盤前訊號。
- [ ] Polymarket 永續接進掃描器——**本機連不到**（2026-09-04 查證）：`polymarket.com`、
      `docs.polymarket.com`、`api.perpetuals.polymarket.com` 三個主機全部解析到同一個
      IP `182.173.0.181`，連線逾時——這是 **ISP 層的 DNS 黑洞**（台灣把它當博弈站擋），
      不是 Polymarket 自己的地區限制（那個只擋美國下單）。後果：(a) 掃描它需要一台
      在台灣以外的機器——**這是東京那台機器第一個具體的、非延遲的理由**；(b) 就算
      掃到了，下單前要先確認帳戶地區可下單。API 形狀（公開文件）：
      `https://api.perpetuals.polymarket.com/v1/`，instruments／ticker（index_price、
      mark_price、funding_rate、next_funding）／book（bids/asks、sequence），有 SDK
      `fetch_perps_book(instrument_id, depth)`。

### 1.04 套利·待辦清單（2026-09-04 使用者：「其他的列入待辦事項」）

排序依「回答一個能關掉整條線的問題」的成本，最便宜的在上面。**每一項都要先
預註冊再跑**；沒做的原因也寫下來，不要讓它們變成默默消失的想法。

- [ ] **一筆 $10 測試單讀回執**（最便宜、最決定性）：確認 Entropy 腿的帳單上
      是不是 3.4 bps（25% 折扣）。這是唯一能把費率從「頁面寫的」變成「帳上
      發生的」的方法。**不下這筆單，所有淨值算式都掛在一張截圖上。**
- [ ] **帶改用費率推導而非 p90**：現在的帶是分佈的 p90（依定義 10% 的分鐘在
      帶外），改成「帶 = 打平成本 × 2」。已量過的形狀：IO 掛單門檻 4.5 → 帶 9 bps
      時 SNDK 0.5 次/天、NBIS 4.6、ANTH 8.3，收斂 97-100%。**這不是門檻掃描**
      （帶由成本決定不由結果決定），但要當報告列加進判決程式，不動凍結判準。
- [ ] **掛單成交率**：所有「掛單模式為正」的算式都假設掛得到、成交得了。
      逆選擇的量在這個專案有前例（V7 的 maker 掛開火價死於逆選擇）。要小額
      實盤才量得到。
- [ ] **開盤兩小時 1 秒取樣錄一週**：§1.01 量到開盤時段分鐘內幅度是收盤的
      3 倍（9.7 vs 3.1 bps），代表比 p90 更寬的瞬間存在而我們看不見。改取樣層、
      資料量 60 倍。**先決定要不要，再動工。**
- [ ] **「HIP-3 vs 真實市價」那條腿**（那對兄弟做的那件事）：先用免費延遲報價
      當粗略錨只錄不做，回答「偏離有多大、多常、收不收斂」。看得到 50-300 bps
      才談 IBKR 帳戶與資金。§1.03 已證明「小所彼此」這條路的帶就是 10 bps 級。
- [ ] **兩腿保證金是不是分開帳戶**（HIP-3 dex 之間、HL 主場 vs dex）：決定
      斷腿風險與資金效率，升格前必查。
- [ ] **xyz/HL 的部署者分成返佣能不能談到**（**優先序 2026-09-04 調降**：量化後
      返佣 25%→50% 只多 +0.75 bps，而吃單→掛單差 4.5 bps，**執行方式的槓桿是返佣的
      6 倍**；且「全部 dex 25%」對現有家族**零影響**——另一腿 Lighter 本來就 0 費、
      IO 已經是 25%。要談就談 50%，而且只對 xyz 的商品配對有意義）：B 情境下商品配對掛單
      全部翻正。這是談判不是研究。
- [ ] **`GOLD` 資產別名要拆開**（§1.03 查到）：PAXG／XAUT／XAU 是三個不同發行
      商的產品，掃描器現在把它們併成一個 `GOLD` 互相配對，量到的是發行商基差。
      修法是別名表拆成 `GOLD_PAXG`／`GOLD_XAUT`／`GOLD_IDX`，或乾脆只認同代號。
- [ ] **掃描器的同場館配對**（§1.03 查到）：`GOLD@bitget-bitget` 這種兩腿在同
      一個場館的列不該存在，加一道 `leg_a != leg_b` 的守衛。

**~~不做的~~ → 改列必做（2026-09-04 當天改正）**：我先前寫「Binance 的 USDT 永續沒有
真股票」是**錯的**——我的篩選只認 `contractType == "PERPETUAL"`，而 Binance 的股票
永續是 **`TRADIFI_PERPETUAL`**（`GPROUSDT`／`DDOGUSDT`／`IONQUSDT`／`SAMSUNGEMUSDT`／
`KODEX200USDT`／`HK0625USDT`…，`underlyingType` 標 KR_EQUITY／HK_EQUITY／CN_EQUITY／
EQUITY）。近 21 天 Binance 上了 **34 個**這類合約。所以 CoinGecko 那個「Binance vs
Hyperliquid 三星 0.93%」的配對**可以重現**，而且 Binance 是本線缺的最大一個場館。
教訓同 mistake.md 2026-09-03 Bitget 單位那條：**「查了沒有」的查詢要先問它看不看
得到目標**——我用一個把它排除在外的篩子去找它。
- [x] **Binance 接進掃描器（2026-09-04，entropy-arb 本機倉）**：714 個市場，其中
      **188 個 TradFi 永續**（AAPL/AMZN/ASML/ANTHROPIC/SAMSUNGEM/KODEX200/HK0625…）。
      簿口單位以真實簿驗過：BTCUSDT 頂檔 **$1.02M**（最深的簿，合理）；新股票永續
      很薄——GPRO $113、SAMSUNGEM $230、KODEX200 $11。踩到一個小坑：`depth` 的 limit
      只收 {5,10,20,50,…}，我們的 25 會回錯誤 JSON、看起來像「沒簿」——修成取
      ≥25 的最小合法值再截斷。掃描器已重啟；§1.03 的 BIG 分級加入 binance。
      費率先用標準 5/2 bps、無返佣、標未查證。

### 1.03 套利·大所 vs 小所（2026-09-04 預註冊，使用者：「先做這個」）——外部報告的可檢驗版本

**觸發**：CoinGecko 2026 代幣化股票報告寫 Binance 的三星永續平均比 Hyperliquid
高 **0.93%**（SK 海力士 1.03%、最寬 2.3%），並解釋成「**後進場的交易所報價偏高，
因為流進去的套利資金少**」。我們家族量到的是 2–15 bps——而**我們兩條腿都是後
進場的小所**。如果那個機制成立，帶窄的原因是配對選錯了，不是方法錯了。

**查證（先於任何 band 計算）**：Binance 的 USDT 永續**沒有真股票**（`SPXUSDT`
是迷因幣、`MUSDT` 是迷因幣、`XAUTUSDT` 是代幣化黃金），所以報告的「Binance 腿」
是別的產品。**不需要它**：OKX（SAMSUNG／SKHYNIX／SKHY／KR200／NVDA／TSLA／
AAPL／SPY／QQQ／MSTR／COIN／XAG）與 Bitget（SAMSUNG／SKHYNIX／KR200／NVDA／
TSLA／AAPL／XAUT／PAXG／COPPER／NDX100）掛的是同一批標的，xyz 也有
（`xyz:SKHX`／`xyz:SKHY`／`xyz:KR200`）——**掃描器已經錄了 4 天，這個比較現在
就做得了，零新整合。**

**凍結（`research/arb/venue_size.py`，先 commit 再跑）**：場館分級**按它是什麼
分，不按它的數字分**——BIG＝okx／bitget，SMALL＝所有 HL builder dex＋HL 主場＋
Lighter 兩鏈。
- **P1**：股票／商品類，BIG-SMALL 的帶中位 ≥ **3×** SMALL-SMALL
  （報告的量級是 10×+；3× 是「足以改變我們配對選擇」的門檻，事前定）
- **P2 對照組**：BIG-BIG（okx↔bitget）必須是三者中**最窄**的——若不是，這個
  分級量到的是別的東西，P1 就沒有意義
- **P3**（方向性報告，不擋 P1）：同樣的順序在加密標的上是否成立

**排除（事前寫死）**：槓桿包裝（CSOP…2L 這類，2 倍產品不是同一個標的）、
報價數 <200 的配對；帶用**凍結的排名指標**（正向可成交空間 p90），不另發明數字。

**這一節只決定一件事**：要不要把錄製家族重新指向 BIG-SMALL 那一格。不是判決、
不開時鐘。

**結果（2026-09-04）：P1 失敗，比值 1.07×——「大所 vs 小所寬一個數量級」在我們
量得到的範圍內不成立。P2 對照組通過。**

| 股票/商品 | n 配對 | 帶中位 | 帶 p75 | 深度中位 |
|---|---|---|---|---|
| BIG-BIG（okx↔bitget）| 29 | **3.24** | 5.66 | $378 |
| BIG-SMALL | 140 | 11.60 | 14.75 | $328 |
| SMALL-SMALL | 71 | 10.81 | 14.27 | $367 |

**BIG-SMALL ÷ SMALL-SMALL ＝ 1.07×**（加密類 1.17×，P3 同向但一樣微弱）。

**P2 通過而且通過得很乾脆**：兩個大所之間只有 **3.24 bps**，是三格中最窄的
不到三分之一——**分級是有效的**，它確實量到了「場館成熟度」，只是那個效應
**發生在 BIG-BIG 那一格**（大所彼此已經被套利到很平），而不是「大所對小所
特別寬」。真正的分界線是「**有沒有小所參與**」，不是「大小差距」。

**兩個儀器缺陷，照 2026-08-02 規矩先查再解讀，v2 同一批資料重跑（v1 判準與
判決不動）**：(a) GOLD 配對把 **PAXG／XAUT／XAU** 混在一起——三種不同發行商的
代幣化黃金加一個黃金指數，PAXG vs XAUT 是**發行商基差**不是場館價差（錄價程式
作者自己的頭號警告，§1.00 記過）；(b) `GOLD@bitget-bitget`、`GOLD@lighter-lighter`
**兩條腿在同一個場館**，根本不是跨場館套利，而它們就坐在 P2 依賴的 BIG-BIG 格裡。
v2 剔除同場館與跨代號配對後：比值 **1.08×**（股票）／1.17×（加密），BIG-BIG
3.17 bps 仍最窄——**修完儀器結論一字不變**。

**因此**：不因為那份外部報告改變配對選擇。報告的 0.93%／1.03% 是「**平均價差**」，
我們的是「**p90 可成交空間**」（已扣掉要穿越的買賣價差）——兩個不同的量，那個
數字不能拿來當我們的期望值。要驗它必須自己錄同一個口徑，而我們現在錄的
GOLD_LL／NVDA_LL（§1.02）正是這件事的乾淨版本。

**順帶記下的事實**（不是判準）：韓股三個場館都有——OKX `SAMSUNG`/`SKHYNIX`/
`SKHY`/`KR200`、Bitget 同名、xyz `xyz:SKHX`/`xyz:SKHY`/`xyz:KR200`。
`KR200@xyz-okx` 帶 23.25 bps 但**深度中位 $1**——這正是「帶寬不等於錢」的
教科書例子。Binance 的 USDT 永續**沒有真股票**（`SPXUSDT` 是迷因幣），所以
報告的 Binance 腿是別的產品，我們沒有它也不需要它。

- [x] 跑（2026-09-04）：P1 FAIL 1.07×、P2 PASS；v2 修儀器後 1.08×，結論不變

### 1.02 套利·零費對照配對：兩腿都在費率表為 0 的場館（2026-09-04 登記，使用者：「加進去然後其他能做的研究繼續做」）

**為什麼**：IO 返佣查證是 25% 不是 100% 之後，家族在吃單模式下全負。「返佣以外
的方法」裡最乾淨的一條是**挑兩腿費率表本身就是 0 的場館對**——Lighter 與
Lighter-RH 都是結構性 0 maker / 0 taker（費率表，不是促銷）。掃描裡有 42 個同一
標的兩邊都掛的配對；帶寬前段：NVDA 8.0、GOOGL 7.1、GOLD(XAU) 5.1（頂檔 $557）、
SILVER 3.3（$816）。在這種配對上，**費率變數是 0、門檻是 0，能殺它的只剩兩樣：
收斂（USDC vs USDG 穩定幣基差偽裝成價差）與深度**。

**登記**：GOLD_LL（lighter XAU ↔ lighter-rh XAU；**刻意不用 PAXG**，底層資產要
完全一致）、NVDA_LL（NVDA ↔ NVDA）。各自 7 天時鐘、同一套四關＋收斂、
`premium_verdict` 逐配對報告不挑。它們是「零費對照」這個獨立問題，**不走
掃描排名的升格**（升格是在找肉，這是在問一個機制問題）。

**工程（entropy-arb 本機補丁，本機倉 commit）**：錄價程式腿 A 原本寫死是 HL；
加 `entropy.venue: lighter|lighter-rh`（預設 hl，既有設定逐位元不變、config_BTC
載入結果相同）、`entropy.symbol` 別名、資金費率輪詢在腿 A 為 Lighter 時改抓該鏈。
兩支各自 config／bat（CRLF 驗過）／log 目錄／開機啟動項；看門狗成員 8 → 10；
freshness 兩列；判決程式 PAIRS 與 VENUE_KEYS 登記。

**兩個先寫下的預期**：(a) Lighter 頂檔深度嚴重低估（§1.00 v5：BTC 頂檔 $25、
1 bps 內 $37k），所以「肥刻深度」欄在這兩支上會偏低，判決時要看累積深度；
(b) 若帶寬主要是 USDG 基差，收斂關會擋下來——BTC@lighter-lighter-rh 帶只有
0.8 bps，代表純基差很窄，XAU/NVDA 若寬得多就是別的東西。

- [ ] 錄滿 7 天（~09-11）出判決；期中不讀

### 1.01 套利·時段條件化——溢價是不是美股開盤的事件？（2026-09-04 預註冊，先 commit 再跑）

**觸發**：使用者問「無返佣要突破什麼」。按小時切 SNDK/NBIS 的 p90 帶，13–15 UTC
比其他時段寬 3–5 倍，ANTH（私募永續、沒有現貨開盤）沒有這個形狀——符合本線
登記時寫的機制（「盤後預言機制度不同」是股票永續溢價的來源）。**這是看過資料
才有的假設**，所以照 §0.94 的規矩：現有資料是選擇窗、只用來決定要不要開時鐘；
從本節登記時刻起錄的列才是驗證窗。

**凍結（寫死在 `research/arb/session_window.py`）**：窗口按機制定：OPEN
13:30–15:30 UTC（美股開盤前兩小時）／RTH／PRE／OFF，**不是表上最好的那個小時**。
四個預測（P1 OPEN>2×OFF、P2 ANTH 對照 <1.5×、P3 窗內偏離仍收斂 ≥70%、
P4 至少 4 天成立）全過才登記變體 W；缺一就死、不開時鐘。費率一律用**無返佣**
費率表（問題本來就是「沒有促銷活不活」）。經濟量：只做 OPEN 窗、一腿掛單、
無返佣，每筆淨 bps × 每天收斂次數 × 肥刻深度。

**結果（2026-09-04，`research/results/arb_session_window.json`）：凍結四關 → 陣亡，不開時鐘。**

| 關 | 結果 | 數字 |
|---|---|---|
| P1 SNDK 與 NBIS 的 OPEN 帶 > 2× OFF | ❌ | SNDK **2.17×**、NBIS **1.36×** |
| P2 ANTH 對照組 < 1.5× | ✅ | 0.94×（沒有現貨開盤就沒有這個形狀，機制對）|
| P3 窗內偏離收斂 ≥70% | ❌（**無資訊，不是失敗**，見下）| 0–1 次偏離 |
| P4 ≥4 天成立 | ❌ | SNDK 4/7、NBIS 2/4 |

**跑完發現兩個儀器缺陷，照 2026-08-02 的規矩先查儀器再解讀，另跑 v2（同一批
選擇資料，v1 判準與判決不動）**：(a) OPEN/OFF 把週六日混進去了——機制是美股
開盤，週末沒有開盤，那兩天 13:30 那格依定義就是 OFF，是**窗口定義錯**不是
發現；(b) P3 的帶取的是可成交空間的窗內 p90（6–20 bps），但凍結的 `convergence`
是在 premium_mean 對中線的偏離上偵測事件——尺度不同，判決帶（2–15）有幾百
個事件、窗內帶只有 0–1 個，所以 P3 是**沒量到**。v2 改成只算平日、P3 改問
「判決自己的偏離事件裡，起始在 OPEN 的那些收不收斂」：

| | 平日 OPEN/OFF | 逐日 >2× | OPEN 起始的偏離 | 收斂 |
|---|---|---|---|---|
| SNDK | **2.19×** | **4/5** | 23/194（12%）| **23/23** |
| NBIS | 1.37× | 2/4 | 22/95（23%）| 22/22 |
| ANTH | 0.99× | 0/4 | 0 | — |

**讀法**：開盤效應**只在 SNDK 上成立**，NBIS 沒有——它的寬帶在 PRE（09–12 UTC，
含一個 62.8 bps 的 10 UTC 格，尚未查是不是單一天）和一個買側持續偏移（OFF 買側
p90 14.9，跟 OPEN 差不多）。兩檔股票永續只有一檔有這個形狀，登記的判準是
「兩檔都要」，所以**修完儀器還是陣亡**，這不是差一點。而且就算只看 SNDK：
OPEN 起始的偏離只佔全部的 12%（每天 ~5 次），肥刻深度中位 **$100**——
只做窗口 × 掛單 × 無返佣算出來每天 **$0.0x**。**深度把它殺掉了，跟時段無關。**

**沒查的（照十道檢查明寫）**：~~NBIS 10 UTC 的 62.8 是不是一天撐出來的~~
→ **查了，是一天**（09-03 一天 102 分鐘 ≥20 bps、其餘三天 09–13 UTC p90 只有
−1～+3）——單一事件，不是時段結構，NBIS 的「PRE 窗很寬」到此作廢；
分鐘內幅度在 OPEN 是 OFF 的 3 倍（9.7 vs 3.1 bps）——代表 1 秒取樣會看到
更寬的可成交空間，但這批資料量不到；夏令時間沒處理（全樣本在 EDT 內）。

**這條研究方向下一步在哪**：機制在 SNDK 上活著、在 IO↔Lighter 這個場館對上
沒有肉。開盤效應＋深度同時存在的地方是 **xyz/CEX 的股票與商品配對**
（SPY@lighter-okx $500k、SILVER@xyz-okx $224k）——09-06 `scan_rank` 出名單、
升格、錄 7 天，**那時再用本節凍結的四關（含 v2 的兩個儀器修正）跑一次**，
不要現在就拿快照猜。

- [x] 跑（2026-09-04）：v1 陣亡；v2 儀器修正後 SNDK 有形狀、NBIS 沒有、深度 $100 → 仍不開時鐘
- [ ] CEX/xyz 股票商品配對升格錄滿 7 天後，用本節四關重跑（窗口定義：平日 13:30–15:30 UTC）

---

### 0.91 站內資金費收租（2026-09-02 登記，產品端請求；**判決 09-30**）

> 產品端請求書 `../jarvis/產品端請求_站內資金費收租_20260902.md`；
> 研究端回覆 `../jarvis/研究端回覆_站內資金費收租登記_20260902.md`。
> 產品端建議 §0.80，**該號已被 08-30「每根 Strong 都進」佔用**，改 §0.91。

**問題**：Bitget 站內「現貨多＋永續空」收資金費，BTC/ETH 的年化分佈長什麼
樣——中位多少、翻負佔比多少。原本只有 08-20 一次抽樣（BTC +10.9%）。

**凍結判準（一字未改產品端提案）**：窗口 09-02 → **09-30**；BTC 與 ETH
**各自**滿足「結算年化中位 ≥8%」∧「翻負 <25%」，且**前後兩半各自成立**。
任一不過 → 關線，產品端不寫現貨 adapter。8% 的推導（對投入資金約 6.7%、
扣一輪 6–12bp 與翻負期）站得住；**在登記當下把門檻挪到感覺比較好的數字，
正是預註冊要擋的事**。

**⚠ 先驗（判準凍結後才拉的 180 天回填，每幣 270 次結算）**：

| | 中位年化 | 翻負 | p10/p90 |
|---|---|---|---|
| BTC | **+4.05%** | 21.5% | −3.9 / **+10.9** |
| ETH | **+3.67%** | 21.5% | −2.4 / +10.9 |

**中位只有門檻的一半，而且 08-20 那次促成提案的 +10.9% 正好是分佈的
p90**——動機來自一次尾部抽樣。**不改判準、不提前關線**（回填是背景不是
判決），但期望值已經講清楚：**這條線目前看起來會 FAIL**。

**已落地（與登記同 commit —— §0.44 的教訓：只寫在文件裡的預註冊等於沒有）**：
`basis_recorder.py`（10 分鐘一輪，BTC/ETH 判決＋8 個山寨背景；每列含
fundInterval **不寫死 8**、mark/index → 溢價 bp）、`basis_verdict.py`
（期限前拒判；結算次數以 (symbol, funding 週期) 去重，用取樣次數當分母
會讓安靜期灌爆分佈）、班車、看板 0.91、freshness（1 小時上限）。
**不擴充 `funding_collector`**：`funding_rates` 沒有 mark/index，而溢價
是這條線的另一半肉。

**兩個產品端問題的裁示**：
1. **對沖 vs 獨立**：兩者都是，**分開判、同一批資料**。獨立照 8%；對沖的
   門檻低得多（任何正 carry 都抵銷網格庫存成本）但**規模被網格庫存綁死**。
   對沖列為附錄讀數，**永遠不能拿來救一個 FAIL 的獨立判決**。
2. **hedge 觸發單四條規格**採為底線，但**判決前不實作**。

**同日自踩一坑並修**：09-01 把錄價輪替改成時間戳之後，`prereg_publish`
的 `_n_arb` 只認字面 `.old`，看板數字從 ~6,000 掉到 **914**——
**mistake.md 08-29 一字不差的重演，而且發生在修好輪替的同一個 session**。
已改 glob 所有 `*.old`（並補上 HYPE）。教訓再寫一次：**輪替一份檔案時，
枚舉每一個讀它的人；計數類下游的錯法不是壞掉，是從零重數。**

---

### 0.87 套利看板的三個缺口（2026-09-01，使用者：「套利的數據網站也沒更新」）

網站的進度條其實有在動（14:06 更新），但查下去有三個真缺口，前兩個是
§0.86 的同族——**東西在本機是對的，只是從來沒到達外面**：

1. **判決檔沒有任何排程**。`premium_verdict.py` 只有人跑才更新，
   `arb_premium_verdict.json` 因此**停在 08-30**（兩天前），而它正是
   09-04 判決要用的那個檔案。已掛進每小時班車（bat 末行已驗）。
2. **看板只數 SNDK**。08-30 登記的家族（NBIS/ANTH/BTC/ZEC/NEAR）在
   `_n_arb` 裡不存在 → 從網站看，那五個錄價器等於沒發生。已改：閘門
   仍以 SNDK 為準（它起算最早、09-04 判決），家族數與 BTC 對照組寫進
   note，看板不再低報這條線。
3. **「可捕獲美元」指標把結構性偏移算成錢——對照組抓到的**。
   BTC（兩邊深簿零費率、帶應≈0）報出全家族最高的 **$97.8/天**。資料
   是真的：HL 的 BTC 持續比 Robinhood 鏈貴 3-5 bps、**100% 的分鐘為正**。
   但它的收斂欄是 `episodes: 0`——溢價從不偏離自己的中線，**因為它就是
   中線**：結構性偏移，不是會回來的價差。鎖進去永遠拿不到錢。
   **判決閘門是對的**（收斂關過不了就不過閘）；**錯的是我加的報告欄**，
   band × fires × depth 沒有考慮收斂。已改成以收斂為前提，不收斂記 0
   並印出理由。修正後同時把 NBIS buy 與 NEAR sell 兩個假數字也歸零。

**這是對照組第一次真正發揮作用，而它抓到的是「我自己的儀器」**——
與 mistake.md 2026-07-29「新診斷先在答案已知的資料上跑」同一條規矩，
只是這次那個已知答案是「BTC 不該有肉」。若當初沒有把 BTC 放進家族，
NBIS/NEAR 的假數字會一路帶到判決日。

### 0.88 套利第二條收益軸：資金費率 carry（2026-09-01 開錄，使用者定調「不嚕空投、要靠套利獲利」）

**觸發**：使用者轉來 MOB (mob.exchange) 的空投教學。讀完後判斷：教學本身
是**刷積分**（MOB＋Ondo 對鎖賺三家積分，收益＝未來空投，EV 不可算），
與本線無關；但平台的 Delta-Neutral 策略卡自己寫著 **「The funding spread
is the return」**，兩腿是 **Hyperliquid ↔ Lighter RH**——**正是我們已在錄
的那兩條腿**。使用者定調不做空投、要靠套利獲利，於是只取這一條情報。

**兩種收益要分開記**：價差要**收斂**才付錢（現有四關管的就是它）；
資金費率是**持有**就付錢。前者已錄兩天到四天，後者連資料都沒有。

**已上線**：
- `entropy_arb/funding.py`（本機補丁）：每 5 分鐘拉兩邊費率。
  **單位是這裡唯一不能錯的東西**——HL 是**每小時**（142/233 幣停在
  1.25e-5 基準）、Lighter 是**每 8 小時**（錨定：同端點 `exchange:
  "binance"` 的列群聚在 1e-4 ＝幣安 0.01%/8h）。全部正規化成 **bps/8h**，
  `diff = hedge − entropy`（正＝多 entropy／空 hedge 收錢）。
- 錄價每分鐘多寫四欄（entropy/hedge/diff/age）；poller 掛掉留空白，
  **永不為 funding 掉一分鐘**。
- **HYPE 加入家族（第七條）**：09-01 截面費率差最大（+10.95%/年）。
- `premium_verdict` 加 `funding_stats`（中位／p10-p90／同號比例／年化）
  ——**報告用不進判準**。08-28 凍結的四關是關於價差的，不因為出現第二種
  收益就事後擴充（那正是預註冊要擋的事）。

**修掉一個會吃資料的 bug**：schema 輪替原本是 `os.replace(path,
path+".old")`——**第二次輪替會覆蓋第一次**，08-28 那 257 分鐘會被靜靜
吃掉。已改時間戳輪替，判決端改 glob 所有 `*.old`。驗證：輪替後分鐘數
連續（SNDK 6031、其餘 2909，與輪替前對得上）。這是 mistake.md 08-29
「輪替要枚舉每個消費者」的延伸——**這次的消費者是未來的自己**。

**截面 vs 序列（為什麼要錄）**：09-01 兩次量測相隔一小時就不同
（HYPE 的 Lighter 側 0 → 0.32、BTC 5.6e-5 → 4.8e-5）。**截面不能下注**，
一週後才知道那個差是穩定 carry 還是每天翻。判決仍照 09-04 / 09-06 的
價差四關；carry 是獨立的第二張表，屆時並陳。
