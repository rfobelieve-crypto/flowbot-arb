# 套利線 TODO（flowbot-arb）

2026-09-04 從 `flow_system/TODO.md` 整段搬過來——內容一字未改，只是換了家。
節號沿用原本的編號（§0.75 是這條線的起點，§1.xx 是後續戰場），因為所有
commit、判決檔與文件都在引用它們。

**讀法**：§0.75 在最下面（它最早），§1.xx 由新到舊。開工前先讀
`README.md` 與 `docs/NEXT_SESSION.md`。


---

## HMM 各節（2026-09-15 從 `flow_system/TODO.md` 搬過來）

使用者：「把HMM相關的東西都移過去」。**內容一字未改**，節號沿用原本的編號。
原檔裡它們分成兩塊（中間夾著量化線的 §1.34），這裡照原本的先後接在一起：
先是 §1.41d / §1.42 / §1.41c，再是 §1.41b / §1.41 / §1.40。
文中的 `research/ops/hmm_watch.py` 等路徑是當時的位置，**現在在 `ops/`**
（`ops/hmm_watch.py`、`ops/live_hmm.py`、`ops/alert.py`，排程 `Arb_HmmWatch`）。

## §1.43 【預註冊·未跑】倉位型做市（Lighter 單場館、庫存上限、不逐筆對沖）的 Gate 0（2026-09-15）

**使用者 2026-09-15**：對「兩側同時掛、持有庫存、超過上限才對沖」回答「**可以試試看這個方向**」。
**授權範圍**：量測＋這一節本身。**不含**引擎模式、設定檔、live —— HMM.md：「換構造另開一次
override」「翻 live 要使用者再說一次」。名字沿用 HMM.md 表裡既有的「倉位型做市」，不另造詞。

### 為什麼值得量（全部是已有讀數，不是新推論）

| 事實 | 來源 |
|---|---|
| MON 掛單腿每筆（60 秒）：半價差 +7.67、逆選擇 −4.01、**淨 +3.67 bps** | 09-15 `tools/fill_decomp.py`，n=31 |
| 把它翻負的是對沖腿：HL 吃單 4.50 ＋ 穿價差 0.65 = **−5.15** | 同上 |
| 兩側同時過門檻的掃描只有 **4%**，一次只掛一張 —— 門檻裡含對沖費 | 09-15 引擎 `sides 0/1/2` |
| 全宇宙 132 市場 **0 個**過六關（HMM 構造下） | `results/hmm_flow_screen_20260915.json` |
| Quant Arb：「量少的市場要持有庫存才賺得到溢價；急著做掉會付太多」；「在小所圍著大所中價報價＝全掛單套利，最低成本（沒有對沖腿成本）」 | `2024-08-26 market-making-for-dummies`（Holding Risk）、`2025-08-02 advanced-market-making` |
| **反面**：flow_system §1.29 在 **HL** 上量到掛單方每筆 1 分 −0.66、5 分 −1.79、30 分 −0.99（前 30 名），CI 不含零 | §1.29（2026-09-15 已複製進本檔，見「flow_system 裡與這條線有關的其他節」；方法照搬到這裡） |

**要回答的只有一件事**：不對沖就要持有庫存幾分鐘到幾小時，而 **Lighter 上 5 分、30 分的
markout 從沒量過**（目前只到 120 秒，5–120 秒是平的）。§1.29 說 HL 在那個尺度是負的。

### 要量的（寫在跑之前）

- **M-A 市場層上界**（§1.29 方法搬到 Lighter）：成交帶 `D:/flowbot_data/lighter/trades` × 頂檔
  `lighter/tob`，對每一筆成交算掛單方單筆損益 = 半價差 + markout(h)，**h = 1 / 5 / 30 / 60 分**，逐市場報
  平均、95% CI、中位、截尾均值。
- **M-B 我們自己拿到的那一片**：MON 全部 live 成交（maker.csv＋.old），同一個分解在 h = 60 秒 / 5 分 / 30 分。
  n 小，先印 MDE；它只用來判斷 M-A 的上界打幾折，不單獨下判決。
- **M-C 庫存風險**：h\* = 以 live 單側成交率算「一張 $15 被反向成交抵掉」的中位時間（MON 09-15 分界後
  1.23 筆/時，兩側合計）；在 h\* 上量 sd_clip（同一段時間中價在 h\* 內的變動標準差 × $15）。
  E_day = n_day × net(h\*)、SD_day = √n_day × sd_clip（**假設各筆獨立 —— 偏樂觀，寫明**）。

**市場清單用規則產生，不手挑**：MON（控制組）＋ `hmm_flow_screen_20260915.json` 裡
**G4 ∧ G5 通過 ∧ Lighter 半價差 ≥ 5 bps** 的全部市場。

### 儀器守衛（任何一關紅 = 該市場該列不可引用）

| | 做什麼 |
|---|---|
| **D1** | `side` 語意用資料證明：吃單買的成交價平均 ≥ 當下 mid，吃單賣的 ≤ |
| **D2** | 由成交帶算的半價差中位，與 tob 記錄的半價差中位差 ≤ 1 tick |
| **D3** | 安慰劑：打亂 side，markout 平均的 |值| < 2 SE |
| **D4** | 已知答案：M-B 在 h=60 秒、用 09-15 那 31 筆，重現 +7.67 / −4.01 / +3.67（各 ±0.5）—— 必須走 `fill_decomp` 同一份載入與對齊，不是第二份實作 |
| **D5** | **長時間窗不可以用「頂檔年齡 ≤ 30 秒」丟樣本**（Lighter 頂檔只在變動時更新，安靜的持有期會被系統性丟掉，偏向「有在動」）。改用錄製器存活（tob 檔案連續、`lighter_mid` 取樣間隔）判定；每一格印丟掉的比例，>20% 標「未量」 |
| **D6** | 成交帶時間範圍必須蓋住 [t, t+h]；蓋不住的列丟掉並計數，不可以算成 0 |

### 判準（凍結，事後不放寬）

- **PASS**（值得設計引擎模式＋審查＋另開 override 的提案，**不是**上線）：至少一個市場同時滿足
  (a) M-A 在 h=5 分 **且** h=30 分的 CI 下緣 > **+0.40 bps**（Lighter 掛單費），
  (b) M-C 的 **E_day / SD_day ≥ 0.5**，
  (c) D1–D6 全過，
  (d) M-B 在 h=5 分的平均不低於 M-A 同格 − 5 bps（我們那一片不可以比上界差到把 (a) 吃光；n 小，只擋極端）。
- **FAIL**（這條路關掉，記錄理由）：所有市場的 M-A 在 h=5 分 **或** h=30 分 CI 上緣 < +0.40。
- **其他 = 樣本不夠**：資料再累積一週，**09-22 用同一支工具、同一組判準重跑**，不得放寬。

### 誠實的限制（先寫）

1. **M-A 是上界**：所有成交的平均 = 假設拿到隨機的一片；逆選擇的定義就是壞的先成交到你。
2. **單向流不在 M-C 裡**：XPL 就是被持續單向的流量頂在庫存上限死的（報價側別 332:0）。
   M-C 假設兩側都會來；G5（少數側 ≥35%）是前提，但不保證短時間內不偏。
3. **資金費、Lighter 保證金、基差跨天漂移**（AERO 翻號）沒有進 E_day。
4. **資料窗** ≈ 1 天成交帶，regime 覆蓋不足 —— 所以有 09-22 重跑。

### 不做的

不寫引擎模式、不改任何設定、不動 MON、不開 live。工具寫在 `arblib/`（或 `engine/tools/`），
**重用 `markout_curve.load_tob/asof`**，D5 需要的話在那裡加參數而不是複製一份。

### 已知未修的儀器缺陷（2026-09-15 系統稽核，§1.43 不使用它們）

| 缺陷 | 位置 | 狀態 |
|---|---|---|
| 同側成交筆數用「最後 30 個檔案」而不是時間窗 —— 與 `why_missed` 的絕對筆數對不上 | `engine/tools/missed_fills.py:77` `fs[-30:]` | **已證**仍在；兩支的絕對筆數不可引用（commit 4b487de） |
| `behind_at_fill_bps` 讀到 −26/−26/−35：成交當下我們那一檔可能已被簿口頻道移除，量到的是**下一檔**的距離 | `engine.py` `_consume_maker_fill` | **推論**（兩條 WS 沒有時間戳對照）；在對照之前這一欄不可解讀 |
| **帳戶串流沒有心跳年齡警報**：Lighter `AccountOrdersFeed` 只在例外時清 `ready`；「連著但沒訊框」看起來是健康的。V7（flow_system `indicator/okx/ws_private.py`）每週期檢查私有 WS 心跳年齡（>30 秒 HALT、>300 秒降級） | `engine/entropy_arb/venue_lighter.py` | 未修。**等 MON 帶 B3b 修正跑過第一天再動**（不同時上兩個沒實盤過的改動） |
| **斷線重連後不補漏掉的訂單事件**：重訂閱只拿到仍開著的單，斷線期間走到終態的單在 `_latest` 裡永遠停在舊狀態 —— 09-15 00:10 撤單確認死鎖 72 分鐘就是它，現在靠 `halt_recover` 重啟收拾。V7 也沒做（它靠每週期 REST 對帳部位） | 同上 | 未修，同上；候選做法：重訂閱後對仍在追的 coi 查 REST inactive orders |
| `flatten_residual.py` 沒起帳戶串流 -> 送單只回 `sent-unconfirmed` | `engine/tools/flatten_residual.py` | **已修**（先起串流、沒 ready 不送；引擎在跑且沒 HALT 則拒絕）＋`tests/test_flatten_residual.py` |
| HMM 產生器的 `.bat` 模板沒有 STOP 閘門 | `arblib/make_hmm_config.py` | **已修**（模板＋`test_watchdog_registry.py`）；`run_hmm_MON.bat` 本身還在跑，未修，見測試裡的 `KNOWN_UNGATED` |

---

## §1.41e 【根因已找到，修法進行中】MON 的 HL 429 全部來自 flow_system 的錄製器 —— 同一個 IP（2026-09-15）

**這一節是從 flow_system 那邊交接過來的**（使用者：「接要接到 arb 上記得喔」）。
錄製器屬於第 5 線（鏈上量化），所以修法在 flow_system 做；**要知道它、要驗它的是這裡**。

### 量到的

    MON runner.log 的 HL 側 429（position fetch / HEDGE MAKER 撤單）   274 行
    落在每小時 05–33 分之間                                           274 行
    如果跟時段無關，預期佔比                                           48%

05–33 分正好是 flow_system 排程 `FlowBot_HLRecord` 的執行窗：每小時 :05 啟動、
約 28 分鐘、約 400 req/分（`research/hl/hl_fuel_recorder.py --max-addr 2500`）。
HL 的限流按 IP 算，而它跟引擎的 HL 腿從同一台機器出去。

**15:13:58 那次撤單 429 就在 15:05–15:33 那一輪裡**：
撤單拿到 429 -> 撤單未確認 -> 改用吃單補對沖 -> 那張掛單 21 分鐘後成交 ->
過度對沖 -> HALT -> 自救收乾淨。

### 修法（flow_system 側，2026-09-15）

錄製器搬上 Railway（服務 `hl-record`，flow_system commit 5dff05c 起），本機排程改成
每小時 :45 只拉檔回來。**預定 19:05（11:00 UTC）起本機不再跑錄製器。**
換完之後本機打 HL REST 的只剩 `hl_verify.py`（每小時 1 次）。

本機仍然連著 HL 的：`hl_tape.py`、`hl_mid.py` 兩支 **WS**（不是 REST 權重）。
上面那 274 行沒有一行落在 05–33 分之外，所以它們目前不是 429 的來源；
但 WS 的訂閱數也是按 IP 算的上限，**沒有量過**它們吃掉多少。

### 要在這裡驗的（可證偽，寫在跑之前）

MON 重啟、而且 19:05 切換完成之後：
* **HL 側 429 在 05–33 分的比例應該掉到跟其他時段一樣**（理想是兩邊都接近 0）。
* 如果 05–33 分仍然明顯偏多 -> 還有別的東西在那個時段打 HL，這一節的根因是錯的，要重查。
* 如果 429 整體沒有明顯減少 -> 限流的主因不是錄製器，而是引擎自己的請求節奏。

### 仍然要修的（跟這個根因無關）

**撤單遇到 429 時會變成「撤單未確認」，而那張單之後還會成交。** 錄製器搬走只是
讓 429 變少，不是讓它不可能發生 —— 別的程式、別的 IP 共用者、HL 自己的節流都可能
再給一次 429。引擎側的處理（撤單被限流時的重試與對帳）仍然是 MON 重啟前要決定的事，
而且它直接影響 §1.41d 的「撤單未確認次數」那個判準。

**引擎側已寫好、未上線（2026-09-15 晚，另一個 session）**：審查 `docs/B3b_HEDGE_ORPHAN_AUDIT.md`、
測試 `engine/tests/test_hedge_orphan.py`（266 passed，7 個破壞注入全紅），**真實 HL 上沒跑過**。
(1) 撤單被拒（含 429）在 `cancel_timeout_sec` 內重送；(2) 仍未確認的對沖單不再丟掉，
登記後由對帳迴圈撤到交易所給終態，期間它的成交歸因給我們（不再判成「不明變動」HALT）、
暫停新報價；(3) `hedge_maker_poll_sec`（MON 1.0）—— 掛單對沖原本沿用 0.05 秒打 HL REST
`orderStatus`（權重 2），理論上限 2400/分。**實際吃掉多少沒量**，而上面 274/274 說主因是錄製器，
所以這一條是減負，不是根因。

**⚠ 上面「要驗的」第三條會被混淆**：若錄製器切換與 (3) 同時上線，「429 整體沒有明顯減少」
與「減少了」都分不清是誰的功勞。要乾淨地驗根因，MON 重啟時要嘛先不帶 (3)
（刪掉 `hedge_maker_poll_sec` 那一行 = 舊行為）跑過至少兩個 :05~:33 時段，要嘛接受這一條只能驗
「05–33 分的偏倚消失」（第一條），不能驗第三條。**要選哪個是使用者的決定。**

**切換完成時間（flow_system session 通知）：2026-09-15 18:00 本地 / 10:00 UTC**，比預定早。
本機 18:05 起不再跑 `hl_fuel_recorder`；`FlowBot_HLRecord` 改成每小時 :45 從 Railway 拉檔＋
`hl_verify.py`（每小時 1 次 HL info）。**分界 = 10:00 UTC**：之前與之後的 429 分布分開算。
**使用者 2026-09-15 選擇「先拿掉」**：`config_MON.yaml` 不帶 `hedge_maker_poll_sec`（= 舊的 0.05 秒），
所以 MON 重啟後第一段的 429 分布只反映錄製器搬家，第三條驗證不被混淆。
本機仍連 HL/Lighter 的（WS，非 REST）：flow_system `hl_tape`、`hl_mid`、`lighter_tape`、`lighter_mid`、
`exit_paths/lighter_recorder`；arb `record_universe.py` ＋ 9 支 record-only 引擎。

**MON 現況（17:40）**：已用控制通道 `flat` 平倉（HL 0；Lighter 殘 −0.8 顆 ≈ $0.018，
引擎量子是 HL 的 1 顆），放了 `engine/logs/stop/run_hmm_MON.stop`。使用者：「先不要重啟等修好了再說」。

---

## §1.41d 【決策關卡，2026-09-16 結算】掛單對沖留還是關 —— **判準是兩個比例，不是模型**

**使用者 2026-09-15 訂立**：「接下來要看接下來一天 `[HEDGE MAKER]` 的
捕獲率與撤單未確認次數，再決定留或關。」

**凍結在跑之前，事後不放寬。** 要看的就是 `arb/engine/logs/MON/runner.log`
裡 `[HEDGE MAKER]` 那幾行，兩個數字：

| | 怎麼數 | 現況 |
|---|---|---|
| **捕獲率** | `filled >= qty` 的行數 ÷ 全部 `[HEDGE MAKER]` 行數 | **0/1** |
| **撤單未確認** | 帶「**未確認**」的行數（以及 `log.error` 的那條） | 0/1（舊版沒記） |

**算術（每筆，$15 名目）**：

    一次捕獲成功  省 3.65 bps ≈ **+$0.0055**（HL 吃單 4.50 + 穿價差 0.65 -> 掛單 1.50）
    一次撤單失效  過度對沖再買回 ≈ **−$0.010** ＋ HALT 停機（實測 4 分鐘）
    => **要三次成功才抵一次失效**

**判準**：
- 捕獲率 < 20% **或** 撤單未確認 > 1/10 -> **關掉**（`hedge_maker_timeout_sec: 0.0`），
  回到已證明的吃單對沖。關掉不是失敗,是「這個場館的佇列不給我們這個位置」。
- 捕獲率 ≥ 40% **且** 撤單未確認 ≤ 1/20 -> 留著，並重算每筆淨值。
- 中間 -> 樣本不夠，再等一天，**不得因為看起來快到了就放寬**。

**背景（為什麼這一關存在）**：09-15 13:25 第一筆真實成交,掛單對沖捕獲 **0**,
而那張「撤掉」的 post-only 90 秒後成交 -> 過度對沖 -> HALT -> 自救買回。
兩個洞當天修掉（撤單改成**有預算的確認**、對沖進行中禁止淨額對沖插手,
commit `57be72f`），但**捕獲率本身沒有被修法改變** —— 佇列模型說 30 秒是
59%，那是**上界**（假設我們排在當下佇列最後、價格不跑掉、前面的單不補）。
實盤 0/1。模型不算數，log 才算數。

**現行參數**：`hedge_maker_timeout_sec: 30.0`、`net_grace_sec: 45.0`（兩個
綁在一起，只改前者會讓每筆成交在 20 秒 HALT）。

**2026-09-15 晚更新（讀數＋語意分界，判準一字未改）**：
- 13:37 修正版之後到 15:34 HALT：`[HEDGE MAKER]` **捕獲 1/2、未確認 1/2**。
  那 1 次未確認的原因是 **HL 撤單回 429**（IP 額度被吃滿），不是佇列 ——
  而那張沒撤掉的單 21 分鐘後成交，引擎判成「部位不明變動」HALT。
- 修法：`hedge_maker_poll_sec`（MON 1.0，原本沿用 0.05 秒打 HL REST）＋
  撤單被拒在預算內重送＋未確認的對沖單持續追蹤到終態。審查：`docs/B3b_HEDGE_ORPHAN_AUDIT.md`。
- **語意分界 = 這個改動上線（MON 重啟）那一刻**。之前與之後的「撤單未確認」
  **不可以加總**。MON 從 15:34 停機，使用者：「先不要重啟等修好了再說」。
- 429 的另一半來源在 flow_system（`FlowBot_HLRecord` 每小時 :05~:33），本 repo 不動。

---

## §1.42 Entropy（io）返佣 —— **一條備著的路,現在對 HMM 沒用,但要記住為什麼**

2026-09-14 使用者提供線索並截圖確認帳戶狀態。**不是新交易所**：
Entropy 的 ticker 就是 `io`,是我們系統裡早就有引擎的那個 HIP-3 deployer
（`OPENAI = io:OAI`、§0.75 錄製家族的 entropy 腿全在 io）。

### 已查證的事實（官方文件 ＋ 帳戶截圖,不是推文）

* 帳戶 `rfo`,**TIER 1**,referral「yourquantguy」**Active**（Sep 4, 2026 綁定）。
* **API 下單算數** —— 官方原文：「Eligible Entropy-market trades are tracked
  for this referral automatically, **including trades placed outside the
  Entropy frontend**」。這一條很重要,它讓返佣對自動化策略可用。
* **百分比套在 Entropy 的份額上,不是總費用。** 官方範例：$100 總費用 ->
  約 $50 給 Hyperliquid、$50 給 Entropy,而百分比只作用在後面那 $50。
  **推文說「tier 2 返 110% 等於倒賺」是漏了這一層**（120% x 50% = 總費用 60%）。
* **只適用 Entropy 自己部署的市場**（`io:`）。我們現在跑的 AERO / MET /
  FIL / GMX 全在 HL core,**一毛都拿不到**。

### 返佣後的損益平衡（HMM 結構：Lighter 掛單 x HL 吃單對沖）

    等級                      實付吃單費   損益平衡
    現況 core（無返佣）           4.500      7.40
    **Tier 1 ＋ 碼（現在就是）**   2.588      **5.49**
    Tier 2 OG ＋ 碼               0.675      3.58
    Tier 3（$10M/30日）＋碼      -0.788      2.11
    Tier 4（$50M/30日）＋碼      -2.250      0.65

### 為什麼現在對 HMM 沒用

`io:` 只部署了 **10 個市場**,跟 Lighter 有交集且有成交的只有 **4 個**,
而它們的半價差全部低於 5.49：

    市場    半價差   淨bps    切片$    吃單少數%
    EWY     3.80    -1.69    76.30     40.8      （而且 2026-08-30 下市過一次）
    NBIS    0.94    -4.55   143.52     41.7
    DRAM    0.81    -4.68   563.68     50.0
    SNDK    0.55    -4.94    73.82     50.0

**五關都過的：0 個。** 升到 Tier 2（門檻 3.58）也只有 EWY 勉強。

**但這四個的切片與吃單流是全場最好的那一類**（$74-564、少數側 41-50%）——
正是 MET/CHIP 死掉的那兩項。所以要記住的不是「io 沒用」,是：
**它們價差窄正因為它們是好市場**,而我們一直把「窄」讀成「沒機會」。

### 什麼情況下這條路會變得重要

1. **Tier 2 是「No qualification threshold」** —— 它不靠成交量升,靠
   **特定邀請碼授予**（官方：tier grants per code）。Tier 1 是 85%、
   Tier 2 是 170%,**同樣的成交量差一倍**。這是一個不花錢的詢問,
   而**要不要去問是使用者的事**,我不代為接觸。
2. **Tier 3/4 之後實付費用是負的**,那時賺的不是價差是返佣本身 ——
   對切片、單向流、對沖延遲**全部免疫**,因為不靠價差。
   但 Tier 3 要 **$10M / 30 天**;以現在 $15 的單量算是 33 萬筆成交,
   不可能。要走這條路必須先有數量級不同的資金與單量,那是另一次 override。
3. **Entropy 增加部署市場時要重看。** 現在 10 個,只要出現一個半價差 > 5.49
   且切片 >= $10、吃單流雙向的,它就立刻是最好的候選 —— 因為返佣讓它的
   門檻比 core 低 1.9 bps。**這一條值得每月掃一次**（`hmm_universe.py`
   加一個 `--dex io` 模式就好）。

### 現在要做的：什麼都不做

不入金 io 池、不改任何設定。**登記在這裡是為了「我們還有這條路」**,
而不是「這條路現在能走」。

---

## §1.41c HMM 的操作面：Telegram 控制面板 ＋ 看護排程（2026-09-14 登記，未做）

使用者：「是不是如果拿 telegram bot 來當控制面版比較好」。**是，而且成本
比看起來低很多 —— 那個東西已經寫好了。**

### 一、Telegram 控制面板：只差兩個環境變數

`arb/engine/entropy_arb/control.py` 的 B5 通道**原本就是為 Telegram 寫的**，
四個動詞已經實作並審過：

    /pause    停止開新倉，對沖與對帳照跑
    /resume
    /flat     全平並保持暫停（**會先跟交易所核對真實部位再動手** ——
              那是 2026-06-07 `/okx-admin/heal` 把 DB 歸零卻沒平 OKX、
              造成孤兒倉之後寫下的規矩）
    /status

它**刻意不能**做三件事：下單、改門檻、解除 HALT。原文：
「A message from a phone must not be able to undo a kill switch.」

現在走的是命令檔（`echo flat > logs/<pair>/control.cmd`），因為
`ARB_TG_BOT_TOKEN` / `ARB_TG_CHAT_ID` 沒設。**要開就是設那兩個變數**，
放 `arb/engine/.env`（已被 .gitignore 擋）。

**做之前要先想清楚的一件事**：Telegram 帳號在 2026-09-05 被盜過一次
（`docs/SECURITY.md`）。控制通道能 `flat` 一個真錢部位 —— 平倉不會開新
曝險，所以最壞情況是「有人幫我們平倉」，不是被開倉。但那仍然是一個
**新的、掛在一個被盜過的帳號上的**控制面。所以：專用 bot、專用私人頻道、
token 只在 `.env`，而且**不要**把 chat id 跟 V7 那個共用。

### 二、看護的持久化排程

`research/ops/hmm_watch.py`（2026-09-14 寫的）現在只在 session 的背景迴圈
裡跑，session 一結束就沒了。要接成排程：

* 放在 flow_system 而不是 arb —— 單向橋的方向（flow_system 讀 arb）。
* 用 `research/ops/run_hidden.vbs` 的包裝（`schtasks` 建的工作預設會彈
  視窗，mistake.md 2026-09-06；而 VBS 那支必須 `Run(cmd, 0, True)`，
  不等待的版本什麼都不會啟動）。
* 判準照舊是**產物**：`research/results/hmm_watch.log` 有沒有長出新行，
  不是 `LastTaskResult`。

### 三、順帶

`alert_last.json` 已經在新鮮度看板上（2026-09-13 那條的修法），所以
「告警送不出去」本身會變成一盞紅燈 —— 看護不需要自己再監測自己的嘴。

---


### 1.41b 【shadow 第一輪】三個缺陷，全部是「跑起來才看得見」（2026-09-13 深夜）

使用者：「今晚可以把 live 上線嗎」。答案是**不能，而理由是量出來的**：
HMM 從 `--record-only` 換成 `--shadow` 之後 **22:44 開機、22:59 HALT**，
十五分鐘用完 `max_stale_episodes: 5`，而位置全程是空的、WS 一次都沒斷。

**為什麼 record-only 看不到**：它根本不跑 `_strategy_loop`（engine.py:375），
所以沒有 evaluation、沒有 stale 計數、沒有報價決策、也不跑 `_balance_loop`
（status.json 那兩盞 `ACCOUNT UNKNOWN` 就是這麼來的）。
**這三個缺陷離線一個都測不到。**

---

#### 一、`_scan_maker` 少了 `_scan` 的防重發，而它的 docstring 說「Same guards」

shadow 第一分鐘：**4,394 次報價決策 / 90 秒（49 次/秒）**，
而 live 的節奏是每 `maker_timeout_sec`（20 秒）一張。**差 1400 倍。**

live 一直看不出來：一張掛著的報價讓 `_maker_open` 有值、函式開頭就 return。
shadow 什麼都不掛，於是裸奔。而既有的
`test_shadow_does_not_refire_faster_than_live` **只跑 `mode="taker"`** ——
HMM 走的掛單路徑從來沒被覆蓋。

修了（arb `98aef93`），49/s -> 0.19/s，log 寫入量少 88 倍。
live 只有一種情況會變：成交之後（那正是吃單路徑一直以來的行為）。

#### 二、M5 沒有儀表 —— 會 HALT 的那個計數器沒有任何地方看得到

X4 四個判準裡 M2/M3/M4 都在看板上，**M5 缺席**。而 `max_stale_episodes` 的
計數在 HALT 之前完全隱形：新鮮度看板讀 `ok`，而 `ok` 只有 RED guard 會翻。
接進 `status.json` 的 counts ＋ 看板一欄（arb `3d6397a`），
著色刻意在**上限前一格就紅** —— 那時還來得及調設定，HALT 之後只剩人工重啟。

#### 三、根因：HL 的 feed 每 5 秒自我證明存活，Lighter 從來不會

`staleness_sec` 比的是 `book.alive_ts`，而 `alive_ts` 由**任何入站訊框**蓋。
`book.py` 的檔頭寫著設計意圖：「a quiet market is not stale, only a dead
feed is」—— **而那句話對 HL 成立、對 Lighter 不成立**：

    HLBookFeed._pinger   每 5 秒送 {"method":"ping"}  -> alive_ts 建構上每 ≤5s 刷新
    LighterBookFeed      只被動回 {"type":"pong"}      -> 完全看市場臉色

`feeds.py` 甚至把這個不對稱描述出來了（第 6 行 server pings / 第 9 行
client app-pings），只是沒有人發現它跟 `staleness_sec` 有交互作用。
GMX 把它放大到會痛：**日成交額 $9,534、頂檔中位 32 秒才動一次**。

量測（10 分鐘、同一條 WS、兩段對照）：

| | n | 中位 | p90 | max | >10s |
|---|---|---|---|---|---|
| 被動 | 61 | 0.95s | 17.85 | **20.10** | **24.6%** |
| 每 5 秒 ping | 101 | 2.05s | 6.07 | **12.08** | **1.0%** |

伺服器回 pong 中位 **38 毫秒**。12.08 不是 5：約每六次 ping 有一次沒被回。

修法（arb `f600539`）：`LighterBookFeed._pinger` 鏡像 HL 那支，
`execution.ws_ping_sec` **預設 0.0 = 關**，只有 `config_HMM_GMX` 打開；
`staleness_sec` 10 -> 30（同樣只有那一份）。

**預設關是這件事最重要的一行**：`recorder.py:341` 用
`is_fresh(staleness_sec)` 數 `samples`，而那走 `alive_ts` ——
全域打開會膨脹九支錄製器的樣本數，那正是 `book.is_fresh` 的 docstring
明文禁止的「moving what counts as a sample would move the instrument
under a measurement already in flight」。三支測試釘住，其中一支直接掃設定檔。

驗收（同一個時間窗前後對照）：修之前 6 分鐘 -> episode 2/5、15 分鐘 -> HALT；
修之後 **4.5 分鐘 -> 0/5、streak 0**，Lighter age 穩定在 4.5 秒。

---

#### ⚠ 語意分界：GMX 的 `minutes.csv` 從 2026-09-13 23:2x 起，`samples` 換了定義

這是這次改動**唯一**動到既有資料的地方，而它是 CLAUDE.md 核心原則 7
（「產生某個欄位的程式碼改了意義，它之前的每一列就在量別的東西」）。

`recorder.py:341` 用 `is_fresh(staleness_sec)` 決定一秒算不算一個 sample。
`staleness_sec` 從 10 改成 30，於是：

    改之前 153 分鐘   samples 中位 **39/60**
    改之後  12 分鐘   samples 中位 **59/60**

**50% 的跳躍**，而價格一個都沒變 —— 多出來的是「連線活著但簿口十幾秒沒更新」
的那些秒，它們記的是同一組價格。所以受影響的是**分母與分鐘內的加權**，
不是價格本身。

**範圍只有 GMX。** 九支錄製器的設定一個字沒動（`test_recording_family_
configs_have_no_ws_ping` 直接掃設定檔釘住），所以 §0.75／§1.02 的證據基礎
完全不受影響 —— 那正是 `ws_ping_sec` 預設 0.0 的理由。

**為什麼還是要寫下來**：`arblib/half_life.py` 會**自動掃 LOGS 底下每個
有 minutes.csv 的目錄**（`os.scandir(LOGS)`），所以它會撿到 GMX；
而 `band_convergence.py:85` 連 `minutes.csv.*.old` 一起讀，所以輪替也隔不開。
兩支都不在任何排程上（是手動分析工具），所以今天沒有損失 ——
**但下一次有人手動跑它，兩種定義就會被合在一起而且不會有任何東西報錯。**

任何用到 GMX minutes 的分析，`since` 要 floor 在這個分界，或明說它跨了。
（同族：CLAUDE.md 的 `DECODE_EPOCH` / `CONFIDENCE_EPOCH`。）

#### 未做（登記，不擋今晚）：`staleness_sec` 應該是逐場館的

現在它是一個全域值，而兩條腿的自然節奏差兩個數量級：

    HL       訊框間隔 0.3 秒
    LIGHTER  訊框間隔 中位 3.6 秒 / max 34 秒（無心跳時）

**一個全域門檻必須遷就慢的那腿，於是它對快的那腿一定太鬆。**
現在的 30.0 對 HL 的意思是「容許一本比它自然節奏舊 100 倍的簿口」。
心跳把 Lighter 拉近了，所以這件事今晚不痛 —— 但它是**結構性的**，
第二個市場上線（尤其一條腿換成別的場館）就會再咬一次。

修法：`staleness_sec` 拆成 `entropy` / `hedge` 兩個，或給 `VenueConf` 一個
覆寫欄位（`ws_ping_sec` 已經走了後者那條路，可以照抄）。

#### 還開著的（今晚剩下的）

* **episode 曲線要跑滿一小時**才算數 —— 4.5 分鐘只證明它不再立刻爆。
* **M2 在 shadow 裡量不到**（`quotes_rested = 0`）。成交率 ≥30% 是四個判準
  的第一個，而它本質上只有 live 才有 —— 那正是第 9 次 override 的理由。
  但要有預期：**日成交額 $9,534、頂檔中位 32 秒才動一次**，
  M2 會累積得很慢，不是幾小時看得出來的東西。
* `[LIGHTER] position fetch failed: 405 (CloudFront/WAF)` 偶發於 balance
  loop 的 REST。會重試，未量頻率。


---

### 1.41 【Stage 1 上線準備】C1 的紅燈是我比錯統計量、候選名單修正，而**下單層早就寫好了** —— 第 9 次 override 草案 ＋ 金鑰綁定（2026-09-13）

使用者：「先查一下然後起草 override 跟規劃一下交易所的金鑰綁定準備」。
三件事依序做完，而**第三件改變了前兩件的內容**。

---

#### 一、C1 的紅燈：不是 join 壞了，是我拿兩種統計量互比

把 `sweep_markout.py` 的宇宙從 80 個幣放寬到 200 之後，自曝檢查 C1
（「單獨成交的 markout(δ=0) 必須約等於 ＋半價差」）從 PASS 變成
**0.577 vs 0.358 -> 查 join**。照規矩先查自己，而錯的是判準本身：

| 量 | 值 | 怎麼算的 |
|---|---|---|
| 單獨成交的 markout(δ=0) | **0.527** | **成交**加權 |
| 半價差（舊對照） | 0.355 | **時間**中位 |
| 半價差（新對照 `hs_fill`） | **0.449** | **成交**加權、且只取單獨成交那一刻 |

**薄的標的價差本身在變動，而成交集中在價差寬的時刻** —— 所以「成交加權的
半價差」必然大於「時間中位的半價差」（實測高 21%）。拿中位去比，就是把
觀測與預期用了兩種不同的加權（mistake.md 2026-09-12 第二條：觀測÷預期型的
判準，分子分母要同一種統計量）。

修法兩處，都寫在原地：`hs_fill` 用成交額加權，**而且母體限定在單獨成交**
（C1 比的是 `mo_no`，不可把掃單那一刻的價差混進來）。
重跑 **C1 PASS（差 17%）**，舊的中位值仍然印在旁邊當對照。

同一次重跑的新窗（7.11 小時、387,932 筆）：掃單佔**張數 23.3% / 成交額 71.4%**。

#### 二、候選名單：AI 出局，而出局的理由是一道新閘門不是我挑的

`stage1_sizing.py` 的 E 閘門改成**跨視窗穩定**（15s / 30s / 60s 三格都要
> maker 0.40 bps）之後：

* **E 22 / 71｜C（換邊率 25–75%）35 / 71｜兩關全過 8 個**
* **AI 不見了** —— 它的 markout@1s 是 +47，30 秒是 −32.6。半價差 64.88 bps、
  簿口年齡中位 917 ms / p90 28 秒，所以 1 秒的視窗比簿口更新還快，
  量到的是半價差本身。它原本佔投射損益的 60%。

| coin | 邊際 bps（三視窗最小） | 換邊% | 做市者 | 我成交/天 | size$ | 幾天到30 | 庫存上限$ | 日損益$ |
|---|---|---|---|---|---|---|---|---|
| NEAR | 4.935 | 35 | 59 | 100.1 | 20 | **0.30** | 1,203 | 0.90 |
| GRAM | 5.519 | 41 | 10 | 24.8 | 25 | 1.21 | 520 | 0.31 |
| ADA | 4.275 | 28 | 19 | 13.9 | 13 | 2.15 | 131 | 0.07 |
| LINK | 2.632 | 35 | 45 | 8.1 | 51 | 3.72 | 512 | 0.09 |
| DOGE | 0.869 | 41 | 28 | 14.4 | 20 | 2.09 | 108 | 0.01 |
| TAO / AAVE / BNB | 1.17 / 0.95 / 0.52 | 33–40 | 31–43 | 12–17 | 24–73 | 1.8–2.5 | 230–601 | 0.01–0.05 |

**合計 $1.47/天（8 個市場）**，我方日成交額 $5,646。前五名庫存上限合計
**$2,476**（安全倍數 2）。

**損益的邊際也換成三個長視窗的最小值** —— 閘門說 1 秒不可信，損益就不可以
還用 1 秒算錢。

#### 三、最重要的一件：**這條線的下單層已經存在，我差點要重寫一次**

`arb/engine` 的 B3 掛單路徑（2026-09-04）不只是「有掛單功能」，它的
`execution.maker_venue` **就是在選哪一腿掛單**：

    _maker_legs():  maker_v = entropy if maker_venue == "entropy" else hedge
                    taker_v = 另外那一條

所以 **`mode: maker` ＋ `maker_venue: hedge` ＋ `--hedge lighter`
＝ 掛單在 Lighter、吃單對沖在 HL** —— 逐字就是 §1.40 的構造，
零行新程式碼。`maker.py` 的三條規則（本地狀態只由交易所真相清除／成交逐筆
消化／未確認的撤單是悲觀的）已經寫好並審過。

> **⚠ 2026-09-13 同日更正：「零行新程式碼」是錯的，而查出它的是第一次
> `--shadow` 彩排。** 第一個評估週期就死在
> `AttributeError: 'LighterVenue' object has no attribute 'maker_fee_bps'`
> —— 那個屬性只存在於 `VenueConf`，**兩個 venue class 都沒有**，
> 所以 `mode: maker` 在**兩條腿上都是壞的**。B3 那條掛單路徑寫完、審過、
> 在 `plan_maker` / `maker.py` 那一層單元測試過，但**從來沒有用真的 venue
> 物件跑過 `_scan_maker`**。引擎測試用的 StubVenue 是照測試需要定義屬性的，
> 所以它結構性地看不見這件事。
>
> 這是 flow_system facade-skip 那一族（重演三次才有人寫 AST 測試）。
> 修法照同一個處方：補上兩個 venue 的 `maker_fee_bps`，
> **再加一道結構性守衛** `engine/tests/test_venue_surface.py` —— AST 掃
> `engine.py` 讀過的每一個 venue 屬性，斷言兩個 class 都有，豁免必須具名
> 並寫理由。它當場又抓到第二個：`self.entropy._query_address()` 掛在一個
> 只檢查 `hedge.kind` 的條件後面，所以 `entropy.venue: lighter` 配
> `--hedge tradexyz` 會炸（潛伏的，這台機器還沒有那個組合）。
>
> **所以正確的說法是：執行層的「難的部分」已經存在（B3 狀態機、對沖、
> 對帳、HALT），但它從來沒被執行過，而沒被執行過的程式碼不算能跑。**
> 彩排重跑之後：零 traceback、兩條 WS 都連上、strict reconcile 讀到兩個
> 帳戶的真實部位。修法在 ../arb `6d29e8c`。

**但這件事同時砍掉 §1.40 的大部分候選。** `HEDGE_VENUES = (lighter,
lighter-rh, tradexyz)`，entropy 腿是 HL —— **CEX 不是可對沖場館**：

| §1.40 的 290 個正毛邊際配對 | 對沖場館 | 今天跑得動？ |
|---|---|---|
| 96 | okx | **不行**（沒有 CEX 場館模組） |
| 72 | binance | **不行** |
| 44 | bitget | **不行** |
| 28 | lighter-std | **不行**（那是 Lighter 的 Standard 費率層＝**第二個帳號**，而 `config.py` 只有一組 mainnet 憑證槽） |
| **29** | **HL** | **可以，而且零新金鑰** |
| 13 | xyz（tradexyz） | 可以（預設共用 HL 帳戶） |
| 4 | lighter-rh | 要 `LIGHTER_RH_*` |

掛單腿是 lighter 且對沖腿在引擎已實作的場館：**38 個配對**，其中
**24 個對沖在 HL**。

| ticker | 對沖 | 掛單半價差 | 對沖半價差 | 深度$ | 毛邊際 | 同時有報價的格數 |
|---|---|---|---|---|---|---|
| **GMX** | HL | 33.48 | 1.41 | 13.45 | **+27.17** | 6,357 |
| PROVE | HL | 23.57 | 4.76 | 399.93 | +13.91 | 6,309 |
| APEX | HL | 19.23 | 3.35 | 22.45 | +10.98 | 6,301 |
| BIO | HL | 13.23 | 1.28 | 14.43 | +7.04 | 6,319 |
| EIGEN | HL | 12.00 | 2.48 | 471.77 | +4.62 | 6,326 |
| MORPHO | HL | 10.63 | 1.16 | 107.43 | +4.57 | 6,313 |
| OAI | lighter-rh | 60.42 | 2.23 | 17.82 | **+54.99** | 1,295 |

**這 21 個候選 ticker 全部現在仍在 Lighter mainnet 上市**（234 個市場，
逐一對過），最小下單額一律 **$10**。

> **差點寫下的錯誤結論，留檔**：第一次查上市狀態時我用 `OAI` 去比，
> mainnet 沒有，我幾乎要寫「OAI 已下市」。它在 mainnet 與 RH 的符號都是
> **OPENAI**，掃描器做了正規化。**符號別名不是下市** —— 而「下市」會讓
> 這條線的頭號配對被誤判掉。查上市狀態要比**兩邊的原始符號表**。

#### 四、綁束不是 size，是**抵押品**（這就是「size 怎麼樣才可以正常跑」的答案）

`python tools/check_env.py --root` 今天實跑，**全綠**，而餘額是另一件事：

| 項目 | 狀態 |
|---|---|
| HL agent wallet `Arb` | **已授權，到 2026-12-11 12:20Z（還 89 天）** |
| HL_PRIVATE_KEY / ADDRESS | 64 hex ／ 0x5777…152e，形狀正確 |
| Lighter mainnet 帳號 743078、api key index 4 | 存在、**可簽**（chain_id 304） |
| **Lighter mainnet 權益** | **$18.89** |
| **HL 權益** | **$0.00**，0 個持倉 |
| LIGHTER_RH_* | **沒設** |
| OKX / Bitget / Binance（`arb/.env`，M1 唯讀） | **沒設** |

所以 `stage1_sizing.py` 算出的庫存上限 $2,476 **不是現在的約束** ——
現在的約束是「一個場館有 $18.89、另一個有 $0」。
而 X4 規格（`arb/docs/LIVE_50U_SPEC.md`，2026-09-04 凍結）早就寫了答案：
**每所 $50**。所以缺口是 **HL ＋$50、Lighter ＋$31**，不是一個要我重算的數字。

**一個還沒有解的結構問題，必須在上線前決定**：引擎一次只允許**一張**掛單
（`if self._maker_open: return None`），而一個行程只跑一個 ticker。
所以 5 個市場 = **5 個行程，共用同一個 Lighter 帳號與同一個 HL 帳號**，
而 `max_position_usd` / `max_gross_usd` 是**逐行程**的。
**5 個行程各自守 $1000 上限，帳戶層可以是 5 倍。**
這正是 CLAUDE.md §帳戶隔離 付過兩次代價的那個形狀（kill switch 分不出
虧損是誰造成的），只是這次對手是我們自己的另一個行程。

> **✅ 2026-09-13 同日解掉了（使用者：「先把這個解掉」），所以上面那句
> 「沒有中央曝險帳本之前市場數只能是 1」不再是綁束。** 修法分兩半，
> 而它們的回應刻意不同：
>
> | | 在哪 | 量什麼 | 超了怎麼辦 |
> |---|---|---|---|
> | **預防** | `_headroom()` 多兩個項 | 帳戶層曝險與剩餘抵押品 | 縮小或不開新倉 |
> | **偵測** | `_maybe_hedge()` | 帳戶層曝險已經超過天花板 | **HALT**（一次性，要重啟） |
> | **啟動前** | `tools/account_budget.py` | Σ 逐行程額度 vs 帳戶天花板 | 紅燈、不要啟動 |
>
> **帳本就是交易所，沒有共享檔案也沒有鎖。** 關鍵發現是
> **帳戶層的數字本來就在我們已經付過錢的那個回應裡**：
> `fetch_position()` 兩邊都是抓**整個帳戶**再濾掉只留一個市場 ——
> HL 的 `marginSummary.totalNtlPos` 就是全市場名目合計，
> Lighter 的 `positions[].position_value` 逐筆加總也是。
> 所以**零額外 API 呼叫**，而且它是交易所真相，也就是這個引擎唯一
> 允許用來清除本地狀態的東西（`maker.py` 規則一）。
> 新檔 `engine/entropy_arb/account.py` 把它包成 `venue.exposure`。
>
> **兩個新開關，都進了 `REQUIRED_RISK`（live 沒設就拒絕啟動）**：
>
> * `risk.max_account_gross_usd` —— 帳戶層的 |部位×中價| 合計上限。
>   超過就 HALT，而訊息會講清楚**有多少不是這個行程的**
>   （`mine` vs `others`）。**不會去動別人的部位** —— 那是
>   `admin_heal` 的錯（mistake.md 2026-06-07：歸零你不擁有的狀態）。
> * `risk.min_account_free_usd` —— 帳戶剩餘可用抵押品的下限。
>   **只擋開新倉，不 HALT**：危險的不是手上那組對沖部位，是
>   「掛出一條對沖腿卻沒有保證金可以對沖」，而那取決於**還剩多少**；
>   而且抵押品會自己恢復，halt 需要人。
>
> **三個 fail-closed 的方向都實測過**：`exposure` 還沒讀到 → headroom 0；
> 快照超過 4 倍 `reconcile_sec` 沒更新 → headroom 0（`_venue_down` 要連續
> 三次失敗才擋，這一層補那個窗口）；剩餘抵押品低於下限 → headroom 0。
>
> **啟動前那一關的真相源是看門狗的 `$Members`，不是 `config_*.yaml` 的
> glob** —— 後者會數到從來沒被排程的設定檔。但只讀那張表會漏掉
> 「寫了 `run_GMX.bat` 還沒加進註冊表」，所以它同時掃 `engine/*.bat`
> 把遊蕩的 live 啟動器單獨報出來（B4）。
>
> **接線與反向證明（兩者都做了，缺一個這個守衛就是隱形的）**：
> 看門狗每 5 分鐘跑一次並寫 `arb/results/account_budget.json`
> （實跑過：log 多一行 `account budget OK`）；
> 新鮮度看板新增一列 `arb 帳戶額度加總 (B6)`，
> **綠過一次也紅過一次**（注入 ok=false -> `1 red / 55 tracked` 指名該列，
> 還原 -> `recovered`）。
> 21 個新測試（`engine/tests/test_account_risk.py`），引擎 157 個全綠。
> 每個閘門都配一個「不該綁束時不綁束」的對照，包括
> 「沒設開關時 `_headroom` 的值與 B6 之前逐位元相同」。
>
> **所以 Stage 1 仍然從 1 個市場開始，但理由換了** ——
> 不再是「沒有中央曝險帳本」，而是「這條 call path 從來沒有送過真單」。
> 加第二個市場的前置條件也換了：**帳戶天花板已武裝 ∧ 啟動前加總是綠的**，
> 不是「去蓋一個中央曝險帳本」。

---

#### 五、第 9 次 informed override —— **草案，等使用者的話才生效**

> 下面這一節照 §1.03 的格式寫好，但**使用者決定那一行是空的**。
> 使用者說出那句話之前，這是一份草案，不是一條 override；
> 生效時把它原話填進去、整節搬進 CLAUDE.md。

**使用者決定（原話）**：＿＿＿（待填）

**這條 override 推翻的是什麼（誠實版）**

不是 CLAUDE.md 的「策略 #2/#3 不得進 executor」—— 套利是第 4 線、
鏈上是第 5 線，兩條的定位都是「只錄不做」。真正被推翻的是**我們自己在
§1.40 寫下的 Gate 0**，而它有五關，現在的狀態是：

| 關 | 內容 | 狀態 |
|---|---|---|
| G1 | 每 ticker 每天 ≥ 10 筆可能打到我們的成交 | **8/9 過，但過的是宇宙的補集** —— 成交帶錄前 80 名（價差緊），而 20–60 bps 的毛邊際全在看不見的 57 個長尾裡 |
| G2 | 被打到的成交 markout > −毛邊際（逆選擇） | **沒量。** 而 backtest-audit 第 13 項的陷阱（逆選擇最低的往往是 API 壞掉的）也沒查 |
| G3 | 最大 1/5/10 個 ticker 佔總 $/日 | **量了：30% / 93% / 100% -> 那是 5 個機會不是 8 個** |
| G4 | cost=0 對照 | 沒做 |
| G5 | 用判決那台機器跑 bootstrap SE | 沒做 |

**所以這是一次知情的推翻，而且推翻的是自己三天前寫的判準。**
§1.40 自己的「下一步」寫的是「放寬成交帶宇宙，7 天後重跑 G1」，
而這條 override 要在那 7 天到期之前先上線。

**為什麼仍然值得做，而理由不是「我覺得會賺」**

這次要買的東西**統計上買不到**，跟 §1.03（第 7 次 override）同一個論證，
只是買的品項不同：

| 問題 | 誰能回答 |
|---|---|
| 長尾的流量夠不夠（G1） | **成交帶，而且只能等** —— 實盤在這題上嚴格更差（樣本更少、還多了成交的變異） |
| **我們掛進去之後簿口長什麼樣** | **只有實盤。** §1.40 的「未量測 1」寫著「掛進去會改變簿口，而我們沒有任何資料說這件事」—— 那是 backtest-audit 第 16 項（博弈動態），而它**沒有任何離線儀器量得到** |
| **掛 20–60 bps 打到我們的是什麼流** | 只有實盤（G2 的分子要我們自己的成交） |
| 兩腿延遲、對帳、HALT 在真實延遲下撐不撐得住 | 只有實盤（X4 的 M4 / M5） |

**第二列是這條 override 的全部理由。** 它不是「提前驗證」，是
**一個離線量不到的量**，而 §1.40 已經把它標成推論不是量測。

**為什麼代價比第 7 次低**

| | 第 7 次（2026-09-08，SDV） | **本次** |
|---|---|---|
| 帳戶 | Bitget，**與 V7 共用**（已被手動單爆過兩次） | **Lighter ＋ HL 的獨立帳戶**，不碰 OKX/Bitget 體系 |
| 金額 | $300–500 | **每所 $50**（X4 凍結值） |
| 下單層 | 產品端新寫 | **既有的 B3 掛單路徑，已審過** |
| 方向暴露 | 有（單邊持倉） | **兩腿對沖**，設計上沒有方向 |
| 市場數 | — | **1 個**（理由是第一次送真單；帳戶層上限已武裝，見第四節）|

**寫死、事後不放寬的紀律**

* **損益不得作為 edge 證據。** Gate 0 的 G1/G2/G5 判決只認計分器的輸出，
  不吃這批實盤損益（§0.92 判掉 C/D 變體的同一種錯）。
* **唯一要看的四個判準**，全部是 X4 的既有條目，不新發明：
  M2 掛單成交率 ≥30%（<10% 這條路關掉）／M3 掛單成交後 60 秒中價漂移
  > −1 bps／M4 兩腿延遲中位 <2s、p95 <10s／M5 一腿行情延遲 >10 秒時
  引擎必須 **HALT 而不是補單**。
* **市場數 = 1，而且不得因為「它在賺」而加。** 理由是**第一次送真單**，
  不是「沒有中央曝險帳本」—— 帳戶層的曝險上限當天（09-13）已經做好並
  反向證明過（見第四節的 ✅ 區塊）。所以加第二個市場的前置條件是
  **`max_account_gross_usd` 已武裝 ∧ `account_budget.py` 是綠的**，
  而不是績效。
* **不得挑 ticker。** 上線的那一個在**跑之前**寫進本節並 commit。
  （候選排序已在上面第三節，GMX 是毛邊際最高且對沖腿零新金鑰的那個。）
* **不得退回 Standard 費率層省那 0.40 bps。** CLAUDE.md 已判 Standard 的
  60 req/min 結構上做不了做市。
* 要放大金額或市場數，另開一次 override。

**代價自負（使用者知情後選擇的）**

* **第一次送單就是真錢**，而掛單路徑的 `unknown` 狀態（撤單超時）
  在真實網路上從未發生過。
* **資金費沒算進毛邊際**。兩腿都是永續，持有過結算點就付／收。
* **提款費／gas 沒算**，要有可成交量才攤得出來。
* **未成交的放棄邊際沒算** —— §1.28 量到被動執行要付 7.27 bps，
  那是在 Binance 厚標的上量的，這裡未知。
* 緩解：每所 $50、單市場、兩腿對沖、`max_position_usd` 壓到 size 的
  兩倍以內、M5 的 HALT 不可關。

---

#### 六、金鑰綁定準備 —— 依相依排序，而前兩層**今天就是完成的**

X4 §7 已經有一份檢查表（2026-09-04）。今天實查之後它該長這樣：

**第 0 層：掛單 Lighter ＋ 對沖 HL（24 個配對，零新金鑰）**

* [x] HL **API (agent) wallet** —— 已建，`Arb`，到期 2026-12-11。
  agent wallet **永遠不能提幣或轉帳**，所以 DEX 腿的金鑰風險已經拉平到
  CEX 腿的等級。**這一格是 X4 §7 的第 1 項，已完成。**
* [x] Lighter mainnet 帳號索引 ＋ API key 索引 ＋ API 私鑰 —— 已填、可簽。
* [ ] **入金**：HL ＋$50、Lighter ＋$31（目前 $0.00 / $18.89）。
  **這一步我不做** —— 轉帳是使用者的事。
* [ ] agent 到期日進 freshness（89 天後會安靜失效，而「簽章突然不能用」
  是引擎解釋不了的訊息）。

> **穩定幣：這一層兩腿都是 USDC，所以沒有穩定幣基差**（2026-09-13 查證）。
> HL core 的永續保證金是 USDC；Lighter mainnet 的抵押品也是 USDC
> （`/api/v1/account` 的 `assets[0]` = `USDC`, asset_id 3,
> `margin_balance` 18.89）。HL 現貨那邊另有 USDE / USDT0 / USDH 的欄位，
> 但**永續只認 USDC**。
> 這是選第 0 層的第四個理由（前三個：引擎跑得動、零新金鑰、GMX 毛邊際最高），
> 而它在下面第 1 層與第 3 層都不成立。

> **HL 的錢分兩個口袋，而永續那個從來沒被啟用過**（2026-09-13 查帳本）。
> `userNonFundingLedgerUpdates` 全帳戶只有**一筆**：09-04 從另一個 HL 帳戶的
> **現貨**送進來的 11.8683 USDC。**零筆橋入金、零筆 spot⇄perp 劃轉** ——
> 所以 `clearinghouseState.accountValue` 是 $0.00 而現貨有 $11.87。
> 操作者回報「找不到任何劃轉鍵」，很可能是介面在永續權益為 0 時不渲染那個面板。
>
> 一個會被 AI 摘要帶歪的點，寫下來：Google 的 AI 摘要說
> 「Hyperliquid 現貨與永續共用同一個保證金帳戶、不需要劃轉」。**那是錯的。**
> 兩個反證：(a) 我們自己的帳戶兩個端點不同值（共用的話 accountValue 會是
> 11.87）；(b) HL 的 exchange 端點有 `usdClassTransfer`，參數帶 `toPerp`
> 布林值 —— 一個需要指定方向的 API 不可能是共用帳戶。
> **判準是帳戶回報，不是文字描述**，所以 `tools/funding_status.py` 是唯讀
> 去問交易所而不是照文件寫死。

**第 1 層：lighter-rh（4 個配對，含毛邊際最高的 OAI/OPENAI +54.99）**

> **⚠ 開這一層之前先知道：它的兩腿穩定幣不同，而那塊風險沒有被對沖掉。**
> `lighter-rh` 報價在 **USDG**，mainnet 在 **USDC**。所以
> lighter × lighter-rh 的配對對沖掉的是**標的價格**，沒對沖掉的是
> **兩個穩定幣之間的匯率** —— USDG 脫錨 1%，那個「中性」部位就多出 1%
> 的損益，而它跟標的漲跌完全無關。
>
> 這條**早就在成本模型裡**（`arblib/cost_model.py:112`）：
> `("stablecoin depeg USDG/USDC", 0.10, 0.03, "ASSUMED; lighter-rh quotes in USDG")`
> —— 機率 0.10、損失 0.03，而且標著 **ASSUMED，我們沒量過**。
>
> **它打到的正好是最肥的那個配對**：OAI/OPENAI 毛邊際 +54.99 是全表第一，
> 而那 54.99 裡有一塊是在收 USDG 的脫錨風險溢價，不是純粹的價差。
> 所以第 1 層的 Gate 不只是「金鑰辦好了沒」，還要回答
> **「USDG 的基差在我們持有的時間尺度上有多大」** —— 那是一個可以用
> 兩條腿的報價差直接量的東西（掃描器 13.6 天的資料裡就有），
> **在開 RH 帳戶之前先量，不要開完才問**。
>
> 同一個形狀在第 3 層（CEX）是 **USDC × USDT**：USDT 比 USDG 穩得多，
> 但一樣不是零，而且屆時的配對數是 212 個不是 4 個。

* [ ] 在 **RH 鏈**另開一個 Lighter 帳號（與 mainnet 是不同的鏈、不同的帳號）
* [ ] 用 `tools/lighter_new_api_key.py` 產 RH 的 API key —— **使用者自己跑**，
  那支用 `getpass`、只印長度不印值；我不碰私鑰。
* [ ] 填 `LIGHTER_RH_ACCOUNT_INDEX` / `_API_KEY_INDEX` / `_API_PRIVATE_KEY`
* [ ] 跑 `check_env.py` 確認 RH 帳號**存在於 RH 鏈**

> **這一層有一個安靜的地雷，寫下來**：`config.py:53` 在 `LIGHTER_RH_*`
> 為空時**回退到 mainnet 的憑證**，然後把它指向 RH 的 host。
> 也就是說「兩腿都是 Lighter」的設定在沒填 RH 金鑰時**不會報錯**，
> 它會拿 chain_id 304 的簽章去打 chain 466324。`check_env.py` 已經會對
> 這件事印一行，**判準是看到那一行變成 OK，不是看到引擎啟動成功**。

**第 2 層：tradexyz（13 個配對）**

* 預設共用上面的 HL 帳戶，**不需要新金鑰**。
  要獨立帳戶才填 `HL_PRIVATE_KEY_XYZ` / `HL_ACCOUNT_ADDRESS_XYZ`。

**第 3 層：CEX（okx / bitget / binance）—— 212 個配對，而它是工程不是金鑰**

* X4 §7 把這三個標成「未實作（新工作）」，而今天確認了它的意思：
  **沒有 CEX 場館模組，所以金鑰辦好了也跑不動。**
  先辦 key 是**白做**，順序要反過來：先寫場館模組，再辦 key。
* **唯讀那一組例外，而且它有獨立的價值**：`arb/.env` 的
  `OKX_/BITGET_/BINANCE_` 是給 `fee_receipts.py`（X4 的 M1）用的，
  **只需要讀取權限、不要勾提幣、綁 IP 白名單**。它回答的是
  「費率表相信的數字 vs 交易所實際收的數字」，而 CLAUDE.md 的費率
  今天為止仍然是**文件查證不是收據查證**。
* [ ] 子帳戶 ＋ 只勾讀取 ＋ IP 白名單（三所各一組）

**四條安全界線（不變，重申）**

1. 私鑰我不碰：產 key 的腳本使用者自己跑，我不執行轉帳、不點會顯示
   私鑰的介面。
2. `.env` / `config.json` **絕不進 git**（`.gitignore` 已覆蓋）。
3. 套利線的錢**只放這條線**，不與 V7／獵取／個人資金共用帳戶。
4. 公開面只出百分比、方向、時間與筆數 —— 不出金額、張數、權益。

---

#### 上線前的第 10 個發現：Lighter 的 WS 拒絕新連線，而原因是我們自己（2026-09-13）

驗收 record-only 那一輪時 `lighter_tape` 紅了。最小測試：

    wss://mainnet.zklighter.elliot.ai/stream
    -> WebSocketBadStatusException('Handshake status 403 Forbidden')

**而既有連線全部正常**：GMX 引擎的 Lighter 簿口 3.3 秒、NBIS 0.08 秒、
`lighter_mid` 201/217 市場活著、49.5 萬列、最後訊息 0 秒前。
**所以是連線建立被擋，不是封 IP。** 殺掉重啟 -> 又拿到 403、凍在 uptime=1s，
證明它不是半開 socket 而是結構性的。

**代價**：`lighter_tape` 死了約 1.7 小時，而它錄的是**不可回填**的 217 市場
長尾成交帶 —— 正是 §1.40 下一步在等的那份資料。

#### 他們的限制（查了，而且查到的比沒查到的少）

文件（`docs.lighter.xyz/llms-full.txt`）只寫了 Plus：
**sendTx/sendTxBatch 8,000/分、唯讀權重 120,000/分**。
**Standard 與 Premium 的數字、WS 連線數上限、每連線訂閱數上限 —— 一個都沒寫。**
CLAUDE.md 記著 Standard 約 60 req/min（來自分級對照，非文件）。

**而擋住我們的那一層根本不是帳戶分級。** 403 來自 CloudFront/WAF
（同一層在 REST 上回 `x-amzn-waf-action: captcha` 的 405），它是 **per-IP**，
而且**掃描器一個憑證都不帶** —— 對那一層而言，我們帳戶是不是 Premium
完全不相關。所以「升級分級」不會解決這件事。

#### 我們自己的腳印（量出來的，不是估的）

| 來源 | Lighter REST | 怎麼算的 |
|---|---|---|
| **掃描器** | **65 次/分** | 最後一輪去重後 lighter 143 ＋ lighter-rh 53 = 196 次，`CYCLE_SEC = 180` |
| 10 支引擎 reconcile | **40 次/分** | 每支 `reconcile_sec = 15` |
| 引擎 keepalive ／ 其他 | 約 10 次/分 | `http_keepalive_sec = 10`，閒置時才打 |
| **合計** | **約 105–115 次/分，24 小時不停** | |

另外還有**約 12 條常駐 WS**（`lighter_tape`、`lighter_mid`、每支引擎一條簿口）。

**掃描器一支就佔 65/分，是 live 引擎（約 4/分）的十六倍。**

#### 所以對上線的意思

**live 引擎不是主因，但它是受害最重的那個。**

* 正常運行沒問題 —— WS 是長連線，403 只擋新連線。
* **危險在重連**：引擎的 Lighter 簿口若在封鎖期間斷線，它**回不來**，
  而那時它手上有部位。`_venue_down` 會在三次失敗後暫停交易、
  `max_consecutive_stale` 在有曝險時 HALT —— 也就是說系統會正確地停下來，
  **但會停在一個對沖腿連不上的狀態**，而那正是這條線最不該處在的狀態。
* 而觸發封鎖的是**研究負載**，不是交易負載。

#### 提案（不自己動，理由見下）

**把掃描器的 Lighter 節奏從 180 秒放寬到 600 秒**：65 -> 20 次/分，
總量掉到約 60/分。

**這不損失任何東西**，而且理由是掃描器自己的量測：§1.39 量到它的
**腿差 53 秒**（lighter 那一段是序列 for 迴圈加 sleep），所以 180 秒的格
本來就超出它能分辨的解析度 —— 它是一個 13.6 天的排名儀器，不是即時行情。

**不自己動的理由**照 mistake.md 2026-09-13 那條：
「併行化那個不該我一個人決定 —— 它在動一個活著的錄製器的請求節奏」。
改節奏會改變 `scan_*.csv` 的格寬，而 §0.75b 的升格排名讀它。

**替代方案**（比較粗但立即）：HMM live 期間把掃描器停掉。
研究負載讓給執行負載，而現在兩者是對等競爭同一個 per-IP 預算，
偏偏研究那一側大十六倍。

#### 現在的處置

**不再打 Lighter 任何 API**，讓 WAF 冷卻。
`lighter_tape` 自己活過來就是封鎖解除的產物證據 —— 那之前不要再重啟它
（每次嘗試都在刷新封鎖），也不要跑 `funding_status.py` 的 Lighter 那一側。

#### 七、下一步（三件，而第一件是使用者的）

1. **使用者決定要不要這條 override**，以及入金（HL ＋$50、Lighter ＋$31）。
   決定之後我把原話填進第五節並整節搬進 CLAUDE.md。
2. 入金之後：寫 `config_GMX.yaml`（`mode: maker`、`maker_venue: hedge`、
   `--hedge lighter`、單市場、`max_position_usd` 壓住），**先 `--record-only`
   跑一輪確認兩腿簿口都活著**，再開 live。
3. 不依賴上面兩件、現在就在跑的：成交帶 217 個市場的長尾樣本累積到 7 天，
   然後用同一支計分器重跑 G1／G2。

#### 本節動到的程式

* `research/mm/sweep_markout.py` —— C1 的對照改成 `hs_fill`（成交加權、
  母體限定單獨成交），舊的中位值保留並列印
* `research/mm/stage1_sizing.py` —— 讀入 15/30/60 秒欄位；損益的邊際改用
  三視窗最小值；視窗標籤的 `rstrip(".0")` 把 30s 印成 3s，修掉

---


### 1.40 【開線＋Gate 0 預註冊】價差套利（掛單在不流動場館、對沖在流動場館）——**判準在量流量之前凍結**（2026-09-13）

使用者三句話定的方向：「我要怎麼運用散戶的資源分一口肉吃」、
**「我願意接受這個數字，我們先求有再求好」**、「先推上去再去量」。

#### 「先求有再求好」改變了什麼、沒改變什麼

**沒改變的**：§1.39（毛利 1.1 對成本 11.5–16）與 §0.75 家族（扣費就負）
**不是「太小」，是負的**。接受小數字不會讓負數變正。

**改變的**：凡是「我們付錢的那一側變成我們收錢的那一側」的構造，
它的門檻本來就低，只是我們一直沒做。本節就是那一類。

#### 這條線不是新發明，是 `small-trader-alpha-1` 推薦的那一個

他把機會分兩類，而**我們過去一年做的全是他不推薦的那一類**：

| | 機制 | 他的評語 | 我們 |
|---|---|---|---|
| **中價套利**（兩腿吃單） | 兩所中價錯位 | 「太容易套，在轉帳到達前就回去了」 | §0.75 / §1.39 **全部是這個，全部 NO-GO** |
| **價差套利**（不流動腿**掛單**／流動腿吃單） | **兩所中價可以完全一樣**，賺不流動那所的價差寬 | **「the favorite」，叫讀者專注這裡** | **從來沒做** |

第二類**不需要錯價存在**。它賺的是結構性質（不流動場館的寬價差），
不是暫時的偏離 —— 所以**沒有收斂風險**，而收斂正是前一類的死因。

而它跟 `why-is-my-backtest-wrong` 那句對得上：他說真的在生產上實現直線的
**本來就回測不出來（做市）**。價差套利就是帶跨場館對沖的做市。

#### 已量到的（同一 ticker 兩條腿，計分器 `research/mrp/spread_arb_paired.py`）

掃描器 13.6 天、10 場館、705 ticker、6,729 個 (ticker, 掛單場館, 對沖場館)
且兩邊同時有報價 >= 1,000 格。要求**兩腿凍結比例都 <= 25%**（擋掉 1,154 個）：

| ticker | 掛單 | 對沖 | 掛單半價差 | 對沖半價差 | 深度 | 毛邊際 |
|---|---|---|---|---|---|---|
| OAI | **lighter** | lighter-rh | 60.09 | 2.22 | $18 | **+54.67** |
| BMNR | lighter | bitget | 34.56 | 2.00 | $48 | +29.16 |
| GMX | lighter | HL | 33.39 | 1.41 | $13 | +27.08 |
| MINIMAX | lighter | bitget | 26.34 | 1.69 | $232 | +21.26 |
| ZHIPU | lighter | bitget | 19.61 | 1.42 | $330 | +14.79 |

**288 個配對為正、156 個 ticker**，掛單場館 lighter / para / okx / bitget /
xyz / IO / lighter-rh。**深度中位 $50。**

毛邊際 = 掛單腿半價差 − maker(掛單腿) − 對沖腿半價差 − taker(對沖腿)。
**未扣**：提款費／gas、逆選擇、未成交的放棄邊際、資金費。所以是上界。

#### 外部佐證（跨儀器，而且是獨立的）

`brucexu_eth` 2026-09-11 的 Robinhood 復盤裡，`Chosmos110` 做的是
**Robinhood Lighter 與 Lighter 之間的價差**，200U 本金、一天約 2U。
**那就是我們的 `lighter-rh` vs `lighter`，而上表第一列正是它。**
我們有 13.6 天逐 180 秒，他有一天實單 —— 兩台獨立儀器指向同一個配對。

**但那是一個匿名帳號的單日自述**（那篇自己也寫「沒有鏈上錢包可驗證就不用
當真」），所以它只能讀成「這筆交易存在」，**不是報酬率估計**。

#### 散戶側的資源（為什麼這一類我們做得，機構做不得）

| 我們有的 | 值 | 機構為什麼用不了 |
|---|---|---|
| Lighter **maker 0.40 bps** | 已付費（Premium） | — |
| **撤單不加延遲** | Premium 專屬（Standard +300ms） | — |
| 撤單往返 **49.9 ms**（實測 n=20） | 已量 | 夠分鐘級報價 |
| Lighter **Standard taker 0.00**（對沖腿） | 可用 | **60 req/min，結構上做不了做市** |
| 沒有法遵部門 | — | 碰不了 OAI / ZHIPU / MINIMAX 這種標的 |
| 小額 | $13–330 頂檔全是我們的 | 基金懶得理 |

**執行路徑已經存在**：`arb/engine/entropy_arb/venue_lighter.py`（下單／撤單／
帳戶 WS）、`tools/cancel_latency.py` 已真的掛過並撤過 post-only 單。

#### Gate 0 判準 —— **在量流量之前凍結**（核心原則 11）

**G1 流量（唯一的判決數字）。** 深度中位 $50、毛邊際 20 bps
-> **一趟約 $0.01**。所以這門生意完全取決於次數。
判準：逐 ticker 的**每日吃單成交額**（`lighter_tape.py` 正在錄），
要求該 ticker **每天 >= 10 筆可能打到我們的成交**。
低於 10 筆就不是生意（backtest-audit 第 2 項：「3 isn't many」），
而且在任何資金規模下都不是。

**G2 逆選擇。** 在不流動永續上掛 20–60 bps，打到我們的是資訊流。
判準：被打到那些成交的 **markout 必須 > −毛邊際**。
我們自己的 `flow_toxicity` 量到 HL 掛單方全體 **−0.38 bps**，長尾未知。
**這一關有一個已知陷阱**（backtest-audit 第 13 項）：逆選擇最低的標的
往往是 **API 壞掉**的那些。凍結比例 <= 25% 只是第一層，
G2 要另外查報價更新率與長時間空白。

**G3 集中度。** 報 **最大 1 / 5 / 10 個 ticker 佔總 $/日的比例**
（backtest-audit 第 2 項，我們從來沒做過這一項）。
集中在 1–2 個 ticker = 那不是 156 個機會，是 2 個。

**G4 cost=0 對照**（factor-research 第 10 項）：含成本結果不得 >= 零成本結果。

**G5 解析度**（mistake.md 2026-09-04 / 09-06）：$/日 的估計量要用
**判決會用的那台機器**跑 bootstrap SE，不是公式手算。
**SE >= 門檻就是這個設計做不了決定**，要先重新設計。

#### 現在就要寫下來的三個未量測（免得事後變成「我早就知道」）

1. **我們掛進去會改變簿口，而這件事沒量。** 觀測到的 60 bps 半價差是
   **沒有我們在裡面**時的寬度。掛在現有頂檔＝排在隊伍後面但被打到時收
   全額；掛進去改善報價＝拿到優先權但收得少。**哪一種會發生取決於有沒有
   人跟我們競價，而我們沒有任何資料說這件事**（backtest-audit 第 16 項
   博弈動態）。這是推論不是量測 —— 照 mistake.md 2026-09-07 的規矩標明。
2. **固定成本沒進來。** 提款費／gas 按筆攤提，要有可成交量才算得出來。
3. **資金費沒進來。** 兩腿都是永續，持有過結算點就付／收資金費，
   而 backtest-audit 第 11 項警告它會污染時段效應。

#### 搬幣／AA 智能錢包的定位：**第三步不是第一步**

使用者提的 AA 搬幣是對的方向，但它買到的不是「更快抓到價差」：

* **這條線不靠搬幣。** 掛 Lighter、對沖 CEX，需要的是**兩邊各有抵押品**，
  不是每筆搬一次幣。搬幣是**補庫存**。
* **它不會縮短轉帳。** `small-trader-alpha-1` 量到的轉帳是 10–15 分鐘，
  瓶頸在鏈上確認與交易所提款處理；AA 改的是我們這側的簽章與編排。
* **CEX → CEX 那一段它完全不在路上**，而且反方向有雷：
  不少 CEX 不認合約錢包入帳、有些不給提到合約地址 —— **要逐所查**。
* **提款費是交易所定的**（他範例的主導固定成本是 Binance 的 1 USDT）。

**它確實買到兩樣**：(a) **週轉率** —— 搬得便宜又頻繁就能用更少的錢停在每個
場館，那是乘數，需要先有一個正的被乘數；(b) **金鑰面** —— session key 帶
花費上限與白名單，嚴格優於把 EOA 私鑰放進 bot 的 env，而這個 repo 的帳戶
被手動單爆過兩次。**(b) 是獨立於 alpha 的採用理由。**

#### ARC（Circle）：準備窗口，不是機會窗口

同一篇貼文：**ARC 主網目前不開**，只有測試網與 Circle faucet。
定位是穩定幣金融、EVM 相容、**USDC 當 gas**。
它自己的建議是「新鏈第一件事先準備跨鏈入口」—— **那正是 AA／LI.FI 該放的
位置，而且該在上線前做完**。

可以帶過去的四樣，我們三樣已有：行情採集 ✓、成本計算 ✓、雙邊執行 ✓、
**庫存管理 ✗**。所以 ARC 的準備工作與上面 (a) 是同一件事。

#### G1 跑完了（2026-09-13 同日，`research/mrp/lighter_flow_gate0.py`）

**G1 過了，但過的是分佈的錯誤那一端，而真正的獎品看不見。**

成交帶 680,386 筆、82 個幣、23.6 天、清算佔 0.3%。

**儀器範圍先講**：掛單在 lighter 且毛邊際 > 0 的是 **66 個 ticker**，
而成交帶裡只有 **9 個**，另外 **57 個不在帶子裡 = 未知，不是 0**。
原因：`lighter_tape.py` 的宇宙是 `TOP_N_DEFAULT = 80`（日成交額前 80 名），
而**寬價差住在長尾**。所以儀器的宇宙是這門生意宇宙的**補集**。

| ticker | 對沖 | 毛邊際 | 筆/天 | 流量$/天 | 做市帳戶 | 最大佔 | maker費 | 我方$/天 |
|---|---|---|---|---|---|---|---|---|
| PONS | okx | 4.79 | 77.8 | 15,969 | 44 | 34% | 0.28 | 3.83 |
| ANSEM | para | 5.89 | 231.4 | 11,834 | 17 | 21% | 0.32 | 3.49 |
| ORCL | bitget | 9.71 | 6.1 | 5,771 | 16 | 54% | 0.12 | 2.80 |
| ETHFI | okx | 2.66 | 53.1 | 14,399 | 32 | 30% | 0.20 | 1.91 |
| XPL | okx | 2.72 | 14.7 | 13,709 | 35 | 25% | 0.28 | 1.87 |
| WLFI / USELESS / MET / AERO | okx | 2.07 ~ 0.05 | 11–327 | 4.6k–11k | 14–26 | 18–35% | 0.19–0.32 | 0.68 ~ 0.02 |

* **G1：8 / 9 過門檻**（>= 10 筆/天）。合計 **$12.69 / 天**
  （份額 50% 的**假設**下，未扣逆選擇、未成交、固定成本、資金費）。
* **G3 集中度：最大 1 個佔 30%、前 5 個 93%、前 10 個 100%**
  -> **那是 5 個機會不是 8 個。**
* 可見的這 9 個毛邊際只有 **0.05–9.71 bps**，不是長尾的 20–60
  —— 因為帶子錄的是前 80 名＝流動的那些＝價差緊的那些。

#### 「未量測 1」變成量測了，而它不樂觀

§1.40 原本寫「我們掛進去會改變簿口，取決於有沒有人競價，而我們沒有資料」。
成交帶有 `ask_acct` / `bid_acct`，所以量得出來：

* **做市帳戶數中位 23 個**，最大一個佔做市量 31% -> **不是沒人掛的空簿口**。
* **現在的做市方實際付 0.280 bps，我們付 0.400 -> 我們貴 0.120 bps。**
  8 個裡有 1 個（AERO，毛邊際 0.05）**光這個費率劣勢就變負**。
  追平要質押 LIT，而 CLAUDE.md 已判那是**競爭均衡價不是優勢**。
* **吃單側零費率（Standard＝散戶）佔量中位 53%** —— 那是我們要報價對上的流，
  散戶流對逆選擇是**有利**的方向（但 G2 仍未量）。

#### 單位差 10 倍，被「數字不合理」抓到

第一版把 `maker_fee` 當 1e-5 換算，跑出「做市方付 **2.80** bps」——
而 2.80 是 Premium 的**吃單**費率，做市方不可能付它。查 distinct 值逐一
對上 CLAUDE.md 的費率表才定案：**單位是 1e-6，raw/100 = bps**
（28->0.28 質押 Premium、40->0.40 零質押、50->0.50 Plus、196->1.96、
280->2.80；**空白 = Standard = 0**）。理由寫在計分器原地。

#### 所以現在唯一該做的事（有時鐘）

**把 `lighter_tape.py` 的宇宙從前 80 名放寬到涵蓋長尾。**

* 這門生意的 66 個標的裡有 **57 個儀器看不見**，而 20–60 bps 的毛邊際
  全部在那 57 個裡面。
* **WS 串流不可回填** —— 每過一天就永久少一天（與 §鏈上錄製集同一條）。
* 成本微不足道：現在 82 個幣約 **6.6 MB/日**，放寬到 250 個幣約 20 MB/日，
  而 D 槽還有上百 GB。
* 接線已經都在（freshness、watchdog、manifest），只是 `--top` 一個旗標。

放寬之後要等約 7 天才有夠判 G1 的長尾樣本。**在那之前不要寫任何關於
「長尾能不能做」的結論** —— 現在手上的 9 個是流動端，它回答不了那個問題。

#### 下一步（只有一件）

放寬成交帶宇宙，然後 7 天後用同一支計分器重跑 G1。

> **⚠ 2026-09-13 同日補註（§1.41）：上面那句「只有一件」已經不是只有一件了，
> 而補上來的那一件比它便宜。**
>
> 本節通篇假設這條線要先等長尾樣本。查了執行層之後發現**下單路徑已經存在**
> （`arb/engine` 的 B3，`mode: maker` ＋ `maker_venue: hedge` ＝ 掛單在
> Lighter、對沖在 HL，零行新程式碼），所以「掛進去之後簿口長什麼樣」
> ——本節自己標成**未量測 1**、而且**沒有任何離線儀器量得到**的那一項——
> 現在就買得到。
>
> 同時要更正本節的一個隱含前提：**上表那 288 個配對裡，今天跑得動的只有
> 38 個**。對沖腿在 okx / binance / bitget / lighter-std 的 212 個
> **沒有場館模組**，`HEDGE_VENUES` 只有 `(lighter, lighter-rh, tradexyz)`。
> 所以「156 個 ticker」要讀成 **24 個對沖在 HL ＋ 13 個 xyz ＋ 4 個 lighter-rh**。
> 逐配對的表、金鑰相依、以及第 9 次 override 的草案在 **§1.41**。

#### 本節的計分器

* `research/mrp/venue_spread_arb.py` —— 逐場館半價差 ＋ 凍結比例（第一版，
  **對沖腿用場館全體中位，已知有缺陷**，保留當歷史紀錄）
* `research/mrp/spread_arb_hedge.py` —— 對沖腿掃描 ＋ para 逐 ticker 審查
  （同一個缺陷，它算出的 para +12.50 **不成立**）
* `research/mrp/spread_arb_paired.py` —— **判決用這支**：同一 ticker 兩條腿、
  帶深度與兩腿凍結比例


---

## flow_system 裡與這條線有關的其他節（2026-09-15 複製進來）

使用者：「如果任何地方有缺回flowsystem找」「再次檢查系統有沒有缺漏的地方」。
稽核（2026-09-15）列出這些節只存在 flow_system。**內容一字未改**，原文仍在 flow_system （那邊的 session 同時在用），節號沿用原編號，由新到舊。文中相對路徑指的是 flow_system。
§1.23（MFT 橫斷面 alpha）不屬於這條線，沒有複製。

### 1.39 【判決 NO-GO ＋儀器重大發現】多場館單標的 MRP：權重解出來沒有價值，而量它的過程抓到掃描器的腿差 53 秒（2026-09-13）

使用者：「量一下」。量了，而**量到的東西比要量的那個數字重要**。

#### 先撤回我自己在 §1.38 結尾寫的那句預期

我寫的是「同一標的跨場館的 PC1 佔比**預期遠低於 67–81%**，因為市場因子被
建構移掉」。**方向完全相反。** 同一個標的在 N 個場館的報酬幾乎一模一樣，
所以 PC1 是 **99%+**（實測中位 **0.9911**），殘差（那不到 1%）才是套利訊號。

所以 PC1 從判準降格成**自曝檢查**：它低就代表我把不同的東西當成同一個標的。
真正的判準是 Gate 0（核心原則 11）：**振幅 ÷ 該場館的來回成本**。

#### 判決：**NO-GO。解出來的 N 腿權重，對寫死的 (+1,−1) 沒有增量**

計分器 `research/mrp/mrp_crossvenue.py`。同步面板（見下）352 個 ticker、
各 3 個場館、13.6 天、**前半挑權重 / 後半算績效**（兩臂同一個程序 ——
第一版我讓基準用全樣本挑最好的配對，那是讓基準偷看答案）：

| 樣本外（後半） | 值 |
|---|---|
| MRP（零和）扣費後贏最好兩腿 | **142 / 352（40%）** |
| 純看均值回歸品質（portmanteau，不扣費）贏 | 186 / 352（53%）|
| MRP 打敗**隨機零和權重** | 172 / 352（49%）|
| 扣費後淨值為正 | 兩腿 **2 / 352**｜MRP **1 / 352** |
| **三關全過**（淨值>0 ∧ band > 雜訊底 2 倍 ∧ 半衰期在解析度內）| **0 個** |

（樣本內 portmanteau 中位 0.2183 vs 樣本外 0.2604，比值 **1.19**。）

**機制不神秘：第三條腿要付錢，而它沒有買到任何東西。** 三腿的來回費用
11.5–16 bps，而誠實的跨場館 band 是 7–25 bps -> band/2 = 3.5–12.5 bps
**小於成本**。綁束在**場館的費率**，不在權重怎麼解。

連帶：MRP 這一族（§1.38 不同幣的橫斷面 ＋ §1.39 同幣跨場館）**兩端都結案**。

#### 我自己的解釋被自己的檢查否證（這次第三個）

第一版表上 MRP 的 band 反而**比最好兩腿大**（ONE 123 vs 109、FLNC 38 vs 12），
我的假設是「GEP 沒有約束 Σw = 0，所以價差裡混著裸方向曝險」。**量完是錯的**：
Σw/Σ|w| 中位 **−0.000**，352 列只有 **1 列**淨曝險超過 0.10 —— 白化矩陣
`rho_inv_sqrt` 會把共同因子那個方向壓掉，零和是 GEP 的自然解。

真正的解釋：GEP 最小化的是**正規化後**的自相關（變異數已經被除掉），
所以它可以挑一個變異數更大、正規化可預測性更小的組合。**band 大不是作弊，
是目標函數本來就不管 band** —— 而那正好說明為什麼它在扣費之後沒有用。

（§1.38 兩個假設被否證、這裡第三個。零和投影仍然算並當判決用，
因為「幾乎零和」不等於「零和」。）

---

## 儀器：掃描器的兩條腿不是同時抓的，lighter 家族晚 53 秒

這一節是本節的主要產出。**C1 一開始沒過**（BTC 的 PC1 只有 0.8596，而 BTC
是控制配對），照規矩先查儀器 —— BTC 的跨場館相關矩陣分成兩塊：

    HL / binance / bitget / okx   彼此 0.953 – 0.996
    lighter / lighter-rh          彼此 0.997，**但跟上面那塊只有 0.66 – 0.71**

根因在 `arb/engine/tools/scanner.py`：`scan_once` 把 `ts = int(time.time())`
在**週期開頭取一次**，然後 `quote_all` 分**三段序列**抓，而整個週期的每一列
共用那一個 ts：

    1. kind=="hl"   ThreadPoolExecutor(4)   HL / xyz / para / mkts / io
    2. CEX          ThreadPoolExecutor(4)   okx / bitget / binance
    3. **lighter    普通 for 迴圈，每筆 time.sleep(0.06)**   <- 排最後且不併行

實測偏移（落後相關，以 binance 報酬為基準，格寬 180 秒）**逐項對上那三段**：

| 場館 | 抓取段 | ρ | 推估偏移 |
|---|---|---|---|
| okx / bitget | cex | 0.996 / 0.995 | **1 秒** |
| HL | hl | 0.957 | **8 秒** |
| **lighter / lighter-rh** | lighter | **0.703 / 0.701** | **53 / 54 秒** |

#### 它注入多少假價差 —— 預測先寫，再驗

偏移 δ 在格寬 T 之下注入約 σ√(δ/T) 的價差離散度。BTC 每 180 秒報酬
σ = **8.0 bps** -> 53 秒偏移注入 **4.3 bps**。

| 量 | 值 |
|---|---|
| 預測（8.0 × √(53/180)） | 4.3 bps |
| **實測 lighter−binance 價差 std** | **4.7 bps** |
| 對照 HL−binance（相距 8 秒） | 1.9（預測 1.7）|
| 對照 lighter−lighter-rh（同段） | **0.8** |

#### 跨儀器對照（mistake.md 2026-08-26：兩台儀器不同意就是不准解讀）

錄製器走**兩條常駐 WebSocket**（`engine/entropy_arb/feeds.py`，雙邊簿口
在記憶體裡、同一瞬間讀兩本），所以它是同步的。同一個配對：

| 配對 | 錄製器（WS，同步） | 掃描器（REST，腿差 45 秒） | 倍數 |
|---|---|---|---|
| BTC（HL / lighter-rh） | **0.985 bps** | **4.831** | **4.9x** |
| HYPE（HL / lighter） | **2.214** | **8.759** | **4.0x** |
| BTC binance/okx（同段、1 秒）| — | **0.712** | 掃描器同步時自己的底 |

#### 損害範圍（查了，比我一開始怕的窄，但是活的）

- **§0.75 的判決不受影響。** 它的證據是錄製家族的 `minutes.csv`，走 WS。
- **§0.75b 的升格排名受影響**：`scan_rank.py` 的 band 來自這些 REST 報價。
  凡是**跨抓取段**的配對，band 被 σ√(δ/T) 撐大 —— 而 lighter 腿撐最多。
- **目前升格的三個配對沒有一個有 lighter 腿**
  （GOLD@bitget-binance、GOLD@okx-binance、QNT@xyz-binance；
  bitget/okx/binance 同一個執行緒池、相距 1 秒）。**所以還沒有一個升格
  決定被污染** —— 它是地雷不是損失。
- BTC 控制帶 `control_band_bps = 7.527` **自己也被撐大了**（誠實的同步值是
  **1.138**），但那是**保守方向**：雜訊底越高，被判「與價差分不開」的配對越多。
  所以它沒放壞東西進來。

**而最危險的方向正好是我們最想要的那個**：lighter 是費率最低的場館
（Standard 吃單 0 bps，§1.34），所以**最該被升格的那一批，band 被撐大最多**。

#### 沒有免費的修法（查過了）

Lighter 公開 REST **沒有批量頂檔端點**：`orderBookDetails`（一次 234 個市場）
只有 mark / index / last_trade，`orderBooks` 只有市場參數，頂檔要逐個
`orderBookOrders`。所以把那 139 次序列呼叫收成一次是做不到的。

兩個真的選項，**都要在 arb 的 session 決定**：
1. **把 lighter 那段改成 ThreadPoolExecutor**（139 / 4 x 0.25s ≈ 9 秒，
   而且三段可以併行）。風險：那個序列 + sleep 是刻意的（"be polite to
   Lighter's public REST"），而被限流時 REST 回 text/html、`_safe` 回 None
   = 安靜少列。**這是在動一個活著的錄製器，不該我一個人決定。**
2. **讓偏移在決策點上看得見**：每條腿各戳自己的抓取時間（純新增欄位），
   `scan_rank` 在 band 旁邊印出該配對的腿差與預期撐大量。
   **報表修正不是閘門鬆綁**（同 mistake.md 2026-09-03 的分類）。

#### 本節的計分器與對照

- `research/mrp/mrp_crossvenue.py` —— 判決。`FETCH_GROUPS` 把 `quote_all`
  的三段寫下來，`--sync-only`（**預設開**）只取同一段內的場館；
  `--no-sync-only` 保留污染版當對照。七道自曝檢查 C1–C7 全格印出。
- 污染版對照（混段）的結論**和乾淨版一樣**：MRP 贏 150/310（48%）、
  淨值為正 17 vs 17。**這讓 NO-GO 更強而不是更弱** —— 污染注入的正好是
  「一格內完成的大振幅均值回歸」，也就是 MRP 最想找的東西，而它**還是只打平**。

#### 還開著的

- **Lighter 面板量不到**：它的腿晚 53 秒，所以「Lighter 上的跨場館基差」
  用掃描器資料**無法量**。而 Lighter Standard 吃單 0 bps 是唯一一個成本
  低於已量到 band 的場館 —— 也就是說**唯一可能翻盤的面板，正好是儀器
  看不見的那一個**。它的解鎖條件是上面那兩個選項之一，不是新的統計方法。
- 這支的 `band` 用**中價**算，所以它是**上界**：沒扣買賣價差。


### 1.37 【構造換人＋預註冊】跨場館 lead-lag：位置與量級量到了，**方向被儀器擋住**（2026-09-13）

使用者：「就是跨場館 lead-lag，而它跟資金費那篇的 Part 3b『Volume & Lead-Lag』
與 3e『Global Lead Lag』是同一個主題，去讀這兩篇」。讀了，而**3e 直接回答了
同一天前面那個「定案定義，這確定是正確的嗎」**。

---

#### A. 3e 說：完全最佳化之後，這個交易沒有「帶」也沒有「均值」

原文：「this trade becomes a lead-lag trade **because you stop holding risk and
start marking each leg as its own separate trade**. You effectively need to model
the expected share of the distance each leg will converge to, and then you only
take the leg if it is over some threshold (some minimum profit + cost to trade).」

3d「Mean-Estimate Free Reversion」講同一件事更白：「**there is no mean for our
prices to revert to**」—— 取而代之是回歸，用其他場館的指數價／標記價／中價
當特徵，預測**我們這條腿**的移動。

**所以 §1.36 那三個帶定義的爭論，是 Part 1 基礎版內部的爭論。** 分三層：

| | |
|---|---|
| **已查證** | `band.py` 逐值重現 `analyze.py`（GOLD_LL 3/3）。「三個變一個、而且是兩個下游都聲稱在照的那一個」—— 這個意義上定案是對的。 |
| **未建立** | 原始定義是不是**交易決策**的對的定義。`analyze.py` 本來是建議 config 門檻用的，`fires` 數分鐘不數交易那個病還在。 |
| **3e 新增** | **帶本身是錯的物件。** 把帶的定義磨得更好，天花板很低。 |

§1.36 的判決（midline 砍掉 GOLD_LL 56% 毛利 -> 零費率也沒有配對存活）
**仍然成立** —— 它在帶的框架內是正確的更正，同時說明那個框架到底了。

#### B. 成本結構被換掉，而且往好的方向

單腿之後：費用事件 4 次 -> **2 次**；**§1.35 剛加的第 8 項（incomplete
spread）整個消失** —— 沒有對沖腿可以錯過。代價是開始持有**單腿方向風險**
（作者說的 "stop holding risk" 指的是基差風險，不是方向風險，這點要講清楚）。

連帶：**§1.34 把 MFT 排第一、套利排第三，那是在排同一個東西的兩個視角。**
跨場館 lead-lag 就是 MFT，而套利線錄的兩腿中價本來就是它的特徵集。

#### C. 實測（09-12，唯一有量的重疊日）

Lighter 帶 96,826 筆／HL 帶 1,130,661 筆／**39 個共同標的**／共同時窗 16.3 小時。

**位置：切成 1 秒一格時 9/9 峰值落在 lag 0 -> 效應住在 1 秒以下。** 下到 250 ms：

| | BTC | ETH | SOL | LIT | PUMP | HYPE | ZEC | NEAR |
|---|---|---|---|---|---|---|---|---|
| 峰值相關 | 0.182 | 0.110 | **0.296** | 0.139 | 0.124 | 0.022 | 0.038 | 0.041 |
| 峰值 lag | +250 | +250 | +250 | −250 | −250 | +250 | 0 | 0 |

**量級是真的**（相關不依賴誰領先）。**方向不是。**

#### D. 方向為什麼不可判定 —— 兩台獨立儀器，而人造量大於效應量

兩邊的 `ts` 都是**交易所自己戳的**（Lighter `timestamp` / HL `time`），
沒有共同時鐘。兩種算法估那個偏移：

| 儀器 | 樣本 | Lighter | HL | 偏移（HL − Lighter） |
|---|---|---|---|---|
| 中價錄製器的 `stale_ms`（p1） | 09-12 全日、135k 列 | 18 ms | 389 ms | **+371 ms** |
| 逐筆帶的新欄 `rx_ms`（p1） | 重啟後 4 分鐘、9.4k 列 | **−5 ms** | +163 ms | **+168 ms** |

**數值不同但同號同量級，而效應本身只有 100–270 ms。**
`rx_ms` 那一列的 **Lighter min = −7 ms（負的）** 是最乾淨的證據：
交易所時戳比我們收到的時刻還晚，物理上只有「兩個時鐘不可互比」一種解釋。

**所以上一輪那個「Lighter 領先 HL 200ms」不可報。** 修正之後方向可能反過來。

#### E. 文章 3b 的成交額那一關也量不了（儀器不足，不是主張被證偽）

250 ms 一格下**有三分之二的時間格至少一邊沒成交** -> 報酬恆為 0 -> 三分位退化成
`nan`。照 mistake.md 2026-08-02：有格為 0 一律先當儀器壞掉，不得解讀。
正解是**量中價不量成交價**（中價不需要有成交就會更新），而中價錄製器是
5 秒（Lighter）／60 秒（HL）。

#### F. 回答使用者「商品會不會掃得還不夠多」—— 不是廣度，是解析度

掃描器實際覆蓋：**3,124 配對／1,445 ticker／10 場館／13.1 天／12.7M 列**，
3,114 / 3,124 過 500 報價門檻。**廣度夠。**

但它**每 180 秒取樣一次**，而效應住在 1 秒以下 —— 差 **180,000 倍**。
同一個病在整條線上：180 秒（掃描器）／60 秒（HL 中價）／5 秒（Lighter 中價）。
**而錄製解析度不可回填**（mistake.md 2026-09-11）。

#### G. 已做：兩個逐筆帶各加一欄 `rx_ms`

= **我們收到那則訊息的本機時刻**（每則訊息取一次）。它同時解兩件事：
去掉跨所時鐘污染，而且照 3c「the message that is received first is the most
accurate price, and you tend to get incompletes on the leg that the message for
the price was received last」—— **收到的時刻本來就是決策該用的那個時鐘**。

動 schema 之前枚舉了 5 個讀者（`flow_toxicity` / `hl_verify` / `mm_markout` /
`fig_ledger` / `data_manifest`），全部用欄名取值（`hl_verify` 已有
`if "mark_snap" in d.columns` 的先例）-> 加欄安全。
**等旗標 mtime 一變（剛落完盤）才殺**，損失秒級不是 5 分鐘級；
重啟走看門狗那條路徑（被測的就是被部署的）。
`lighter_tape 32180 -> 31988`、`hl_tape 10512 -> 24920`，兩邊
`ok=True「啟動中（已訂閱，還沒有成交）」`。落地驗收見上表。

#### H. 預註冊（**寫在資料累積之前**，核心原則 8）

資料：`rx_ms` 從 2026-09-13 00:44 UTC 起累積。判決日 **2026-09-20**（7 天）。

| | 假說（文章原文的符號） | 判準 |
|---|---|---|
| **L1** | 在**我們自己的時鐘**上，39 個共同標的仍有非零 lead-lag | 峰值相關 CI 下緣 > 0，且峰值 lag 不在 0（至少 6/9 大標的同號）|
| **L2** | 方向：低費率場館領先（「Exchanges with lower fees tend to lead those with higher fees」）| 符號一致 ≥ 6/9，且**逐標的符號與成交額排序無關**（否則是量在主導）|
| **L3** | 相關強度隨成交額**單調上升**（「large orders -> meet in the middle；small orders -> only one exchange move」）| 三分位單調 ≥ 6/9，且每個三分位兩邊都有成交的時間格 ≥ 70%（否則判**儀器不足**不判 FAIL）|
| **L4** | **領先方**的意外成交額 z 上升 -> 領導性**加強**；**落後方**的 z 上升 -> **減弱** | 兩個方向的符號都對 ≥ 6/9。這一關有符號所以可被證偽 |
| **L5** | 3c：我們**最後**收到的那條腿就是會 miss 的那條 | 用 `rx_ms` 逐筆配對，最後到的那側的後續逆行 > 先到側（CI 下緣 > 0）|

**L1 不過 -> 整條線停，不做 L2–L5。**（Gate 0 排在資訊層之前，核心原則 11。）

**刻意寫下的反向證明**：L2 若在「Lighter 領先」這個方向成立，**可交易的腿
就在 HL** —— 付 HL 的費率，**不是** §1.35 贏到的那個 Lighter Standard 零費率。
所以 **L2 成立反而部分抵銷 §1.35 的勝利**，這件事要在投工程之前知道，
不可以在看到結果之後才想起來。

#### H2. 計分器已註冊，而且証明過它能從 0 變成 1

`research/leadlag/xvenue_leadlag.py`。預設 **strict（只用 `rx_ms`）**，
`--allow-exchange-ts` 會跟但會印三行警告並在標題標
「已污染，方向不可引用」。

照 mistake.md 2026-09-08（註冊任何一條會累積的時鐘，
**當天就要証明它能從 0 變成 1**）：

* strict 模式跑 09-13 的 `rx_ms` 資料（共同時窗 **0.083 小時**）
  -> 它**正確地拒絕下判決**：「沒有任何標的湊到 600 個兩邊都有
  成交的桶 —— 樣本還不夠，這不是判決」。
* 污染模式跑 09-12 整日（16.74 小時、241,062 個時間格）
  -> **產出 3 個帶 CI 的 L1 讀數**，錬子是通的：

  | | both_cells | peak_lag | peak_c | CI |
  |---|---|---|---|---|
  | BTC | 1,899 | 250 ms | 0.170 | [0.100, 0.239] |
  | ETH | 962 | 250 ms | 0.110 | [0.070, 0.159] |
  | HYPE | 676 | 250 ms | 0.023 | [0.010, 0.075] |

  **這三個的 CI 下緞都 > 0、lag 都不是 0 ——
  所以 L1 在污染的時鐘上會過。方向仍然不可引用。**

#### H3. 而這一跑順手量出一件比我估的更強的事

**逐筆成交帶在這一題上是結構性不夠的，不是「再等幾天就好」。**

16.74 小時 = **241,062 個 250 ms 一格**，而 BTC（兩邊成交最多的那個）
只有 **1,899 個時間格兩邊都有成交 = 0.8%**。39 個共同標的裡面
**只有 3 個**湊得到 600 個時間格。

L3 可行性關的原始數字（要 >= 70%）：

| | 低三分位 | 中三分位 | 高三分位 |
|---|---|---|---|
| BTC | 0.0% | NaN | **4.9%** |
| ETH | 0.0% | NaN | 3.2% |
| HYPE | 0.0% | NaN | 1.6% |

**差 70% 門檳差了十幾倍，而不是差一點。**
所以 L3 / L4（文章 3b 的成交額主張）**用逐筆帶永遠答不了**，
不管累積幾天 —— 因為「兩邊同時有成交」本身就是罕事件。
**只有簿口答得了**（中價不需要有成交就會更新）。

這把下面那個決定從「要不要更多資料」改成
**「這兩個假說有沒有任何路徑可以被回答」**。

#### I. 還沒做的決定（要使用者拍）

共同宇宙 39 個標的要不要開**次秒級的簿口頂檔事件錄製**（事件驅動不是取樣
—— WS 本來就逐筆推，在薄標的上比 4 Hz 取樣更小也更準）。估 1–2 GB/日。

沒有它：L3 / L4 永遠只能拿退化的三分位回答，因為成交帶在 250 ms 上太稀疏。
有它：L1–L5 全部可判，而且它同時是倉位型做市（§1.34 第 2 名）的輸入。
**兩條線的擋路石仍然是同一份資料。**


### 1.36 【定案＋判決】家族的「帶」只有一個定義，而它把 §0.75 最後一個存活的配對也判掉了（2026-09-13）

使用者：「好」（指定案帶的定義）。**定案了，而且它不是在三個之中選一個，是回到原本那一個。**

#### 三個定義，而兩個的 docstring 都聲稱照第三個

| | `analyze.py`（原主人） | `premium_verdict`（判決） | `scan_rank`（升格） |
|---|---|---|---|
| p90 的母體 | 全部列 | 全部列 ✅ | **只取正值** ✗ |
| **扣 midline（溢價中位數）** | **是** | **否** ✗ | **否** ✗ |
| 扣手續費 | 是 | 否（另外算）| 否（另外算）|
| 百分位 | 線性內插 | `v[int(0.9*n)]` ✗ | pandas（線性）✅ |
| 四捨五入到 0.5 / 下限 1.0 | 是 / 是 | 否 / 是 | 否 / 否 |

`engine/tools/analyze.py` L129 的註解逐字寫著
「**the band that fired in ~10% of minutes**」——所以「帶」的語意是
「**有多少比例的時間**超過它」，而只取正值回答的是另一個問題
（多少比例的**機會**），而那個問題的分母隨配對變動，**兩個配對的「10%」不可比**。

#### 定案的實作：`arb/arblib/band.py`（單一定義，不再各寫一份）

**已知答案對照**：在同一份輸入（`engine/logs/GOLD_LL/minutes.csv`，589 列）上
逐值重現 `analyze.py` 自己的輸出 —— midline **2.3**、upper **1.5**、lower **1.0**，
**三個全中**。所以這不是我的判斷，是原主人的算術。

舊的兩種寫法保留為 `band_bps_legacy_positive_only` /
`band_bps_legacy_all_rows`，**只為了並列報告**：`scan_rank` 的升格指標
凍結於 2026-08-30、`premium_verdict` 的閘門也已經做過判決，
**不可以靜默換掉一個凍結的判準**（「判準事前寫死、事後不放寬」）。

#### 全歷史逐配對（含所有輪替檔，與 premium_verdict 同口徑）

| 配對 | midline | 賣：判決 / 升格 / **定案** | 買：判決 / 升格 / **定案** | 毛利變化 |
|---|---|---|---|---|
| SNDK | −2.6 | 4.13 / 8.82 / **6.50** | 10.27 / 11.08 / **7.50** | −27% |
| NBIS | +11.6 | 26.39 / 27.80 / **15.00** | 8.85 / 14.94 / **20.50** | −22% |
| ANTH | +15.5 | 420.6 / 467.9 / **405.0** | 5.15 / 44.47 / **20.50** | −4% |
| BTC | +1.0 | 5.52 / 5.70 / **4.50** | 2.11 / 2.76 / **3.00** | −18% |
| HYPE | +1.5 | 8.69 / 9.23 / **7.00** | 5.44 / 7.30 / **7.00** | −19% |
| ZEC | +1.0 | 11.15 / 12.85 / **10.00** | 9.07 / 11.24 / **10.00** | −10% |
| NEAR | +3.6 | 10.66 / 11.51 / **7.00** | 4.78 / 9.40 / **8.50** | −20% |
| **GOLD_LL** | **+5.4** | 7.90 / 7.90 / **2.50** | 1.00 / 0.89 / **3.50** | **−56%** |
| NVDA_LL | −0.6 | 1.00 / 2.37 / **1.50** | 2.96 / 3.70 / **2.50** | −15% |

#### 判決：零費率也救不回來

§1.35 算出「Standard（0/0）＋ 混合掛單吃單」之下，全扣完只有
**GOLD_LL 淨 +1.2** 還為正。而定案定義讓它的毛利從 3.95 掉到 **1.75**
（−56%，因為它的中位溢價 +5.42 bps 被算成了可捕獲空間）。
對上它的總成本 2.70 ->

    GOLD_LL   淨 +1.2  ->  **1.75 − 2.70 = −0.95，翻負**

**所以在這個家族自己的原始帶定義之下，即使費率為零，沒有一個配對存活。**

而 GOLD_LL 是 `lighter` vs `lighter-rh` —— 它那 +5.42 bps 的中位溢價
**很可能就是 USDG／USDC 的穩定幣基差**。所以 **midline 修正與 TODO §1.21
（扣穩定幣基差）是同一個修正，從兩個方向走到同一處**，而 midline 是超集：
`small-trader-alpha-6` 說跨場館常態差異有三個來源（穩定幣基差、結構差異、
合約差異），**扣中位數一次吸收掉三個**。

**§1.21 因此不必再單獨做**（它會重新推導 midline 的一個子集），
但它的那句判斷要保留：**不扣它，一個常態偏移會被記成可捕獲的空間** ——
而 GOLD_LL 就是那個實例，代價是一個假的「唯一存活者」。

#### 我在過程中被自己的機制說法騙了一次，撤回

我先推測「midline 會吸收掉 ANTH 那個持續 +400 bps 的楔子」。**不成立**：
ANTH 的中位溢價只有 **+15.49**，扣掉它賣帶 420.56 只降到 405.00（−4%）。
所以 **ANTH 不是「持續偏移」**，是「中位在 +15 附近、但約 14% 的時間進入
平均 93 分鐘的大幅脫勾 episode」。§1.32 裡我寫的「持續偏移」措辭要改成
「episodic 脫勾」——兩件事不同，而我上一輪混在一起了。

#### 還沒做的

1. **採不採用**是一個要寫下來的決定。採用＝§0.75 的閘門與升格指標都要
   重新跑一次，而它們都已經做過判決。不採用＝兩個路徑繼續用兩個定義。
   **我沒有自行替換**，`band.py` 目前只是定義 ＋ 並列報告的能力。
2. `fires` 的單位在三個定義下**都數分鐘不是交易**（mistake.md 2026-09-03
   修了下游報表，判決與升格兩條路徑都還沒修）。


### 1.35 【缺陷＋重跑】同一條線有**兩個「帶」定義**，而兩個都在做決定；等級感知重跑後綁束換成「每年筆數」（2026-09-13）

使用者：「開始做」（指兩件零新資料的事：成本模型改等級感知、§1.21 扣穩定幣基差）。
第一件做完了，第二件**卡在一個更上游的缺陷**，而那個缺陷是這一節的主體。

---

#### A. 兩個「帶」定義（這是缺陷，不是口味問題）

    premium_verdict.side_stats（**判決**用）
        vals = sorted(x[key] for x in rows)     <- 全部分鐘，含負值與零
        p90  = vals[int(0.9 * len(vals))]

    scan_rank.side_metric（**升格**用）＋ arb_stable_basis
        pos  = v[v > 0]                          <- 只取正的
        band = pos.quantile(0.9)

在重尾配對上差很多（ANTH，截到凍結 json 自己的 `asof_utc`、18,422 分鐘）：

| ANTH | 全部分鐘 p90（判決） | 只取正值 p90（升格） | 差 |
|---|---|---|---|
| 賣側 | **422.097** | **467.942** | +10.9% |
| 買側 | **4.645** | **40.849** | **8.8 倍** |

**兩個定義回答不同的問題**：全部分鐘的 p90 是「有多少比例的**時間**超過這個帶」；
只取正值的 p90 是「有多少比例的**機會**超過這個帶」。
兩者都可以是對的 —— **錯的是同一個家族裡兩個決定路徑各用一個，而沒有任何地方
寫下它們不同。**

而判決那個定義還帶一個算術恆等式的後果：`fires = 超過 band 的分鐘數`，
配上「全部分鐘的 p90」-> **依定義恰好是 10% 的分鐘**。
那正是 mistake.md 2026-09-03 抓過並修了下游報表的病，
**在判決路徑上還活著**（升格路徑也活著，見 §1.32）。

**§1.21（扣穩定幣基差）因此還不能做。** 它的 D1 要求「重算未修正的 band 必須
重現凍結值」，而它用的是升格那個定義、凍結值是判決那個定義 —— 永遠對不上。
那支腳本**正確地拒絕解讀**（「D1 未過 —— 我接錯了口徑，以下不解讀」），
自曝關做了它該做的事。

**動作（未做，要先定案哪個定義是對的）**：
1. 決定家族的單一「帶」定義，寫在**一個**函式裡，兩條路徑都呼叫它
   （不是「調成差不多」—— mistake.md 2026-09-07）。
2. 定案之後 §1.21 才有可對照的已知答案。
3. 順手補 `fires` 的單位：兩個定義下它都數**分鐘**不是**交易**。

#### 過程中被自己的診斷騙了一次，值得記

第一版診斷我拿 `sell_edge_max_bps` 去查 `premium_verdict.load` 的輸出，
得到「18,422 列裡 0 個非空」，差一步就寫成「判決那側讀不到資料」。
**那是我的儀器錯**：`load()` 在 L117 把欄位**改名**成 `sell_max`。
用對鍵名之後才看到真正的差異。
（自己剛寫的診斷比別人的舊儀器更危險，mistake.md 2026-07-29 第 N 次。）

順帶查證了一件有用的：**兩個讀取端的列數完全一致**（全部 18,480、截到 asof
18,422），所以輪替檔的處理沒問題。但同時發現 **ANTH 的 `minutes.csv` 欄位數
是 25 → 29 → 41 → 55 四種 schema**（兩次輪替發生在 2026-09-12 當天一小時內）。
兩個讀取端目前都靠 pandas 按欄名對齊而沒出事，但**沒有任何人枚舉過讀者**
—— 那是 mistake.md 2026-08-29 的形狀，只是這次還沒咬人。

---

#### B. 成本模型等級感知（做完了）

`arblib/fees.py` 加 `lighter-std` / `lighter-rh-std`（0/0，**收據查證**：
缺欄位＝Standard，74.9% 的吃單成交額走這一級，見 §1.33 更正版）。
`cost_model.py` 加 `--lighter-tier {premium,standard}`，**只在費率那一次呼叫裡
映射場館鍵**（改鍵名會讓第 3 項的 `margin_frac` 查不到而落到預設 0.2，
於是「換費率等級」會悄悄改掉資金成本）。

**已知答案對照**：`--lighter-tier premium` 的第 1 項逐配對是 12.3 / 14.6 / 11.2，
與改動前逐位元相同 -> 行為不變。

重跑結果（$200/腿）：

| 情境 | 第 1 項（費用） | **只扣費為正的配對** | 全扣完為正 |
|---|---|---|---|
| Premium ＋ 四次吃單（原假設） | 12.3 / 14.6 / 11.2 | 2 / 9 | 1（ANTH，異常值）|
| Standard ＋ 四次吃單 | 6.8 / 9.0 / **0.0** | 3 / 9 | ＋GOLD_LL **+1.2** |
| Standard ＋ 一腿掛單一腿吃單 | 2.2 / 3.0 / **0.0** | **7 / 9** | 同上 |

**綁束換人了。** 混合模式＋Standard 之下，只扣費有七個配對為正
（NBIS +10.9、GOLD_LL +4.0、SNDK +2.9、ZEC +2.6、NEAR +2.3、NVDA_LL +1.5、
HYPE +1.3），但全扣完仍只有 GOLD_LL 勉強為正，而**壓住它們的是第 7 項
（尾部風險），每筆 6 到 45 bps**。那一項的算式是
`年化拖累 x 鎖住的資金 ÷ 每年筆數` —— **所以綁束回到「每年筆數」**，
正是 §1.20 在費率 bug 之前的結論。

#### C. 新增第 8 項：incomplete spread（未量，**刻意不計入合計**）

來源 `small-trader-alpha-6`（2024-07-09）：作者說簡單版的跨場館永續套利
「don't work so well in the modern market」，混合掛單/吃單是
「practically a gimme」，而他點名的頭號工作是 **incomplete spread** ——
掛單腿成交了、送對沖單時價格不在了，只剩一條腿，要嘛認賠要嘛多付。
「Incomplete resolution is one area you'll spend time working on.」

**我們七個成本項裡沒有這一項。** 現在有了，而且**回報 `None` 不是 `0.0`**：
0.0 與「這個成本不存在」在畫面上無法區分，那正是 mistake.md 2026-09-09
記的那個病（欄位的預設值等於沒被寫的結果）。
`量到%` 因此從 29% 掉到 25% —— 誠實記帳生效了。

**要量它需要**：P(對沖價還在 | 我們成交) x 放棄邊際，逐配對。
而命中機率是延遲的函數，我們剛好量到自己的往返是 50 ms（§1.31）。

**在它被量出來之前，混合模式的任何淨值都不可引用** —— 那是該構造的頭號成本。


### 1.33 【量測】逐筆成交帶上的**實付費率** —— 收據查證第一次做得到，而**沒有人付 0**（2026-09-12）

**這一節關掉一個我們寫過三次「做不到」的東西。** `arb/arblib/fee_receipts.py`
的存在理由是「用真實成交回推實付費率」，而它一直卡在同一句話：
**收據查證要自己的帳戶成交，我們沒有**（CLAUDE.md §HFT 的 (c) 那一條）。

**Lighter 的公開成交帶上，每一筆都帶雙方的實付費率。** 所以不需要我們自己成交
—— 我們可以量**整個市場**的費率分布。

#### 單位是量出來的不是猜的

`maker_fee: 28` 這種整數，單位是 **1e-6 的費率**。證據是清算單：
它的 `taker_fee` 是 **10000**，而 `/api/v1/orderBooks` 的 `liquidation_fee`
是 **"1.0000"**（= 1%）。`10000 / 1e6 = 0.01 = 1%` -> 對上。
反過來驗：一般成交的相異值換成 bps 是

    [0.28, 0.30, 0.32, 0.34, 0.36, 0.38, 0.39, 0.40, 0.50]

**一格一格對上官方費率表**（Premium 質押折扣 0% / 2.5% / 5% / 10% / 15% /
20% / 25% / 30%，加上 Plus 的雙邊 0.50）。九個值、九個對應，沒有一個落在表外。
**這種對法比任何論證都硬** —— 單位錯不可能讓九個值同時落在文件的九個格子上。

#### 分布（92 秒驗收樣本，4,676 筆一般成交、名目 $2.88M）

| maker bps | 筆數佔 | **成交額佔** | 官方對應 |
|---|---|---|---|
| **0.28** | 63.6% | **69.9%** | Premium 質押 500k LIT（−30%）|
| 0.32 | 15.4% | 16.5% | Premium 質押 100k（−20%）|
| 0.39 | 4.3% | 8.2% | Premium 質押 1k（−2.5%）|
| 0.36 | 3.1% | 1.8% | Premium 質押 10k（−10%）|
| **0.40（我們）** | 6.0% | **1.0%** | Premium 零質押 |
| 0.38 | 3.9% | 0.8% | Premium 質押 3k（−5%）|
| 0.34 | 0.7% | 0.8% | Premium 質押 30k（−15%）|
| 0.30 | 1.1% | 0.7% | Premium 質押 300k（−25%）|
| 0.50 | 1.8% | 0.2% | Plus（雙邊 0.5）|

**`maker_fee == 0` 的筆數：0 / 4,676。** 只看最新那一天（4,187 筆、
排除舊重播）再驗一次：**還是 0**。

    成交額加權平均 maker 費率   **0.298 ~ 0.300 bps**
    比我們（0.40）便宜的流量    **98.7% 的成交額**
    跟我們同價                  1.0%
    比我們貴                    0.2%

**我們在最貴的 1.2% 裡。**

#### 這推翻與確立了什麼

- **確立**：同日 `arblib/fees.py` 從 `0/0` 改成 PREMIUM `0.40/2.80` 是對的，
  而且現在是**收據查證**不是文件查證。CLAUDE.md §HFT 的 (c) 那一條可以關掉。
- **推翻**：「Lighter 是零費率場館」這個說法在**任何等級上都不成立** ——
  不只是我們不在 Standard，而是**整個成交帶上沒有一筆是 0**。
  Standard 的 0/0 存在於文件裡，但**做市的流量全部不在那一級**
  （原因見 §1.31：Standard 的 60 req/min 做不了做市）。
- **成本槓桿的量化**：質押 LIT 可以把我們從 0.40 帶到 0.28，
  **值 0.12 bps**。但 0.28 是 **69.9% 成交額所在的位置** ——
  **那是競爭均衡價不是優勢**。到了 0.28 只是跟七成的對手平手。

#### ⚠ 上面「沒有人付 0」是錯的 —— 零是用「不寫欄位」表示的（同日稍晚更正）

**使用者：「Lighter 如果用 standard 版本就是零費率不過延遲會變高」。** 去驗，翻轉。

我數的是 `maker_fee == 0`，而**缺值才是零**。決定性的測試是
「同一個帳戶會不會有時有欄位、有時沒有」（樣本擴到 53,784 筆一般成交）：

| | 帳戶數（>=5 筆） | 全部有欄位 | 全部沒有 | **混合** |
|---|---|---|---|---|
| 吃單方 | 1,545 | 905 | 638 | **2** |
| 掛單方 | 364 | 268 | 96 | **0** |

**0–2 個混合 = 帳戶屬性，不是資料缺漏。** 而決定性的佐證是：
**有欄位的那群裡沒有任何一筆是 `0.000`** —— 0.50（Plus）與
1.96 / 2.24 / 2.38 / 2.52 / 2.66 / 2.73 / 2.80（Premium 階梯）全都在，
就是沒有 0。零費率帳戶若在「有欄位」那群，我們會看到 0.000。看不到。

**所以缺欄位 = 費率 0 = Standard 等級。** 換算過來：

| | 零費率（Standard） | 有付費的那群 |
|---|---|---|
| **吃單** | **74.9% 的成交額**（638 個帳戶、29,430 筆） | 加權 **1.919 bps** |
| 掛單 | 11.4% 的成交額（96 個帳戶、3,489 筆） | 加權 **0.298 bps** |

**這是 mistake.md 2026-09-09 那條的鏡像。** 那次是「欄位的預設值 0 等於
『沒被寫』的結果，所以沒有人發現它沒被寫」；這次是**缺值編碼了零，
而我把缺值讀成『未知』**，於是得到一個剛好相反的結論。
判斷法很便宜而且我沒做：**看有值的那群裡有沒有出現 0**。沒有，就代表
0 被編碼在別的地方。

#### 這對策略的意思（§1.34 接手）

**§1.28 的「吃單門檻 3.00 bps」是 Binance 的費率。** 在 Lighter Standard 上
吃單費是 **0**，而 Lighter 最緊的幾個市場半價差是 **0.01–0.02 bps**
（同日 `lighter_mid` 量的：BTC 0.01、XAG 0.01、BNB 0.02）。
所以一個**慢**策略在 Lighter Standard 上的來回成本是
**0 費用 ＋ 約 0.02–0.05 bps 的價差**，而不是 3.00。

**這是整個 session 裡第一次有一個門檻低於已量到的毛利**（+0.58 bps，
但那是在 Binance 的標的上量的，必須在 Lighter 重量 —— 09-18）。

而 Standard 的代價（300ms 延遲、60 req/min）**對慢策略不構成約束**：
小時級再平衡一次幾張單。**它只擋做市**（要連續改報價）。
所以正確的結論不是「Standard 沒用」，是
**「Standard 給慢策略、Premium 給做市」，而我們現在用 Premium 跑慢策略 = 白付 2.80 bps。**

#### 樣本限制與下一步

上面是 **92 秒的驗收樣本 ＋ 一次重播**，不是判決。常駐錄製器
（`research/lighter/lighter_tape.py`）已於 2026-09-12 22:23 UTC+8 上線，
接了三道線（freshness／watchdog／manifest，反向證明過）。
**滿一天之後重跑這張表**，判準是：分布的形狀（0.28 佔大頭）是否穩定。
若穩定，「質押 LIT 值 0.12 bps」就是一個可以拿去算的數字；
若不穩定，先查是不是少數幾個做市商的活動時段造成的。

**這一節不回答「我們有沒有毛利」**（那是 §1.29，要等同一份資料滿六天）。
它只回答「這個場館的掛單成本實際上是多少」，而答案是
**0.28~0.40 bps，不是 0**。


### 1.32 【判決 ×2】費率修正後把資料在手的全部重跑 —— 套利家族扣掉 ANTH 是 $0，而 ANTH 不是一個配對（2026-09-12）

**觸發**：使用者「先都回測好，另外還需要再加其他交易所嗎」。
上游是同日的 `arblib/fees.py` 改正（Lighter 從 `0/0` 改成 PREMIUM 的
`taker 2.80 / maker 0.40`，見 CLAUDE.md §HFT），它讓 §0.75／§1.20／§1.21
引用的每一個數字過期。

---

#### A. 套利家族（§0.75／§1.20／§1.21）—— **扣掉 ANTH 之後是 $0/年**

`gate0_arb_capacity.py` 重跑，逐位元對照舊檔：

| | 修正前（09-11） | **修正後（09-12）** |
|---|---|---|
| `n_pairs_positive` | 2 | **1** |
| `total_usd_per_year` | $1,430.99 | **$1,098.95** |

而**那唯一的 1 個是 ANTH**，佔全家族 **89–95%**（`prereg_arb_patience`
的 k=0 逐側：ANTH:sell $1,112.4 / 全家 $1,248.9）。**其餘 8 個配對的
`size_max` 全部是「無」** —— 沒有任何下單金額讓淨值為正。

**而 ANTH 不是一個套利配對。** 證據在我們自己的設定檔第 2 行
（`arb/engine/config_ANTH.yaml`）：

    # leg A: Entropy(io) ANTH | leg B: lighter-rh ANTHROPIC

**兩腿是兩個不同名字的合成 pre-IPO 市場**（io 的 `ANTH` 與 Lighter
Robinhood 鏈的 `ANTHROPIC`），**沒有共同交割，所以沒有任何機制迫使它們
收斂**。對上錄製資料（18,347 分鐘）：

    premium_close_bps   中位 15.8   p90 420.8   p99 667.0   最大 1004.2
    buy_edge_max_bps    p1 −623.6   p10 −398.8            <- 兩個方向都有
    sell_edge_max > 200 bps 的分鐘：2,599 / 18,347 = 14.2%
    收斂事件 ~28 次/年  ->  2,599 / 28 ≈ **每個事件平均開 93 分鐘**

兩邊的頂檔都很緊（io 0.1、對手 0.9）、報價都很新（age 0.1–0.4 秒）——
**所以價差是真的，不是儀器壞掉**；壞的是「它是套利」這個假設。
93 分鐘的持續楔子是**慢速偏移**的形狀，不是套利回彈。
這正是 mistake.md 2026-09-03 記下的那個陷阱（「持續偏移被當成錢」）
的下一層：收斂關擋掉了大部分，但沒擋掉「兩腿根本不是同一個東西」。

**連帶：腿 B 今天下不了單。** `check_env` 的 `LIGHTER_RH` 三格全空 ——
我們在 Robinhood 鏈上沒有帳戶。所以 ANTH 與 NVDA_LL **在憑證層就不可執行**，
與 edge 無關。

**`arb_stable_basis.py`（§1.21）的 D1 自曝關擋住了自己**，而且擋得對：
ANTH 賣側本支 425.967 vs 凍結 427.823（0.43%，而其他 8 個配對都對到小數 3 位）。
**那個凍結值釘在一份滾動的錄製窗上** —— 錄製器一直在附加分鐘，而 ANTH 的
band 是重尾上的 p90，加分鐘會讓它比別人漂得多。這是 mistake.md 2026-09-10
「sha 釘樁釘在滾動的資料窗上」的同一族，**它會持續發紅而原因與它要保護的
東西無關**。修法同那條：改釘進 git 的凍結切片，或改成對資料窗免疫的形式。
**在修好之前 §1.21 的數字不得引用**（本支自己這麼說的）。

**判決**：§0.75 家族的可捕獲金額，在**扣掉一個不是配對的配對、以及一條
沒有帳戶的鏈**之後，是 **$0/年**。這不是「容量太小」，是**沒有東西在那裡**。

---

#### B. 做市 markout 篩選（`research/hl/flow_toxicity.py`，HL 資料在手）

來源是 Advanced Market Making 的「Being selective of what you quote」。
1 分鐘 markout（配我們的持有期，不是他的 5 秒）、成交金額加權、100 個
成交筆數 ≥200 的標的。自曝關兩道都過（吃單方向 買 51.0%／賣 49.0%、
成交價 vs 當下中價 中位偏離 9.14 bps）。

**全體成交額加權 −0.382 bps；為正的標的 51/100（≈ 丟硬幣）。**
HL 的 maker 費是 1.5 bps，所以**平均做市 ≈ −1.9 bps**。

**而正向那一側是鯨魚假象。** 加權值與中位差 20–80 倍是第一個訊號，
去掉最大的幾筆成交之後直接崩掉：

| 標的 | 加權 | 去前 1% | **去前 5%** | 前 1% 佔成交額 |
|---|---|---|---|---|
| REZ | +62.5 | +12.9 | **+4.3** | 24.8% |
| ETC | +39.4 | +6.8 | **+2.3** | 25.5% |
| ATOM | +24.1 | +9.4 | **+0.9** | 31.0% |
| MORPHO | +19.7 | +2.5 | **−1.2（翻負）** | 32.2% |
| USELESS | +13.7 | +3.1 | **+1.6** | 19.8% |
| BLUR | −12.1 | −3.0 | **−3.1** | 26.6% |
| SPX | −10.5 | −9.3 | **−6.9** | 33.0% |
| MET | −9.6 | −7.4 | **−5.1** | 20.6% |
| **BTC** | +1.0 | −0.3 | **+0.3** | 31.0% |
| **ETH** | −3.2 | −1.9 | **−1.4** | 27.6% |

**前 1% 的成交佔 20–33% 的成交額** —— 那就是機制。

**所以這個篩選的用法要反過來**：它**可靠地指出「不要報哪些」**
（負向那一側去掉前 5% 之後仍然明確為負），但**「流量不帶毒」那一側
大多是少數大單造成的，不可用來選要報什麼**。原文的用法是後者，
我們的資料只支持前者 —— 這個差異要寫下來，否則下一個人會照原文用。

**第二個要記的**：**BTC 與 ETH（最深的兩個名字）對做市方是 0 到微負**
（+0.3 / −1.4，去前 5%）。那是競爭市場該有的樣子，也就是說
**HL 的流動名字在掛單那一側沒有免費的午餐** —— 與 §1.27／§1.28 一致，
不是新的壞消息，是第一次用**逐標的的 markout** 而不是總量毛利看到同一件事。

**不是判決的部分**（照那支腳本自己的結尾）：它量的是「平均一筆成交」，
不是「我們會拿到的成交」。做市方被逆選擇，拿到的是對自己不利的那些 ——
所以真實的 markout 只會比這張表更差，不會更好。

---

#### C. 「還需要再加其他交易所嗎」—— 不需要，而理由是上面兩個判決

1. **MM 線的綁束在分子（毛利），不在分母（費用）。** 我們已經在能碰到的
   最便宜掛單場館：Lighter Premium **0.40 bps** vs HL 1.5 vs Binance 2.0。
   再加一家最多再省 ~0.4 bps，而 §1.28 的缺口是 **6 倍**。**槓桿方向錯了。**
2. **套利線剛剛在兩個獨立的點上死掉**（費率、以及 ANTH 不是配對）。
   在一個「9 個配對只扣費都為負」的家族裡加第 10 個場館，不會修好機制。
3. **我們缺的不是場館，是已選場館上的憑證與錄製器**：
   - **Lighter 逐筆成交帶：沒有**（不可回填，時鐘在跑）
   - **Lighter-RH 帳戶：沒有**（所以 ANTH／NVDA_LL 在憑證層不可執行）

**唯一值得查的例外，而且只要十分鐘**：有沒有哪個我們碰得到的永續場館
**真的付掛單返佣（負 maker 費）**。那會把損益平衡的毛利門檻推到 **≤ 0**，
是結構性改變不是邊際改變（`docs/external_reading.md`：「rebate = 最小價差」）。
**未查。** 這是「加場館」這個方向唯一有機制的版本。

---

#### D. 「先都回測好」的誠實現況

**MM 那條的回測現在跑不了，不是分析沒寫，是輸入不存在。** 要
Lighter 的逐筆成交帶 ＋ 細解析度中價，**兩個都還沒錄**。
`flow_toxicity` 能跑是因為 HL 的成交帶與中價都在錄 —— 換到 Lighter
一列都沒有。**這是唯一有時鐘的一件事**（WS 串流不可回填，每小時永久少一小時）。

所以順序是：**錄製器先建 → 六天後（09-18）才有東西可回測 → 回測過了才談實盤**。
使用者要的「先都回測好再上實盤」在 MM 這條線上**卡在第 0 步，不是第 2 步**。


### 1.31 【預註冊·未寫判準】Lighter 的 **300ms 取消延遲**值幾 bps（→ 要不要退回 Standard）（2026-09-12）

**來源**：docs.lighter.xyz/trading/trading-fees（官方，12 天前更新）。
查費率時翻到的 —— **他們的帳戶分級不是只分費率，是同時分延遲**：

| 帳戶級別 | Maker | Taker | **取消延遲** |
|---|---|---|---|
| Standard（預設） | **0** | **0** | **300ms（場館強加）** |
| **Premium（零質押）—— 我們現在就在這一級** | **0.40 bps** | **2.80 bps** | **不加延遲** |
| Premium（質押 500k LIT） | 0.28 | 1.96 | 不加延遲 |
| Plus（opt-in） | **0.5 bps 雙邊** | 0.5 bps | 200ms |

**方向是反的，這一點很重要**：帳戶 743078 的 portfolio 徽章是 **PREMIUM**
（同日四項證據，見 CLAUDE.md §HFT）。所以問題不是「要不要花 0.40 bps 買零延遲」，
是「**已經在付了，要不要退回 Standard 把它省下來**」。

原文：Premium 是「all order cancelations & Post Only order placements
not subject to any additional latency」—— 所以 **Standard 的免費是用「撤不快」換的**。

**為什麼這一節存在**：Market Making For Dummies 的原話是「maker 取消延遲
通常是做市最重要的」。CLAUDE.md §HFT 寫著三個門檳：吃單 3.00／naive 掛單 8.27／
**倉位型做市 ≈ 0.5**。「Lighter 零費率」看起來把分母歸零，
**它沒有歸零，它換了形式** —— 而 Premium 的 0.40 跟 Plus 的 0.50
**正好就坐在那條 0.5 線上**。

**要量的是一個數**：在決定要撤之後的 **300ms** 內，價格平均逆向走多遠（bps）。

    < 0.40 bps  ->  退回 Standard（那 0.40 bps 買到的東西不值錢）
    > 0.40 bps  ->  留在 Premium（現況，繼續付）

> **⚠ 2026-09-12 同日稍晚：上面這個判準不成立，本節的問法要換掉。**
>
> 使用者提供的分級對照補上一行我漏掉的，而它是決定性的：
>
> | | Standard | Premium |
> |---|---|---|
> | **API rate limit** | **約 60 req/min** | sendTx **4,000~48,000/min**（依質押）|
> | Sub-accounts | 4 | 64 |
> | Fast withdrawal | $3 固定 | 免費、無限制 |
> | 切換冷卻 | **降級要等 ~24h** | 升級通常無冷卻 |
>
> **60 req/min = 1 次/秒。** 而倉位型做市要連續改報價 —— 我們自己量到的
> 取消往返是 **50 ms**（物理上做得到約 20 次/秒），Standard 會把它壓到 1 次/秒。
> **所以 Standard 在結構上做不了做市**，跟「300ms 值幾 bps」完全無關：
> 就算答案是「不值 0.40 bps」，退回去省下的錢買到的是一個做不了這件事的帳戶。
>
> **帳戶等級不是費率題，是能力題。0.40 bps 是入場費，不是可選成本。**
> 官方自己的分類也這麼寫（Standard 的「適合對象」是零售／中低頻／方向性／
> Funding Arb；Premium 才是 HFT／做市）。
>
> **所以真正的成本槓桿換成質押 LIT：0.40 -> 0.28 bps（−30%）。**
> 而同日的成交帶量到 —— 這是量測不是推論 —— **69.9% 的成交額已經在 0.28**
> （逐筆 `maker_fee`，見 §1.33）。**那是競爭均衡價，不是優勢**：
> 到了 0.28 只是跟七成的對手平手，而我們現在在 0.40 等於貴 0.12 bps。
>
> **本節保留但改成這一題**：質押多少 LIT、鎖多久、機會成本多少，
> 換到的 0.12 bps 在我們的成交量上值多少。**未算**（要先有成交量估計，
> 而那要先有毛利 —— 所以它排在 §1.29 之後，不是之前）。
>
> 原本那個「300ms 逆行幾 bps」的量測**仍然有價值但降級**：
> 它不再是「要不要換級」的判準，而是「做市報價在 50 ms 的真實延遲下，
> 逆選擇成本是多少」的輸入。

**已量到的那一半（2026-09-12，首次實測）**：`arb/engine/tools/cancel_latency.py
--live -n 20`，NBIS，post-only 買單掛在 mid **−500 bps**、名目 $10.20、
送出即取消、六道閘全過、**G6 掃尾 0 張殘留**：

| | n | 中位 | p90 | 最大 | 最小 |
|---|---|---|---|---|---|
| 送單往返 | 20 | 54.1 ms | 67.0 | 126.0 | 51.7 |
| **取消往返** | 20 | **49.9 ms** | **54.7** | **60.3** | **48.2** |

**因為帳戶是 Premium，這 49.9 ms 就是我們自己的往返延遲，不含場館強加的地板。**
（最大 126 ms 是第 1 輪的冷 TLS，`venue_lighter.warm_http` 的註解早就寫過。）

**所以 300ms 是「加在 50ms 之上」，不是「300ms 總共」** —— 官方原文是
"not subject to any **additional** latency"。退回 Standard ＝ 50ms -> 約 350ms，
**七倍**，而不是「從 300 降到 50」。這讓那個選項比先前看起來更貴。

**一個有用的負面結果**：50 ms 遠低於分鐘級策略的決策尺度，所以
**取消延遲不是我們的綁束**——不需要買主機代管、不需要離場館更近的 VPS。
CLAUDE.md §HFT 說「延遲競賽進不去」仍然對（頂檔報價是 microsecond 的生意），
但**倉位型做市在分鐘尺度上不需要贏那場競賽**，而這是第一次有量測支持這句話。

**仍然未量的（本節不因此結案）**：
1. 「300ms 的額外裸奔值幾 bps」—— 判準沒變，需要 Lighter 逐筆成交帶。
2. **我們在 Lighter 上有沒有做市的毛利** —— §1.28 的 +0.58 是在 Binance
   量的，重量日期 09-18。**沒有這一項，延遲量得再漂亮都不構成可交易。**

**Gate 0（核心原則 11）：這一題本身就是執行層，所以它排在任何做市資訊層工作之前。**

**資料已經在手，不用新錄**：`hl_mid`（牙鐘 60 秒中價）解析度不夠，
但 **Lighter 自己的成交帶**足夠 —— 量「一筆成交之後 300ms 內的下一筆成交價」就是
這個數的上界（因為逆選擇最強的情境正好是有人連續吃）。

**需要先做的**：Lighter 逐筆成交帶錄製器（未建，不可回填）。

**連帶**：`arb/engine/tools/cancel_latency.py` 在 **Premium** 帳戶上量到的
就是**真正的往返延遲**（沒有場館強加的地板），所以它的數字可以直接讀成
「我們的基礎設施有多快」—— 這反而讓那支更有價值。
**但如果哪天退回 Standard，同一支量到的東西會變成交易所的計價政策，不是網路。**
換級之後必須重量，而且不可以拿兩級的數字互相比較。

**已改的下游（同日）**：`arblib/fees.py` 的 `lighter` 從 `0/0` 改成
`taker 2.80 / maker 0.40`，`arb_cost_model.json` 重生成。
費用桶每筆來回：單腿 Lighter **+5.60 bps**、兩腿都在 Lighter **+11.20 bps**。
**§0.75／§1.20／§1.21 引用的數字全部要重跑**（在 `../arb` repo）。

**同日的一個誤讀（留檔）**：我在 LIT 頁面看到
`Taker: 0.028% | Maker: 0.004%`，當場跟使用者說它推翻了「Lighter 零費率」。
**不成立** —— 那一組一字不差是 **Premium 零質押**那一格。三個來源一致：
BTC 頁面 `0% | 0%`、公開 API 上 **245 個市場**的 `taker_fee`/`maker_fee`
**全部 `0.0000`**、文件寫 Standard 是 0/0。底層的教訓是舊的：
**兩台儀器不同意時，先查它們跑在什麼上，不要先選一邊**（mistake.md 2026-09-11）。
而我選邊了，並且把它講成了結論。


### 1.29 【預註冊·未跑】倉位型做市的 Gate 0：**把他的 0.5 bps 換成我們自己量的數**（2026-09-12）

§1.27 / §1.28 之後，MFT 只剩一條路，而那條路的門檻用的是**別人的數字**：

    吃單 3.00 bps/單位成交量   （Bitget 返佣後，實查）
    掛單 8.27                  （我們實測：被動代價 7.27 + 掛單費 1.00）
    倉位型做市 **≈ 0.5**       ← Advanced MM 的「偏移進部位約少賺 0.5 bps」
                                 **未經我們驗證，而整條路的存廢押在它身上**

核心原則 11 的 Gate 0 (b) 寫著「那個價格在目標場館拿得到嗎 —— **要量測，
不是論證**」。所以這一節就是把 0.5 換掉。

而 `docs/external_reading.md` §17 的 C 項**早就登記了做法**並註明
「我們有 `hl_tape`（全市場逐筆成交），這個可以直接算」—— 登記了沒做。

#### 要量的東西（從**掛單方**的視角）

對每一筆成交（價格 P、吃單方向 s）：

    markout(h) = s × (P − mid(t+h)) / P × 1e4      s = +1 吃單方是買、−1 是賣
    單筆毛利   = 捕獲的半價差 + markout(h)

意思是：掛單的人在成交當下賺到半個價差，然後行情往哪邊走決定他最後是賺是賠。
**若 markout 比半價差更負，這個場館的被動做市本身就是賠錢的** ——
那時候「用訊號偏移報價」連地基都沒有。

h ∈ **1 / 5 / 30 分鐘**（mid 是牆鐘 60 秒取樣，秒級量不到；而我們的用途是
小時級再平衡，分鐘尺度才是對的那一個）。
按流動性分層報：**前 30 名 vs 31–100 名** —— CLAUDE.md 已量到尾 50 名頂檔
價差中位 38.5 bps、前 30 名只有 2.9，差 13 倍，合池會把兩個世界混成一個。

#### 判準（寫在跑之前）

| | 做什麼 | 為什麼這一關有牙齒 |
|---|---|---|
| **D1** | **`side` 的語意用資料證明，不用猜**：吃單方是買的那些成交，成交價必須平均**高於**當下的 mid；是賣的必須低於 | `A`/`B` 猜反會讓整個 markout 變號，而輸出**看起來完全正常** |
| **D2** | 實測半價差必須對得上 `hl_mid` 自己記的 `spread_bps`，而且前 30 名要落在 CLAUDE.md 已量的 ~2.9 bps 頂檔（半價差 ~1.45） | 對不上就是 bid/ask/mid 接錯 |
| **D3** | **安慰劑**：把 `side` 隨機打亂，markout 必須塌回 ~0 | 打亂後還有明顯非零 = 我量到的是某種漂移，不是「條件在吃單方向上」 |
| **MDE** | 先印 SE，再說這個樣本分不分辨得出「半價差 + markout > 0」 | mistake.md 2026-09-04 / 09-06：SE 要用判決那台機器算，而且要算在**被決定的那個量**上 |

#### 誠實的限制（先寫，不是事後補）

1. **這是上界，不是我們拿得到的數。** 我們量的是**所有成交的平均** markout，
   那等於假設我們拿到隨機的一片流量。**沒有排隊優先權的人拿不到隨機的一片**
   —— 逆選擇的定義就是「壞的那些會優先成交到你」。所以真實會比這裡差。
2. **秒級看不到。** mid 60 秒取樣。若 1 分鐘 markout 已經很負，結論成立；
   **若它是正的，就需要更細的資料才能確認**（只有一個方向的結果需要追加）。
3. **只有一天的行情。** tape × mid 的重疊是 **~23 小時 × 100 幣**
   （09-11 05:03 ~ 09-12 03:49 UTC）。筆數不是問題（09-11 一天 336 萬筆），
   **regime 覆蓋才是** —— 一天不是一個分布。

#### 所以這一節**不下判決**

它是**儀器 + 第一次讀數**。判決與 §1.23b 的 HL 扳機**同一天（09-18）**，
到那天只是換資料窗重跑，判準一個字不改。

先做的理由是 mistake.md 2026-09-08 那條：**扳機到期那天才發現儀器是壞的，
比晚一週知道答案貴得多。**

#### 資料勘查的副產品（記一筆，免得下一個人嚇到）

`D:/flowbot_data/hl/trades` 有 **39 個日期資料夾，最早 2024-03-15** ——
看起來像回填，**但不是**。那 37 個舊資料夾合計只有 0.6 MB，內容是
**死掉的幣的最後幾筆成交**（STRAX 停在 2024-03、RLB 停在 2025-04）：
WS 訂閱時會送「最近成交」，而殭屍市場的「最近」就是兩年前。
真資料 **99.3% 在 09-11（336 萬筆、整天）與 09-12**。

所以 CLAUDE.md §鏈上錄製集 的「成交帶不可回填」**沒有錯**。
但任何按日資料夾分組的程式會看到 39 天，**其中 37 天是幾十列的殭屍**。

#### 第一次讀數（2026-09-12，**不是判決**）

樣本：09-11 05:03 ~ 09-12 04:04 UTC、100 幣、**257 萬筆成交**。四道守衛全過
（D1 用資料證明 `side` 語意、D2 半價差逐筆對得上錄製值、D3 打亂後塌回 0、
D4 分解殘差 1.4e-13）。

| h(分) | 分層 | 筆數 | 半價差 | **掛單方單筆損益** | CI下 | 中位 | 截尾均值 |
|---|---|---|---|---|---|---|---|
| 1 | 前30 | 205 萬 | 0.99 | **−0.663** | −1.023 | −0.301 | −0.658 |
| 1 | 其餘 | 52 萬 | 1.76 | **−0.621** | −1.045 | +0.086 | −0.627 |
| 5 | 前30 | | 0.99 | −1.786 | −2.621 | −0.707 | −1.821 |
| 5 | 其餘 | | 1.77 | −3.478 | −4.559 | −0.823 | −3.293 |
| 30 | 前30 | | 0.99 | −0.991 | −3.044 | −0.439 | −1.072 |
| 30 | 其餘 | | 1.77 | −2.619 | −5.074 | −0.480 | −2.539 |

**在 HL 上當掛單方，1 分鐘的水平是每筆 −0.66 bps，CI 不含零。**
截掉雙尾 1% 只動 0.005（−0.663 -> −0.658）——**不是尾巴撐出來的**。

這一格已經**含了捕獲的半價差**（前 30 名 0.99 bps），所以逆選擇約 −1.65 bps。
換句話說：**HL 上的吃單流在 1~30 分鐘的尺度上是有資訊的，而被動的那一方在付錢。**

#### 對 §1.27 那張三門檻表的意思

    吃單 3.00 ／ naive 掛單 8.27 ／ 倉位型做市 **他說 ≈0.5**

**那個 0.5 在我們的場館、我們的持有尺度上不成立**：在「用訊號偏移報價」
之前，**被動做市本身就已經是負的**。0.5 是他相對「正常做市賺 2 bps」的
增量成本，而我們量到的正常做市是 **−0.66**，不是 +2。

所以 §1.27/§1.28 指出的那扇唯一還開著的門，**在這一天的 HL 資料上也是關的**。

#### 但要說清楚這**不是**什麼

- **不是說做市不可行。** 真正的做市商用秒級在翻庫存；我們量的是
  **持有 1 分鐘以上**的那個人。那正好是我們（小時級再平衡），
  但它不是對 HFT 做市商的陳述。
- **這是上界。** 量的是所有成交的平均 ＝ 假設拿到隨機的一片流量。
  沒有排隊優先權的人拿不到隨機的一片，真實會更差。
- **一天。** 判決在 09-18 跟 §1.23b 的 HL 扳機同一天換窗重跑。

#### D4 抓到的東西（比主結果更該記）

**60 秒取樣分不開「捕獲」與「漂移」。** 新鮮子樣本的捕獲是 **+5.01 bps**，
而半價差只有 1.20 —— 比值 **4.16**，該是 1.0。往下查到底：

| 診斷 | 結果 | 說明 |
|---|---|---|
| 偏離 vs 取樣後秒數 | 0–1 秒 4.58 -> 20–40 秒 9.07 | 陳舊佔一部分 |
| 偏離 vs 成交大小 | 最小單 5.13 -> 最大單 9.15 | 吃穿簿口佔一部分 |
| 最乾淨那格（<1 秒 ∧ 最小 20%） | **仍然 3.61**（半價差 1.30） | 兩者都解釋不完 |
| `book_time` vs 取樣戳 | 中位落後 **3.1 秒** | 多 3 秒只值不到 1 bps |
| `dev` 的分佈 | p5 **−37**、p25 −3.8、p95 **+65** | **買方吃單不可能印在買價之下** |

最後一列就是證據：**錄下來的 mid 在成交當下對不上時間**，
所以「捕獲」這一欄不可解讀。

**但總額不受影響** —— `s×(P − mid(t+h))` 只用成交價與一個時間已知的
未來 mid，陳舊的 mid(t) 根本沒進式子。所以主結果站得住，壞的是分解。

**連帶的工程結論（省下不可回填的錄製預算）**：
**現在不要動錄製解析度。** 主問題（做市賺不賺）用現有資料量得到；
只有「要把損益拆成捕獲 vs 漂移」才需要更細的 mid，而那是解釋層不是判決層。
要拆的那天再加，而且只加前 30 名的頂檔（不必整本 L2）。

#### 順帶量到的容量事實

HL 頂檔掛著的錢：**前 30 名中位 $989、其餘 $336**（單邊平均）；
5 bps 帶內 $7,746 / $2,526。成交名目中位 $175、p90 $4,316。

**頂檔只有四位數**——就算做市的單筆經濟性是正的，容量也在那裡。
這一條要跟 §1.20 套利的「對手簿頂檔只有 $264」放在一起讀：
**兩條線都撞到同一種天花板，而那個天花板是對手的深度不是我們的本金。**

**不做的**：不實作做市、不接任何下單路徑、不碰 §1.23b 的 HL 扳機。


### 1.25 【工程】加寬宇宙 —— 三條線在同一天撞到的同一個綁束（2026-09-11）

使用者：「開始做」。

#### 為什麼是這一件

2026-09-11 同一天，**三條線各自獨立撞到同一個東西**：

| 線 | 綁束 |
|---|---|
| §1.20 套利容量 | 事件數不夠（只錄 **8** 個配對）|
| §1.23 MFT | 橫斷面只有 **11** 個名字（他用 50），SE 2.2 bps/h 幾乎全來自它 |
| §1.24 耐心 | 事件數不夠，所以「挑慢的做」退化成「少做」|

而 Quant Arb〈How to level up your arb game〉第 5 條**逐字描述我們**：

> 「把交易所上每一個標的都交易。這聽起來很平凡，但人們常常只手動挑標的，
> **而沒有真正交易一切所需的自動化**。」

我們的形狀正是那個：**一個配對一個行程、一份 yaml、一支 .bat**，九份。

#### 凍結的規則（寫在看任何 band 之前）

`../arb/arblib/universe.py`：

    錄製宇宙 = 每一個「在 >= 2 個我們能接 WS 的場館上都有」的 canonical ticker
               **沒有排名、沒有門檻、沒有績效相關的排除**

**刻意不含任何篩選。** 理由不是偷懶是偏誤：任何「先看哪些配對帶比較寬再決定
錄哪些」就是 §0.92 判掉 C/D 變體的同一種錯，而且它會讓後面所有的樣本外失效。
唯一的排除是機械性的（場館自己說下市／零成交量／我們接不到它的 WS）。

標的跨場館對齊**不寫第二份**——直接 import `engine/tools/scanner.py` 的
`hl_universe()` / `lighter_universe()` 與它的別名政策（那個政策刻意保守：
SPY/SP500、XAUT/PAXG **不互為別名**，因為「同一個標的」不等於「同一個合約」）。

#### 可行性：**實測，不是推論**

加寬的前提是「一條 WS 能訂幾個」。這個數字如果是幾十，加寬就得改架構。
實測（2026-09-11）：

| 場館 | 訂閱數 | 回資料 |
|---|---|---|
| **HL** | 40 / 95 / 171 / **234（整個主場）** | **100% / 100% / 100% / 100%** |
| **lighter-rh** | 20 / 50 / **57（整個場館）** | **100% / 100% / 100%** |
| lighter | 市場清單 233 個；訂閱上限當時 REST 405（它自己的註解記過這個 burst 限制）| **未測** |

**沒有任何場館是瓶頸。**

> 探測腳本第一版對 lighter 在**每一個** size 都回 0%。那不是場館拒絕，
> 是**我的儀器壞了**——漏了 production 有做的兩件事：**要等 `connected` 幀
> 才能訂閱、而且要回 `pong`**。補上之後 57/57。
> （規矩：自己剛寫的儀器全 0 先查自己。）

#### 宇宙的大小

`../arb/results/arb_universe.json`（場館自己的端點，不是掃描器的樣本）：

    HL 178 個標的 / lighter 216 / lighter-rh 57
    >= 2 個場館都有的 ticker = **126 個**
    配對 = **150 個**（現在錄 8 個，**18.75 倍**）
    WS 訂閱 = HL 95 + lighter 126 + lighter-rh 43 = **264，三條連線**
      逐場館對：HL-lighter 95、lighter-lighter-rh 43、HL-lighter-rh 12

用掃描器 CSV 獨立估過是 125 / 149 —— 兩個來源對得上。

#### 錄製器：`../arb/engine/tools/record_universe.py`

**不改既有引擎**，理由有兩個：150 個行程不可行，而且 `main.py` 裡有實盤
下單路徑，只為了錄資料去跑 150 份它是錯的。這一支是**純錄製**，
不 import `Engine`、不碰憑證。

**新的程式碼只有一層：「一條連線帶多個簿口」**（production 是一個幣一條
連線）。其餘全部重用、一行都不重寫：

    entropy_arb.book.OrderBook           apply_hl / apply_lighter 的唯一實作
    entropy_arb.recorder.MinuteRecorder  HEADER 與分鐘聚合的唯一實作
    arblib.universe                      凍結的宇宙
    engine/tools/scanner.py              標的對齊

**簿口共用**是省下來的地方：簿口以 `(場館, ticker)` 為單位（264 個），
`BTC@HL-lighter` 與 `BTC@HL-lighter-rh` **共用同一個 HL:BTC 簿口**，
而 `MinuteRecorder` 的建構子本來就收兩個 book 物件。

**欄位 schema 與既有 `minutes.csv` 逐欄相同** —— 刻意的：下游
`premium_verdict.load()` 因此一行都不用改。

#### 已知缺口（先寫出來，不要等人發現）

1. **資金費欄位會是空的**（`funding=None`）。既有九個配對有資金費是因為
   它們跑完整引擎帶 FundingPoller。後果：新配對算不出成本模型的**桶 4**。
   要補就另外接一支逐場館的資金費輪詢。
2. **只有頂檔**（schema 的限制）。分檔深度仍然只有 REST 掃描器有，
   而 §1.20 的容量正是靠那個——所以新配對的容量要等掃描器覆蓋它們。
3. **`premium_verdict.PAIRS` 仍然寫死九個。** 本支只產生資料；
   判決吃這 150 個是下一步。**先錄是因為錄不到的不可回填。**

#### 三道接線（CLAUDE.md：缺一個它就在某個方向上隱形）

| 道 | 狀態 |
|---|---|
| freshness | 本支每 30 秒寫 `logs/universe/_flag.json`（{ok, reason, asof}），**啟動那一瞬間就寫一次 ok=True**（否則看門狗會殺掉剛起來的行程，mistake.md 2026-09-11 成交帶踩過）。**flow_system 的 `freshness_board` 要加一列——未做** |
| watchdog | `ops/arb_watchdog.ps1` 要加一條（行程名 `record_universe`）——**未做** |
| manifest | `logs/universe/` 要進資料清冊——**未做** |

**那三道沒接完之前，這支不算上線。**

---


### 1.24【預註冊·未凍結】用耐心換速度 —— **設計測不動，而且看起來變好的那一半全在 ANTH**（2026-09-11）

> **2026-09-11 稍晚補一個註記（§1.25 前置量測，`arb/arblib/band_convergence.py`）**
> 這一節與 §1.20 的所有數字都是從**帶寬**長出來的，而帶寬（p90）的取樣誤差
> **從來沒有被量過**。量完的結果：一天的錄製下 **0/15 個「配對:邊」**把帶寬
> 估到 ±20% 以內；而 **ANTH 兩邊都在最不穩的那一群**
> （`ANTH:buy` 1 天 1372% → 3 天 1257%、`ANTH:sell` 96% → 96%）。
> 本節「87% 的效果在 ANTH」與 §1.20「95% 的容量在 ANTH」因此要一併標上
> **輸入的取樣誤差未量化**。這不改變本節的結論（本來就是無效判決），
> 但它讓那個結論**更站得住**，而不是更弱——測不動的原因多了一層。

使用者：「我覺得可以做這個，**我們只求穩不求快**」。
來源是 Quant Arb〈Ultimate Crypto Arbitrage Guide〉：
「如果你在延遲上跟不上，就去交易比較慢的那些機會。」
入場券我們早上已經量過（`gate0_arb_persistence.py`：P(再活1分鐘|已活k)
= 0.596/0.821/0.916/0.982，8/8 配對單調、打亂對照平坦）。

`research/prereg_arb_patience.py`。**門檻刻意留空**，這一輪只算 SE。

**動手前先寫下的衝突（而它最後被資料證實了）**：§1.20 說綁束是事件數，
而年齡濾網會讓事件數更少 -> 在現有配對上「耐心」是**減法**。
所以主要指標寫死成 **$/年**，不是每筆 bps
（用每筆 bps 這個測試幾乎保證會「通過」而且毫無意義——條件在「活得久」上
依定義會選到偏離大的那些）。

**自曝三關全過**：C1 凍結計分器回傳的每一個起點、我重算的 |dev| ≥ band
（0 個不符）；C2 k=0 的可捕獲 / (band/2) 中位 1.251；
C3 k=0 就是 §1.20 的構造，逐配對比值 1.18–1.84（同量級）。

**表面結果（不要只看這個）**：

| k（分鐘） | 0 | 1 | 2 | 3 | 5 | **8** | 13 | 21 |
|---|---|---|---|---|---|---|---|---|
| 事件數 | 34 | 33 | 28 | 28 | 21 | **21** | 19 | 15 |
| $/年 | 1,854 | 2,027 | 2,128 | 2,121 | 2,595 | **3,573** | 2,618 | 2,737 |

看起來耐心把錢**將近翻倍**，而事件數只掉到 61.8%。**但兩個檢查把它推翻：**

**(1) 功效：SE ≥ 效應。** 日聚類 bootstrap（重抽「天」，11 天）：
k=8 比 k=0 多 **$2,049/年**，而**那個差的 SE 是 $2,222/年**。
-> **無效判決（inconclusive by design），不是 PASS 也不是 FAIL**
（mistake.md 2026-09-04：`SE ≥ 門檻 ⇒ 這個設計不能做決定`）。
每一臂的 `P(>k=0)` 都只有 64–79%，而曲線本身不單調（k=8 最好、k=13 變差、
k=21 又好）——那是 n=15~21 的雜訊形狀。

**(2) 集中度：87% 在一個配對。** k=0 的總美元逐配對：

    ANTH:sell  51.85   <- 87%
    NBIS:sell   4.29
    ZEC/HYPE/NEAR/SNDK/NVDA_LL  各 0.5~1.1

**去掉 ANTH 之後：k=0 $278.9/年 -> k=8 $32.2/年 —— 方向反過來。**
也就是說「耐心讓錢變多」這件事在**其餘六個配對上是反的**，
而 ANTH 正是那個 464 bps、要先當風險溢酬處理的配對。

#### 結論與解鎖條件

**耐心這條路沒有被推翻，是「在這個宇宙上測不動」。** 而我動手前寫下的
衝突得到了證實：**事件數是綁束的時候，耐心是減法。**

**解鎖條件是宇宙寬度，不是更多參數**：
- 事件數要夠多，耐心才能「挑慢的做」而不是「少做」
- 這跟 §1.20（容量）、§1.23（MFT 橫斷面 11 個名字）指向**同一件事**，
  也就是他〈How to level up your arb game〉第 5 條：
  **「把交易所上每一個標的都交易；人們只手動挑標的、沒有自動化」**
- 我們錄 8 個配對，掃描器已經在看 **3,094 個**

**三條線（套利容量、MFT、耐心）在同一天各自獨立撞到同一個綁束。**
所以下一個工程項目是**加寬宇宙**，不是再調任何一條線的參數。

**門檻不凍結**（沒有測量能力的設計不該開時鐘，mistake.md 2026-09-04）。
宇宙加寬之後再回來，屆時 SE 會隨事件數下降，判準才寫得出來。

---


### 1.22 錄製缺口：`minutes.csv` 沒有成交量，所以 lead-lag 的條件式主張檢定不了（2026-09-11）

> ### ⚠ 2026-09-12：這個缺口現在擋住**兩**件事，而且第二件是容量瓶頸本身
>
> 讀完 Small Trader Alpha **#1 / #2**（`docs/external_reading.md` §19）之後，
> 同一個沒錄的欄位又擋住一個**獨立登記**的需求：
>
> **我們 §1.20 的容量模型量的是「對手頂檔的瞬時深度」（$264），
> 而他量的是「一段時間流過來的量」。** 他的例子是每 20 秒 $3,000 連續數小時
> ＝ **$540,000/小時**，先假設吃得到一半。那跟 $264 不是同一個量 ——
> 一個是快照，一個是時間累積，**而套利的持有期本來就是分鐘到小時**。
>
> 加上 #2 的「往深處掛」：頂檔不是上限，怪物檔位才是量的所在，
> 而「貼頂檔還是往後退」取決於機會的成因（有人亂倒／風險溢價／太冷門）。
>
> **所以「要不要錄成交量」不再是一個開放問題。** 一個不可回填的缺口
> 擋住兩個獨立登記的需求，其中一個是這條線唯一可能的突破口。
>
> 照他們的算式反推，要錄的是三樣（不是隨便加欄位）：
> **逐側成交量**（買/賣分開，#1 的第一步就是拆兩側）、
> **成交的 offset 分佈**（離公允價幾 bps —— 跟 §1.27 的 δ 掃描同一個量）、
> **頂檔以外的深度**（否則「往深處掛」量不了）。

同一篇的第 (6)(7) 點：**誰在移動是條件於單量的**，而 lead-lag 的強度要用
**非預期成交量**（成交量的 z-score ＋ log 轉換）動態建模，
不是相對成交量的水準。

**我們的 `who_moves` 量的是無條件中位數（0.485–0.543）**，
而那個數字與「大單 50/50 ＋ 小單 100/0」的混合**相容**——
所以它既不支持也不反對他的主張。原本寫的「他的核心假設失敗」**已撤回**
（更正寫在 `research/gate0_arb_unhedged.py` 的 `who_moves` 判讀旁邊）。

**要能檢定，需要錄成交量**。`minutes.csv` 現有 29 欄裡
只有 `samples`（取樣數）與四個**掛單量**欄位，**沒有成交量**。

**這是一個有時鐘的決定**（CLAUDE.md §鏈上錄製集同一條）：
判準可以之後再寫，**錄得不夠細是不可回填的**。所以要先決定錄不錄，
再決定要測什麼。

> ### ⚠ 2026-09-11 同日更正：診斷寫錯了一層
>
> 讀完該文附的碩論〈High Frequency Lead-Lag Relationships In The Bitcoin
> Market〉（127 頁，已下載到 `D:/flowbot_data/quant_arb/attachments/`）之後，
> **上面那句「缺的是成交量」不是最根本的那一層**：
>
> 1. **缺的是解析度。** 該文用 HY 估計量量到「價格移動在落後的交易所
>    **晚 10–15 秒**發生」。而我們的錄製器**內部每秒取樣、只寫分鐘聚合**
>    —— **10–15 秒的現象在 1 分鐘的桶裡依定義不存在**。
>    就算補上成交量也看不到。**而解析度不可回填。**
> 2. **「誰領先」最可能的解釋是流動性不是成交量，而流動性我們有。**
>    該文摘要：「本論文的分析指向**交易所的流動性**是最可能的解釋」；
>    結論：「較小、較不流動的交易所對其他**全部**交易所呈現落後」。
>    它還註明流動性**對洗量更穩健**——那正是 Quant Arb 自己在
>    〈Finding Arbitrage Opportunities〉裡警告的東西。
>    我們有深度（掃描器 1bps/3bps、§1.25 起 264 個簿口的頂檔），
>    **所以「誰領先」可以用已經在錄的東西測，不必等成交量。**
>
> **所以這一節的解鎖條件改了**：從「錄成交量」改成
> **(a) 用深度當流動性代理去測領先關係**（現在就能做），
> **(b) 決定要不要把秒級快照也落盤**（有時鐘的決定，不可回填）。

**順帶，同一篇有兩件用現有資料就能測的**：
- 第 (8) 點「先收到的訊息價格最準，你傾向在訊息最舊那一腿漏單」
  -> `minutes.csv` 已有 `sell_max_age_s` / `buy_max_age_s`。
- 第 (9) 點未完成定價 `True Price = Price − E[成本|未完成]×P(未完成)`
  -> 現在 `leg_fail_rate` 是全家族一個 6% 的常數；
     他說未完成率**逐交易所差很多**，而我們有 `venue_toxicity_*.json`。

---


### 1.21 §0.75 的 premium 從來沒有扣掉穩定幣基差 —— 我們寫了它的名字三次（2026-09-11）

> **⚠ 2026-09-12：本節的數字已被 §1.32 取代。** 上游的 `arblib/fees.py` 把 Lighter 從 `0/0` 改成 PREMIUM 的 `taker 2.80 / maker 0.40`，成本模型重生成後容量從 $1,431 掉到 $1,099、正格配對從 2 個掉到 1 個，而那 1 個（ANTH）**兩腿不是同一個合約**。詳見 §1.32。

來源：Quant Arb〈Small Trader Alpha #6: Perpetual Arbitrage〉付費全文
（`docs/external_reading.md` 第 13 則）。他把穩定幣正規化列為**第一步**：

> 「除非我們針對**交易所各自的**穩定幣價格做調整，而不是套用某個全球價格，
> 否則我們看到的永續之間的基差會是**一個對 USDC 的賭注**。」

**查證結果**：`lighter-rh` 的報價在 **USDG**，而 8 個配對裡
**6 個的 B 腿是它**（SNDK/ANTH/BTC/HYPE/ZEC/NEAR），
GOLD_LL/NVDA_LL 的 B 腿也是。而 `premium_close_bps` 是**原始價格比**，
**沒有扣任何穩定幣基差**。

而我們**寫過它三次**：

```
arblib/cost_model.py:18        「7 tail … stablecoin depeg (USDG!)」
arblib/cost_model.py:96        ("stablecoin depeg USDG/USDC", 0.10, 0.03, "ASSUMED; lighter-rh quotes in USDG")
arblib/premium_verdict.py:66   「convergence (USDC vs USDG basis dressed up as premium)」
```

**第三行逐字寫著「被打扮成 premium 的 USDC/USDG 基差」——然後沒有任何
一行程式去扣它。** 這是 mistake.md「我在檔頭寫下這個病的名字，然後把
同一個病留在那個檔案裡」的第三個實例（前兩次：成交帶未註冊 freshness、
`conj_update` 漏掉 levels/events）。

**影響方向未知，所以不准說「我們的判決偏樂觀」**——要量。在量到之前：
**§0.75 的 band 一律標註「未扣穩定幣基差」**，§1.20 的容量數字同樣帶這個註記。

**怎麼量（已做，`research/arb_stable_basis.py`）**：用掃描器 v5 的
**參考標的**（BTC/ETH/SOL/XRP/SUI —— 流動幣，我們不交易它們）算每個場館對的
共同中價偏移。**不可以用配對自己的標的去估**，那會把訊號一起扣掉。
自曝三關：D1 重現凍結的 band、D2 偏移必須**跨標的共同**、
D3 兩條「USDC 場館 → lighter-rh」必須接近才准轉運到 ANTH。

#### 結果：**我們擔心了四次的東西很小，真正沒扣的是另一個**

D1 **9/9 配對逐側完全對上**（第一版的 D1 自己犯了 mistake.md 2026-09-10
那個病——把等號釘在還在長的資料窗上，差 0.4%；改成截到 JSON 的 `asof` 後
變成完全相等）。

D2 用三個**獨立的** CEX 當第三方，結論一致：

| 場館對 | 共同偏移（中位 bps）|
|---|---|
| **lighter-rh → binance / bitget / okx** | **+0.22 / +0.03 / +0.24** |
| lighter → binance / bitget / okx | +0.86 / +0.70 / +0.82 |
| HL → binance / bitget / okx | +1.64 / +1.87 / +2.40 |

> **USDG 基差本身 ≈ 0（< 0.25 bps）。** `lighter-rh` 相對三個 CEX 幾乎沒有偏移。
> 四處警告寫的那個東西，量出來很小。

**而真正沒被扣掉的是場館自己的結構溢價**：HL 比 CEX 群高約 **1.6–2.4 bps**、
lighter 高約 **0.7–0.9 bps**。這是 §1.03（#6）列的三個原因裡的**第二個**
（結構差異），不是第一個（穩定幣）。

D2 也把 `HL-lighter-rh` 自己標成**離散**（IQR 4.33，XRP +4.82 撐高），
所以那四個配對只能走 **D3 轉運（+1.574，一個假設）**，已在輸出標明。
**SUI 在每一個場館對上都是 +6~+9 bps**，被 MAD 規則自動剔除——
那是「SUI 在 HL/lighter 上就是貴」，是標的自己的事，不是場館基差。

**扣掉之後的 band（賣側 / 買側）**：

| 配對 | 原 | 修正後 | 備註 |
|---|---|---|---|
| **NBIS** | 26.10 / 9.33 | **不變** | 兩腿都非 USDG，**沒有基差**——而它是唯一有正 $/年的兩個之一 |
| BTC（對照組）| 5.59 / 2.08 | **4.02** / 3.65 | 對照組的 band 設計上該 ≈0，**修正把它往 0 推，方向對** |
| HYPE | 8.97 / 4.69 | 7.39 / 6.27 | |
| ZEC | 10.98 / 9.36 | 9.40 / 10.93 | |
| NEAR | 10.89 / 2.89 | 9.32 / 4.47 | |
| SNDK | 1.58 / 10.61 | **1.00（碰 floor）** / 12.19 | 扣掉之後賣側沒有空間了 |
| NVDA_LL | 1.03 / 2.93 | **1.00（碰 floor）** / 3.90 | 同上 |
| GOLD_LL | 7.97 / 1.00 | 7.00 / 1.00 | |
| ANTH | **464.11** / 1.54 | 462.54 / 3.11 | 見下 |

**兩個要往下追的**：

1. **ANTH 賣側 band 是 465 bps。** 凍結的 JSON 也是這個數（所以不是我的 bug）。
   那是 Anthropic 的**私司永續、沒有現貨錨**。§1.03（#4 資金費）給了一個我們
   從來沒命名過的競爭解釋：**「有些機會單純是因為那個幣本身有風險」**
   ——風險溢酬，不是無效率。OAI 與 EWY **在我們錄到第一分鐘之前就下市了**
   （`factor-research.md` 第 2 條已記），那就是這個風險實現的樣子。
   **465 bps 要先當風險溢酬處理，不是當 edge。**
2. **修正之後有兩個配對的某一側碰到 1.0 bps 的 floor。** floor 是
   `premium_verdict.NET_BPS_MIN`，它的意思是「再小就沒意義」——碰到它等於
   **那一側扣掉結構溢價之後沒有空間**。

**對既有判決的影響**：`band` 與 §1.20 的容量數字**從現在起一律標註
「未扣場館結構溢價」或引用修正後的值**。NBIS 不受影響，
所以 §1.20 的 $225/年裡 **NBIS 那 $114 不變**；ANTH 那 $111 的 band 變動
在小數點後（465→462.5），也幾乎不變。**所以容量的結論沒有被推翻。**

> **2026-09-11 稍晚，第二個要標的註記**（`arb/arblib/band_convergence.py`）：
> 上面談的是「帶寬有沒有扣對東西」，而**沒有人問過那個帶寬本身估得準不準**。
> 量完：一天的錄製下 **0/15 個「配對:邊」**把帶寬估到 ±20% 以內，三天 4/15，
> 而 **ANTH 兩邊在現有 9.7 天內完全沒有穩下來**（buy 1372%→1257%、
> sell 96%→96%）。§1.20 的容量 **95% 來自 ANTH**，所以那個數字的誤差棒
> 主要由一個估不準的輸入決定。**容量的結論（綁束是事件數不是深度）不變**
> ——事件數那一側的量級差距遠大於這個誤差——但**「$X/年」這種點估計不得
> 再被單獨引用**，引用時要帶這一行。
>
> **2026-09-11 再晚一點，誤差棒算出來了**（`research/gate0_arb_capacity_ci.py`，
> 一天為單位的區塊重抽 400 次，帶寬與事件率**一起抽**以保留它們的負相關）：
>
> | | $/年 |
> |---|---|
> | 點估計 | **1,270** |
> | 90% 區間 | **[0, 4,861]** — 寬度是點估計的 **3.8 倍，下緣為零** |
>
> 而且**點估計 > 0 的只有 1/8 個配對（ANTH）**，其餘七個全是 0
> （NBIS 的 p95 是 8、NVDA_LL 是 3，其餘連 p95 都是 0）。
> 所以「家族容量」這個詞是誤導的——**它是一個配對，而那個配對的區間跨越零**。
> D1（重算的 band 必須重現凍結值）8/8 PASS，D2（重抽中位數不得系統性偏移）
> 偏 13.3% PASS。
>
> **未解的儀器歧異**：NEAR 的事件率重算 60.9 vs 凍結 31.0（2 倍）。算式同一條
> （`cost_model.py:281`，已核對），差在 `days` 或讀檔範圍。NEAR 的容量點估計
> 與 p95 都是 0，所以不影響上面的結論，但它是一個真的不一致，**沒有被解決**。

---


### 1.20 §0.75 的容量 —— Gate 0 的第三關（2026-09-11）

> **⚠ 2026-09-12：本節的數字已被 §1.32 取代。** 上游的 `arblib/fees.py` 把 Lighter 從 `0/0` 改成 PREMIUM 的 `taker 2.80 / maker 0.40`，成本模型重生成後容量從 $1,431 掉到 $1,099、正格配對從 2 個掉到 1 個，而那 1 個（ANTH）**兩腿不是同一個合約**。詳見 §1.32。

> 編號用 1.20 不用 1.12：**§1.00–1.13 是套利線的號段，已移出到 `../arb/TODO.md`**。
> 這一節留在本 repo 是因為腳本在這裡（`research/gate0_arb_capacity.py`，
> 與 `gate0_arb_unhedged.py` / `gate0_arb_persistence.py` 同地），
> 它只**讀** `../arb` 的產物，照既有的單向橋紀律。

`research/gate0_arb_capacity.py`。動機：`gate0_arb_unhedged` 量到「掛單進
＋不跨場館」讓 6/8 配對每筆淨值翻正，但那是在 **size=200** 上算的，
而 $200 × 2.56 bps = **$0.05 一筆**。`factor-research.md` 第 10 條：
**容量是獨立的一關**。

**先修一個會決定答案的儀器問題。** `arblib/cost_model.py:231`
把兩個分檔深度都寫成 `top × 4.5`（註解自己標 ASSUMED）。
但 `engine/tools/scanner.py` 的 `DEPTH_BPS = (1.0, 3.0)` **有真實量測**，
而且它的宇宙**正好涵蓋** §0.75 這八個配對。換掉之後：

| 配對 | 頂檔$ | 1bps$ | 3bps$ | 3bps/頂檔（假設是 **4.5x**）|
|---|---|---|---|---|
| NBIS | 368 | 1,070 | 3,231 | 8.8x |
| ANTH | 166 | 764 | 2,120 | 12.8x |
| BTC | 250 | 62,789 | 334,237 | **1337x** |
| HYPE | 1,000 | 6,490 | 37,443 | 37x |
| ZEC | 881 | 3,979 | 8,604 | 9.8x |
| NEAR | 410 | 1,676 | 5,153 | 12.6x |
| GOLD_LL | 500 | 23,827 | 84,205 | 168x |
| NVDA_LL | 392 | 8,476 | 26,241 | 67x |

（取**兩腿的較小者**、再取**較淺的那一側**，中位數；刻意悲觀。
自曝三關：D1 用假設深度重現凍結的 `net_bps`、D2 深度單調性、
D3 滑價函式串接，**三關全 PASS** 才印下面的表。）

**深度不是綁束——這推翻了我自己的先驗。** 容量曲線有峰（size 變大滑價變大，
但小場館各壓 $300 的閒置資金被攤薄），`size$` = `size × 每筆淨` 最大的格子：

| 配對 | size_max | size$ | 淨@size$ | $/筆 | 次/年 | **$/年** |
|---|---|---|---|---|---|---|
| NBIS | 3,000 | 1,000 | +2.42 | 0.242 | 470 | **113.66** |
| ANTH | 2,000 | 2,000 | +1.66 | 0.332 | 335 | **111.31** |
| GOLD_LL | 50,000 | 25,000 | +1.98 | 4.957 | **0** | — |
| NVDA_LL | 10,000 | 7,500 | +0.99 | 0.743 | **0** | — |
| HYPE | 7,500 | 5,000 | +0.85 | 0.427 | **0** | — |
| NEAR | 2,000 | 1,500 | +1.36 | 0.204 | **0** | — |
| ZEC | **無** | — | — | — | 134 | — |
| BTC | **無** | — | — | — | 0 | — |

**全家族合計 $225 / 年。**

> ### ⚠ 2026-09-11 當日更正：上面整張表跑在一份**六天沒更新**的成本模型上
>
> `arb_cost_model.json` 的 mtime 是 **09-05**。原因是 7 天閘門達標那天
> `premium_verdict` 把 json 的 schema 從 `interim` 換成 `sides`，而
> `cost_model.family_specs()` 只讀 `interim` 並且 **`if not i: continue`
> 靜默跳過每一個配對**（實測今天呼叫它回傳 **0 個**配對）。
> 詳見 mistake.md 同日那條。修完重跑：
>
> | | 舊檔 | 重跑 |
> |---|---|---|
> | 配對數 | 8 | **9**（SNDK 原本整個不見）|
> | NBIS band / 事件/年 | 13.40 / 470 | **26.09 / 62** |
> | ANTH band / 事件/年 | 15.89 / 335 | **464.09 / 31** |
> | HYPE / NVDA_LL 事件/年 | **0 / 0** | **187 / 808** |
> | **合計** | **$225/年** | **$1,431/年** |
>
> **「4 個配對事件/年 = 0」這句話裡有三個 0 是舊檔造成的**，要撤回。
> 但**結論的方向還在，而且變得更尖銳**：
>
> | 配對 | size$ | 淨@size$ | 次/年 | **$/年** |
> |---|---|---|---|---|
> | **ANTH** | 2,000 | +219.06 | 31 | **1,358.7（全家族 95%）** |
> | NBIS | 3,000 | +3.89 | 62 | 72.3 |
> | GOLD_LL | 25,000 | +2.23 | **0** | — |
> | SNDK / BTC / HYPE / ZEC / NEAR / NVDA_LL | — | **任何 size 都沒有正格** | — | — |
>
> **所以修正後的誠實讀法是：拿掉 ANTH 只剩 $72/年，比舊的 $225 更差。**
> 而 ANTH 的 band 是 **464 bps**、是 Anthropic 的私司永續、沒有現貨錨，
> §1.21 已經把它標成「**先當風險溢酬處理，不是當 edge**」。
> **$1,431/年 這個數字不可以單獨引用。**

**真正的綁束換人了**：不是深度（size_max 普遍在 $2k–50k），是**事件數**。
四個配對有正的格子但 `次/年 = 0` —— 那是**沒有量到收斂事件**，
**不是「沒有機會」**，在錄到事件數之前它們的 $/年 是未知不是零。
GOLD_LL 一筆值 $4.96（全家族最高，深度 $84k），只缺事件計數。

**→ 下一步不是改構造，是量事件數。** 那四個配對的收斂事件為什麼是 0
（真的沒有，還是收斂判準在它們上面量不到）是唯一能改變這個數字的事。
`$225/年` 在量到之前是**下界**，不是結論。

#### 連帶：§1.10 的經濟錨現在有了

§1.10（鏈上燃料機制關）凍結時門檻留空，理由是「雜訊量未知」。
**現在經濟錨有了：0.053 R（6 bps）。** 機制關量到的效應若低於它，
就算統計上顯著也沒有下一步 —— 這正是假說 1 要擋的那件事。
（統計門檻仍要等資料算 SE，兩者要同時滿足。）

#### 現在不做的

不建任何下單路徑、不碰 `jarvis`、不碰任何真錢。**做市在這個費率下已排除**，
所以 HFT 那條的變現路徑要重新想（他的文章說 HFT alpha 只能當做市偏移，
而我們剛量出做市是負的）。本節在那之前**只產出量測與筆記**。

---


---

### 0.75 兩場館永續套利（entropy-arb 型）——第四條策略線，錄資料階段（2026-08-28 登記）

> **2026-09-03 修正：可捕獲金額原本高估一到兩個數量級**（使用者問「那 30
> 美金一天是怎麼來的」）。`fires` 數的是**分鐘**不是交易——帶是 p90，
> 所以依定義每天 144 分鐘開火，但一段一小時的偏離是**一筆**交易含 60 個
> 開火分鐘；而收斂定義是回到**半個帶**，所以每筆抓半個帶。改成
> 事件數/天 × 半個帶 × 深度後：**SNDK 賣 $0.84 / 買 $0.07、NBIS 賣 $2.42、
> ANTH 賣 $0.05 / 買 $0.17**（原為 $8.4 / $29.5 / $13.0 / $14.4 / $25.9）。
> 舊值保留為 `_ceiling` 可稽核。**判準未動**。09-04 判決要照新數字讀。
> 詳見 mistake.md 同日第二則。

> **本線的家在 `research/arb/`（2026-08-28 使用者要求與交易策略分開）**：
> README 載明隔離規則（永不 import 交易路徑、資金獨立、只共用兩個顯示
> 看板）、五條件偵察表、戰場帳本。判決程式 = `research/arb/premium_verdict.py`。
> 本節保留為登記與風險清單的原始紀錄。

**來源**：使用者研究了 `your-quantguy/entropy-arb`（兩場館 perp 套利：
Entropy/Hyperliquid vs Lighter/trade.xyz，量 `premium_bps = (A/B − 1)×1e4`
偏離中線超過門檻就雙腿對鎖，等收斂平倉）。使用者：「我是想做一個這個」。

**為什麼值得排**：與現有三條線**完全不同的棋格**——結構性 edge 不是統計性,
機會存在性直接可觀測,**驗證週期是天不是月**（錄一兩週掛單簿 → 扣費後的
溢價分佈就是答案）。不需要預註冊/CI/樣本時鐘那一套。

**風險清單（先寫,免得到時候忘）**
- 斷腿 = 主要事故源：一腿成交一腿被拒 → 裸倉在動的市場裡
- **中線漂移 = 2026-08-08 解碼漂移的同款病**：參考分佈不是被判斷的分佈,
  每筆小虧且看起來像正常交易
- **零費率是獲客補貼,有內建過期日**：Lighter taker 0 bps 不是均衡態
- 撿到的是做市商不要的：簿太薄或風險他們算過不值得
- 該 repo 一次 commit / 零 issue：**下單程式碼要逐行審**（滑價符號 bug 的
  先例——「看起來扣了成本」騙過所有人）
- **「沒有倉位也會爆倉」（2026-08-30 使用者轉來的 Binance 案例）**：統一
  帳戶裡留著平倉後的 USDT 負債，抵押品（RLUSD 折 90%、SOL 折 80%）用
  「最大」轉出後，折算抵押只比負債多 0.6u，任何一個 tick 就強平；查倉位
  歷史看不到任何倉。對本線的規格含義：(1) 對帳要讀**餘額與負債**，不只讀
  倉位——「查無倉位」對負債是瞎的（同 mistake 07-28「DB 只記自己下的單」）；
  (2) **任何資金劃轉都是風險事件**：轉出前自己算轉出後的折算保證金率
  （含各資產折價），留緩衝，不信交易所的「允許轉出」；永不點「最大」；
  (3) 保證金一律用場館的報價穩定幣（USDC/USDG），不用計息的替代穩定幣
  當抵押；(4) 產品端跟單用戶同樣適用——「0 倉位 ≠ 0 風險」要進風控規格。
  **通則（08-31 補，作者的機制拆解證實）**：uniMMR 的分母只算持倉維持保證金
  ＋利息 5%，沒倉位時塌到 0.137u，「轉出後 ≥1.2」的守衛實際只留 0.16u 緩衝
  去對 23,000u 抵押品——**守衛量錯了分母**（同族：08-08 解碼參考分佈≠被判斷
  分佈）。**任何比值型安全門檻，先問分母會不會在某個正常狀態下塌到零**；會的
  話該門檻在那個狀態等於不存在。本線接兩個保證金帳戶時，「淨曝險／保證金」
  在一腿平掉後分母同樣會塌，設計要照此寫。

**錄製已上線（2026-08-28 18:30 起）＋判準先凍結**

- **場館發現**：Entropy 不是 crypto 所——上市 6 個標的全是**股票/私募永續**
  （io:OAI＝OpenAI、io:ANTH＝Anthropic、SNDK、IONQ、NBIS、EWY）。
  「盤後預言機制度不同」是溢價主要來源；OAI/ANTH 連公開市價的錨都沒有
- **首錄配對**：SNDK vs lighter-rh（README 自己的例子；兩邊 taker 0 bps
  **當場驗證為真**）。首兩分鐘：溢價 mean −1.4 bps、std 0.6、buy 側瞬間
  可成交空間最高 +1.38 bps——**中線不在零**，README 的頭號警告當場應驗
- **耐久化**：`run_recorder.bat`（30 秒重啟迴圈）＋使用者啟動資料夾
  （schtasks 要管理員權限，改走 Startup）＋ freshness board 監控
  （1h 上限——**靜默斷錄 = 一週資料白等**）
- **本機補丁**（clone 內，未推遠端）：6 處 `open()` 補 `encoding="utf-8"`
  （cp950 家族）、Windows 無 `add_signal_handler` 的 fallback
- **判準（跑滿 7 天之前凍結，不看資料調）**：錄滿 ≥7 個完整天後，
  存在一個扣費後往返 ≥1 bps 的門檻帶、平均每天觸發 ≥10 次、且
  **前後兩半皆成立** → 進工程閘門討論；否則本線關閉。
  帶寬不掃描——用 analyze.py 的 p90 建議值起步

**判準修訂 v2（2026-08-28 22:40，已看 230 分鐘＝窗口的 3%；只加嚴不放寬）**：
使用者問「有沒有機會」時發現 buy 側 94% 的分鐘都有帳面肉——這不是好消息，
是**中線偏移**的症狀（永遠在的價差賺不到，要收斂才落袋）。而原判準只量
「帶存在＋開火頻率」，量不到「會不會收斂」，也量不到「肥的那刻盤口有沒有
量／是不是卡價」。三項修法，全部在判決程式裡寫死：
1. **加第四關「收斂」**：溢價偏離滾動中線（6h 中位數）超過帶之後，
   ≥70% 的事件要在 240 分鐘內收斂回帶的一半以內——過不了 = 帳面肉是
   結構性偏移，不論多肥都關線
2. **錄價程式補儀器**（本機補丁 v2）：新增 8 欄——最肥時刻兩腿頂檔
   名目的較小值（能吃到多少錢）＋該刻較舊一邊的報價年齡（>5s = 卡價
   嫌疑）。舊 230 分鐘無此欄位，只從量/卡價統計中排除，不從判準排除
3. 深度/卡價統計**列報告不進判準**（供工程閘門評估）——數字是這次才
   開始錄的，拿它當關卡等於在 3% 資料上發明門檻

**階段閘門（依 CLAUDE.md 既有紀律）**
1. **現在可做**：`--record-only` 錄兩邊掛單簿（零憑證、零錢、零風險）
   → `analyze.py` 量扣費後溢價分佈 → 有肉/沒肉,一兩週結案
2. **接錢包之前**：走統一風控框架的門（兩層 kill/中央曝險帳本——
   「第二條策略上線必須先有統一風控」對第四條同樣適用）,且下單路徑逐行審
3. 排序：**在 §0.59/Gate F/地形扳機結算之前不投入工程**,只允許錄資料
   （錄資料越早越好,因為分析需要的就是時間長度）

**錄製家族（2026-08-30 登記；使用者：「有很多標的為什麼只錄一個」）**

08-28 只錄 SNDK 是因為對 Entropy 六個標的做**同名比對**只對到它；重查發現
Lighter 的代號不同（`OAI`→`OPENAI`、`ANTH`→`ANTHROPIC`），加上 HL 主網
本身可當一腿（加密標的）。錄價程式補了兩個本機補丁（`hedge.symbol`
別名、`entropy.dex=""` 走 HL core），每配對獨立設定檔／log 目錄／
30 秒重啟迴圈／啟動資料夾項目，freshness board 逐條 1h 上限。

| 配對 | 腿 A | 腿 B | 定位 | 起錄 |
|---|---|---|---|---|
| SNDK | Entropy io:SNDK | lighter-rh SNDK | 原始 | 08-28 18:28 |
| NBIS | Entropy io:NBIS | lighter NBIS | 股票永續 | 08-30 ~23:00 |
| ANTH | Entropy io:ANTH | lighter-rh ANTHROPIC | 私募永續、無現貨錨 | 08-30 ~23:00 |
| **BTC** | HL BTC | lighter-rh BTC | **對照組**：兩邊深簿零費率，帶應≈0；它一肥就是儀器壞了（mistake 07-29） | 08-30 ~23:00 |
| ZEC | HL ZEC | lighter-rh ZEC | 薄加密 | 08-30 ~23:00 |
| NEAR | HL NEAR | lighter-rh NEAR | 薄加密 | 08-30 ~23:00 |

**io:OAI、io:EWY 首次連線即 `isDelisted`**（meta 仍列但已下市）——登記
當晚就掉兩個，**下市風險是這類場館的實際風險**，記入風險清單。io:IONQ
兩邊都沒有對沖腿。

**紀律**：(1) **同一套凍結判準（四關）逐配對套用，全部報告、不挑**——
六個裡挑最好看的上，就是變體 C/D；(2) 各配對從自己的首列起算 7 天，
SNDK 照舊 09-04；(3) 判決程式（`premium_verdict.py`）已改逐配對，新增
**「可捕獲美元／天」報告欄**＝次數／天 × 帶寬 × 最肥時刻深度——**只報告
不進判準**，用途是「過閘之後值不值得投工程」（SNDK 期中 ≈ $12–16/天，
就是為此登記的）；(4) BTC 若過閘 → 判「對照組亮了」，先查儀器。

**§0.75b 掃描器（2026-08-30 登記，使用者：「新幣／熱門迷因機會大，但需要
搜尋篩選機制」）**：獨立於錄價程式的 REST 掃描（`../entropy-arb/tools/
scanner.py`，每 ~2 分鐘一輪）掃 HL 主網 ∩ Lighter 主網／RH 的全部配對：
兩邊最佳買賣、頂檔美元深度、24h 量、上市日；輸出 `logs/scan/scan_*.csv`
與新上市／下市事件 `listings.csv`。**排名指標先凍結**（`research/arb/
scan_rank.py`）：逐配對逐側 band=p90 正向可成交空間、fires=≥band 的次數
／天、depth=那些時刻 min(兩邊頂檔) 的中位 → **可捕獲美元／天 = fires ×
band × depth**，取兩側較大者排名。掃描滿 ≥3 天且 ≥500 樣本才出名單；
上市 <48h 不列；前 3 名升格到完整錄價，**升格之後的資料才進判準**
（選擇用一段資料、驗證用另一段——防「掃描器變成生產漂亮假象的機器」）。
BTC 永遠在名單當對照。

**與主體的關係（使用者 2026-08-30 定調）**：「除了流動性獵取跟 V7，其他
都是附加價值，這兩個才是主體」——本線維持「只錄不做、判決日看數字」，
不佔研究線的預註冊／統計資源；掃描器與錄製家族是零風險的資料累積，
不是投入。

---

### 1.00 掃描器擴編到非加密（2026-09-03 使用者：「掃描看看其他鏈上的股票期貨黃金」）——瓶頸是深度，而深度在別的場館

**為什麼是這件事**：§0.75 的期中數字顯示這條線的天花板不是價差、是
**對手簿的深度**——SNDK 最肥時刻的頂檔中位只有 **$264 / $288**，
帶再寬也只換得到零錢。所以擴編掃描不是「找更多機會」，是**去有深度的
地方找同一個機會**。

**查到的事實（2026-09-03，`perpDexs` + `metaAndAssetCtxs`）**：
Hyperliquid 上有 **11 個 perp dex**，掃描器只掃了其中一個（Entropy `io`，
5 個標的、24h $57M）。真正大的那個是 **`xyz`：24h $2.24B**，掛著
**黃金、白銀、銅、鉑、鈀、WTI（CL）、布蘭特、天然氣、鈾、SP500、
JP225、KR200、EUR/GBP/JPY**，加上約 90 檔個股。另外 `para`（$5.9M，
含 2Y/10Y/30Y 殖利率）、`mkts`（$8.1M，指數）也有量。

**同時查到 5 個 dex 的 24h 量是 $0、簿口全空**（flx / vntl / km / abcd /
cash）——它們掛著 GOLD、USA500、WTI 這些名字，但**沒有任何一筆報價**。
這正是「名單上有 ≠ 交易得到」，所以擴編的第一道守衛就是量。

**改了什麼（`../arb/engine/tools/scanner.py` v3，純收集端）**
1. 場館 = HL 主場 + **每一個 builder dex** + 兩條 Lighter 鏈
2. 配對 = 任何被 **≥2 個場館**掛出的同一標的，**包含 dex 對 dex**
   （同一條鏈、不用跨鏈搬錢——執行難度比跨場館低一個等級）
3. 兩道便宜的儀器守衛（廣度會放大假數字的種類）：
   - **24h 量為 0 的標的不當腿**
   - **尺度守衛**：兩腿中價差距超過 2 倍就跳過並記一筆——那是
     單位不同（指數 vs ETF，如 SP500 vs SPY），不是價差。
     ETF 對指數的別名（SPY/SP500、QQQ/XYZ100、USO/CL、SLV/SILVER）
     **刻意不建別名**，只認同一標的（GOLD↔XAU、SILVER↔XAG 除外）

**結果：配對從 113 → 255，其中 93 個碰到股票／商品場館。** 首輪快照
（單一 cycle，只是形狀不是判決）：

| 配對 | 兩側最佳 | 頂檔深度（美元） |
|---|---|---|
| GOLD@xyz-lighter | 4.65 bps | **a $35,079 / b $12,489** |
| SNDK@xyz-lighter | 5.18 bps | **a $37,428** / b $5,949 |
| SILVER@xyz-lighter | 8.61 bps | a $3,744 / b $819 |
| NATGAS@xyz-lighter | 20.41 bps | a $1,478 / b $14,997 |
| BRENTOIL@xyz-lighter | 1.48 bps | a $1,976 / b $5,006 |
| NVDA@xyz-lighter-rh | 8.42 bps | a $1,208 / b $5,807 |

**深度比原配對高一到兩個數量級**（$264 → $1k–37k）。可捕獲金額
= 次數 × 帶寬 × 深度，所以這是唯一能把「每天幾十塊」變成別的量級的
那個因子。

**紀律（沒有任何一條被放寬）**
- 升格指標（`arb/arblib/scan_rank.py`：次數／天 × 帶寬 × 深度）
  **2026-08-30 凍結，這次一個字沒動**；新配對照樣要 ≥3 天跨度、
  ≥500 筆才進名單，BTC 對照組的帶寬仍是雜訊底（2×）
- 升格 → 進錄製家族 → **自己的 7 天時鐘**，而且**收斂關**照跑：
  BTC 對照組就是靠它擋下來的（$97.8/天但 episodes=0＝永遠不回來）。
  首輪快照裡 `xyz-para` 那批兩側都是 −20~−46 bps 的持續偏移，
  **形狀跟 BTC 一模一樣**，八成會死在收斂關——這是預測，寫在這裡等打臉
- 掃描期的資料**不進判決**（選擇窗 ≠ 驗證窗）

**還沒查、進場前必須查的**：HIP-3 builder dex 的**手續費**。現有家族
的零費率是 Entropy/Lighter 的獲客補貼（§0.75 已註明有內建過期日），
**不可假設 `xyz`/`para` 也是 0**——費率是分子的直接扣項，沒查證前
任何「可捕獲美元」都只是上限。

- [x] 掃描器 v3 上線、smoke 一輪 236/255 配對成功、35s/cycle（限額內）
- [x] 舊程序停掉、新程序接手（`run_scanner.bat`）
- [x] **上牆**（2026-09-03，使用者：「上線到我的 flowbot 網站方便管理」）：
      `arb_publish.py` 加 `scan` 區塊 → `/public/arb-status` → 網站
      `/charts/arb` 的「戰場掃描」盤。排序用的可捕獲美元不出公開面，
      只有它產生的順序出；深度照舊只給分級
- [x] **五階梯寫在板上**（使用者：「回測驗證完還要上實盤驗證後才算真」）：
      掃描（本板）→ 錄製 7 天 → 收斂關 → 費率查證 → 小額實盤。
      後四關全部標「未做」——板上任何一列只過了第一關
> **掃描帶 vs 錄製帶：同一個配對差到 11 倍（2026-09-03 量測，寫下來以免
> 有人拿掃描數字當收益）**。掃描盤的帶是「**只取正的那些樣本**」的 p90
> （08-30 註冊時就這樣寫），錄製端的帶是**全部分鐘**的 p90。某一側越少
> 為正，兩者差越多：BTC 買側只有 **12.6%** 的樣本為正 → p90(全部) **0.449**
> vs p90(只取正) **5.105 bps**。把修正算式套到掃描盤上會得到「合計
> $333/天」這種數字，**它不能當收益讀**——升格後用錄製端的定義重量一次
> 多半會大幅縮水。這不是掃描盤壞了（它是排名不是判決），是階梯存在的理由。
> 不因此修改凍結的升格指標；偏誤方向已記錄，判決端本來就更嚴。

- [ ] 3 天後（~09-06）跑 `scan_rank.py`，看非加密配對能不能擠進前段
- [x] **費率查證完成（2026-09-03）——這一關把整條線的答案改了**：
      HL 官方文件 taker **0.045%（4.5 bps）**、maker 0.015%；HIP-3 部署者
      可再加 0–300% 的費用分成（growth mode 0–100%）。查 `meta` 的
      `deployerFeeScale`：**io / xyz / para / mkts 全部是 1.0 + growth
      mode** → **交易者付的是 HL 標準費率，builder dex 不是零費**。
      Lighter 相反：標準帳戶**排程寫死 0 maker / 0 taker**。
      所以 §0.75 家族的「兩邊 taker 0 bps」不是費率表，是 **Entropy 的
      推薦返佣 100%**（上游 README 自己寫 Tier 4 referral, 100% rebates）
      ——**促銷，不是結構**，而 §0.75 登記時就寫過它有內建過期日。
      後果：一趟來回在 HL 腿要付 **2×4.5 = 9 bps**，所以需要
      **帶 > 18 bps** 才有得賺；黃金的帶是 4.65 bps。**沒有返佣，深度
      100 倍的戰場一樣是負的。**
- [x] **CEX 兩腿接進掃描（2026-09-03，使用者：「我 CEX 都有 50% 返佣而且
      那些商品他們也有」）**。查證屬實：OKX 有 XAG/SPY/QQQ/NVDA/TSLA/AAPL/
      COIN/MSTR/SPX，Bitget 另有 **XAUT/PAXG（代幣化黃金）、COPPER、
      NDX100**。Bitget 的合約端點**自己公布費率**（XAUT maker 2 / taker
      6 bps），已存進 `universe.json`。
      **50% 返佣把門檻換了一格**：CEX 腿約 2.5–3 bps，配零費的 Lighter →
      需要帶 **>10–12 bps**（對照:未返佣的 HL 腿要 18 bps）。
      掃描器 v4：配對 **255 → 1,629**、9 個場館、每輪 66 秒（週期改 180 秒）。
      首輪非加密快照：SILVER@xyz-okx 16.1 bps（OKX 側 **$224k**）、
      SPY@lighter-okx 14.1 bps（Lighter 側 **$500k**）、GOLD@xyz-bitget
      20.0、COPPER@xyz-bitget 14.8。**深度比原家族高三個數量級。**
      同時修了一個靜默降級：某個場館的 universe 抓失敗會讓配對數從 1,629
      掉到 1,068 而只留一行 log → 改成沿用上一份好的（`_keep`）。
      **警告**：這些是單一快照不是 p90，而且 xyz↔CEX 之間的價差很可能有
      結構性成分（USDT vs USDC、預言機、股票收盤時段），跟 USDG 基差同型
      ——**收斂關是唯一能分辨的東西**，別拿快照當收益。
- [x] **讀了錄價程式作者自己的實戰紀錄（2026-09-03，使用者轉貼；edgeX 60 天）
      ——三件事直接打到我們的算式**：
      1. **他不吃單，他掛單**。文中列了三種執行法（掛 A 成交→吃 B；掛 A→
         試掛 B→不成交才吃；兩腿都掛→一邊成交立刻取消另一邊改吃）。
         我們一路假設「四次吃單」＝最保守的那一格。改成一腿掛單後
         **Lighter×Bitget 的門檻從 12 bps 掉到 4 bps**、xyz×Lighter 從 18
         到 6。重算 3.75 天掃描資料：存活從 **3 側／$6.40** 變成
         **58 側／$50.65 上限**。`fees.py` 已加 maker 費率與三種執行模式，
         **預設仍是四次吃單**（保守值不動，掛單是選項不是假設）。
      2. **他點名「底層資產必須完全一致」**，並舉例 USDT vs USDC 的價差會
         偽裝成跨所價差——**正是我們對 USDG（lighter-rh）與 XAUT／PAXG
         的警告**，獨立佐證。
      3. **他監控訂單簿的每次變化，不是 K 線**。我們是 2 分鐘快照／1 分鐘
         bar，差兩個數量級。升格的配對要開 websocket。
      其餘可抄的：每筆成交後自動對兩邊倉位、監控「預期成交價 vs 實際成交價」
      偏離、近 X 分鐘損益異常告警、IP 白名單＋金鑰登入、TG bot 一鍵減倉／
      暫停。伺服器建議東京 AWS（多數交易所在那裡）。
- [x] **掃描器 v5：累積深度（使用者核准）**。頂檔深度是唯一被記錄的規模，
      而它**嚴重低估且不均勻**：實測 3 bps 內是頂檔的 **4–5 倍**（中位），
      極端如 `BTC@lighter` 頂檔 **$25.6**、1 bps 內 **$37,194**（**1450 倍**）
      ——Lighter 的簿口在觸價只掛很小、量在後面。這會系統性地把 Lighter
      腿的配對排到後面去。現在每列多記 8 欄（兩腿×買賣×1bps/3bps）。
      寫進**新檔名** `scan_v5_YYYYMMDD.csv`（不在既有檔中途換 header——
      那會讓之後每一列錯位；`scan_*.csv` 的 glob 兩個都收得到，無資料損失）。
      **凍結的升格指標不動**，仍讀頂檔；新欄位只給容量估計用。
- [x] **全線復審（2026-09-03，使用者：「重新審視一下整個策略有沒有 bug」）
      ——找到一個真 bug，其餘是已知假設**：
      1. **真 bug：Bitget 的深度被除以 10,000**。掃描器把合約端點的
         `sizeMultiplier`（0.0001 for BTC、0.01 for XAUT）當成合約面值，
         但 Bitget 的 USDT 永續**簿口本來就以幣為單位**（實測 BTCUSDT
         頂檔 1.8754 = 1.8754 顆 = **$145,718**）。OKX 相反，它的量是
         **張數**、`ctVal` 才是每張幣數（實測 0.01，用法正確）。
         後果：每一個 Bitget 腿的簿口都被記成空的，而**深度是升格指標
         的乘數**——等於把剛加進來的 CEX 腿自己排到最後面，正是加它們
         的相反。修法 `mult=1.0`，並在 `scan_rank.load()` 丟掉修正前
         （ts < 1788432000）的 38,302 筆 Bitget 列——**這是儀器修正，
         凍結的指標算式一個字沒動**。修正後 XAUT 3 bps 內深度
         **$346k–563k**（原本會記成 $3.5k–5.6k）。
      1b. **last-good 場館清單改成落地檔＋抓取重試三次**，並**反向證明過**
         （讓 `lighter_universe` 必定拋例外：有快取時 1,632 配對、Lighter
         兩條鏈的 216+46 個標的照樣在；清空快取重跑掉到 945 配對、兩條鏈
         歸零 → 測試確實在測東西）。原本快取只在記憶體，而程序由 bat 重啟
         迴圈管理，重啟撞上 Lighter 報錯就整個 refresh 間隔少一個場館。
         注意：**現正在跑的那個程序還沒走過「從檔案載入」那條路**（它啟動
         時檔案還不存在），下一次重啟才會第一次執行到。
      2. **順手清掉重複的掃描程序**：`run_scanner.bat` 有重啟迴圈，
         08-30 與 09-03 各有一個 cmd 迴圈在跑 → 兩個掃描器同時打同一組
         公開 API（先前的限流就是這樣來的）。現在只剩一條。
      3. **不是 bug，是已知假設，但要看見**：掃描盤現在的前兩名是
         `OAI@IO-lighter*`，帶 **428/436 bps**、簿口 **$89/$61**。
         四位數 bps 的「價差」配上百元的簿口，形狀跟 BTC 對照組
         09-01 那次（$97.8/天、episodes=0）一模一樣——**pre-IPO 標的
         在不同場館根本是不同的標記，不是會回來的價差**。掃描盤沒有
         收斂關（階梯第三格寫著「未做」），所以它會、也應該把這種東西
         排在前面；擋它的是升格後的 7 天錄製與收斂關。**預測寫在這裡：
         OAI 兩列會死在收斂關。**
      4. **控制組再次亮燈，且這次是 CEX 腿**：`BTC@HL-okx` 帶 3.31 bps、
         簿口 $46k → 排名指標 $571/天，是全板最大。BTC 對照組**依定義
         不可升格**，但它的帶就是雜訊底：CEX 腿的雜訊底是 3–5 bps，
         所以 CEX 配對要 >16 bps（2× 全體 BTC 最大帶 8.0）才算數。
      5. **查過沒問題的**：OKX `ctVal` 用法、Bitget `limit=25`（它自己
         回 100 檔，`_cum` 反正在 bps 界線就停）、`_legs()` 對
         `lighter-rh` 這種帶連字號的場館名解析正確、公開面（`arb_publish`
         → 端點 → 網站）沒有任何美元數字、`scan_v5_*.csv` 有被 glob 收到、
         每小時班車三支都在跑（premium_verdict → scan_rank → arb_publish）。
      6. **仍是假設不是事實（不動，但別忘）**：`IO` 的 100% 返佣、OKX 的
         標準 taker 費率、掛單成交率、收斂關用的是「均價偏離自己的中線」
         而帶來自「可成交邊際」——後者比前者小，所以偏離次數偏樂觀。
- [x] **判決程式現在讀真實費率（2026-09-03，這一條在 09-04 判決之前必須做）**：
      凍結判準的括號寫著「**fees are 0+0 on this pair**, so net == raw
      executable room」——那句話在 09-03 被自己的費率查證推翻了。所以
      `premium_verdict.py` 改成 import `fees.py`（逐配對明確登記場館，
      不靠字串比對），每個配對印「需要帶 X bps（含返佣）／Y bps（只看
      費率表）」，每一側多四個欄位（兩種假設下的每筆淨值、`passed_at_fee`、
      未查證的腿）。**凍結的 1.0 bps 門檻一個字沒動**——判準沒被改，是
      它的前提被推翻了，所以把前提攤在旁邊，不是把門檻挪走。
      用已知答案強制走過判決路徑（記憶體內把 GATE_DAYS 改 1，不寫產物）：
      **SNDK 兩側都過凍結門檻，但那完全靠 IO 那條「100% 返佣」——照費率表
      每筆是 sell −7.99 / buy −5.08 bps**；BTC 對照組需要 18 bps 而帶只有
      6.65，**兩種假設下都是負的**。順手改正對照組的判決措辭（原文「兩邊
      深簿零費率」已是錯的：Lighter 是 0，HL 不是）。
      `arb_publish` 的錄製家族每一側也跟著帶費率現實上牆——先前只出帶寬，
      讀起來像「有空間」，而費率表可能早就吃掉它了。
- [x] **錄價全家死了 46 分鐘，看板抓到（2026-09-03 21:09→21:55 +0800）**：
      七個錄價程式與掃描器在 21:07~21:09 同時停止，`runner.log` 最後一行是
      **`^C`**——是中斷訊號，不是崩潰、不是網路。restart 迴圈連同 cmd 一起
      被帶走，所以自我修復沒有發生（迴圈本身死了）。**成因無法確定，而且
      不排除是本 session 自己的指令造成的**（同時段有多個前景／背景指令在
      同一個 console 跑；Windows 的 Ctrl+C 是送給整個 console 行程群組的）。
      照開機啟動項 `entropy-arb-record.bat` 原樣重啟（每支自己的 console
      視窗），驗證方式是產物：七支 minutes.csv 與 scan_v5 全部恢復每分鐘
      更新、看板九列全綠。**損失 46 分鐘資料，09-04 的 SNDK 判決照原判準
      讀（分鐘數少 46，占 7 天的 0.5%，不改判準）。**
      教訓兩條：(a) 30 秒 restart 迴圈只能救「子程序死」，救不了「整個
      console 被中斷」——耐久化的層級不夠；(b) **能抓到它的只有產物新鮮度
      看板**，而它的上限（錄價 1h、掃描 0.5h）剛好夠。這是 §0.55 那套
      守衛第一次在套利線上真的擋到東西。
- [x] **看門狗上線（2026-09-03）**：`arb/ops/arb_watchdog.ps1`
      ＋ Windows 排程 `EntropyArbWatchdog`（每 5 分鐘，與既有排程同款的
      互動模式）。它是 .bat 內 30 秒迴圈**上面那一層**：迴圈只能救「python
      子程序死」，救不了「整個 console 被帶走」（今天的死法）。規則：
      (a) 用**程序命令列**判活不用檔案 mtime——在活著的程序上再拉一份就是
      同日的雙掃描器限流 bug；(b) 重啟走與開機啟動項**同一支 .bat**，
      修復路徑＝開機路徑；(c) 每輪都寫一行 log（含「all 8 alive」），
      只在動作時才寫的看門狗在一切正常時看起來像死了。
      **反向證明**：殺掉 ZEC 的 cmd 迴圈＋python → 看門狗一輪只重啟 ZEC、
      八個成員各一份、再乾跑報 all alive；排程觸發一次 → log 落地、ZEC
      minutes.csv 恢復每分鐘寫。沒用「使用者未登入也執行」——那需要存密碼，
      而今天的死法是登入中的 console 中斷，不是登出。
- [x] **IO 返佣查證完畢——是 25%，不是 100%（2026-09-04，使用者 Entropy 推薦頁截圖）**：
      頁面寫 Benefit **25%**（自己交易的費率折扣）、Rebate 60%（推薦別人可分到的收入）。
      上游 README 的「Tier 4, 100% rebates」講的是**推薦人分到被推薦人手續費的比例**
      ——是收入不是折扣，兩個數字同一個名字。08-28 那句「兩邊 taker 0 bps 當場驗證
      為真」因此是錯的，來源要重查。`fees.py` IO 改 rebate 0.25、verified。後果：
      IO+Lighter 門檻吃單 **13.5** / 掛單 **4.5** bps；SNDK 開盤帶 6.34 → 吃單 −3.6 /
      掛單 **+0.9**；NBIS 14.5 → 吃單 +0.5 / 掛單 +5.0。**這家只剩掛單一條路**。
- [ ] **唯一能救這條線的問題**：xyz（或任何有深度的 builder dex）能不能
      拿到同等的返佣／做市商減免。拿得到 → 黃金/白銀/原油那批 $12k–35k
      的簿口值得錄；拿不到 → 這條線的天花板就是 io↔lighter 那幾個配對，
      每天幾塊錢
- [ ] 升格前查兩腿的**保證金是不是分開的帳戶**（HIP-3 dex 之間、
      以及 HL 主場 vs dex）——斷腿風險與資金效率都由這件事決定

### 1.13 預註冊：新聞/LLM 發現層 vs 掃描器（2026-09-04 登記，**未開工**）

全文 `arb/docs/DISCOVERY_VS_SCANNER.md`。使用者在評估「Grok bot 報全市場機會」
會不會比掃描器好、以及讀到錯誤資訊的風險。結論：**不是取代關係，而且風險大小完全
取決於接線位置**——接「發現」層錯了只花幾天無用錄製（下游接得住），接「執行」層
則繞過收斂關／深度關／成本七桶／凍結判準，而且**可被餵**（公開文字驅動下單＝任何人
都能貼餌）。**硬規則**：bot 輸出只能是問題不是答案、只能改掃描器的場館清單、
永不進下單路徑。

**兩週對照測試判準事前寫死**：D1 bot 報出而掃描器沒覆蓋的機會數（=0 → 不開）、
D2 其中經量測真的存在的比例（<50% → 不開）、D3 漏報數（記錄不當閘門）。
**開的條件 D1≥3 且 D2≥50%。** 預期 D1 不會是 0——Polymarket、rToken、HIP-3 oracle
都來自文字——但它們是「哪裡開了新戰場」不是「現在哪裡有 50 bps」。

### 1.12 HIP-3 與 Entropy oracle 設計對套利的意義（2026-09-04 使用者提問）

全文 `arb/docs/HIP3_ORACLE.md`。**HIP-3**＝任何人質押 **500k HYPE**（維持 183 天）
就能在 Hyperliquid 上部署自己的永續市場；前 3 個標的免拍賣、之後走荷蘭拍；
**部署者控制 oracle 價格、槓桿上限、合約規格與 haltTrading**，費用分成 0–300%。
**核心：撮合／清算／保證金引擎由 HL 提供，但「價格是什麼」由部署者定義**——
io／xyz／para／mkts 共用同一條鏈與風控，**各自有自己的 oracle**。

**使用者的觀察**（Entropy 依簿口深度動態調整「市場價 vs 外部價」的權重 → 流動性越好
市場定價權越高 → mark-oracle 偏離小 → funding 低）**方向對但證據薄**。實測各 dex
|funding| 中位：io **0.208**、mkts 0.000、xyz 0.500、HL core 1.000、para 5.124 bps/8h——
但 n=7 對 n=233、資產類別不同，不可比。乾淨檢定是**同標的跨 dex**（25 個重疊）：
io 唯一有 OI 的重疊標的 ANTH funding **+0.012**（極低），但可比樣本只有一個；
para（OI 也小）反而最高 → **funding 高低混著流動性與 OI，不只是 oracle 設計**。

**對套利的兩面**：
- **正面**：低 funding ＝ 持有型套利的第 4 桶成本低。**但對我們現在做的沒意義**——
  §1.07 已量過，5–15 分鐘的持有時間下 carry 進不了帳。要用它必須先把持有拉到以天計，
  那是另一本風險簿。
- **反面且更重要**：資金費率是**把 mark 拉回 oracle 的機制**。oracle 若貼著自己的簿口，
  價格偏離外部世界時**沒有力量把它拉回來**——**funding 低不是因為價格對，是因為它把
  自己的價格當成對的**。後果：**io 上的價差更可能是結構性偏移而非可收斂價差**。
  而我們的收斂關量的是「回到自己的 6h 中線」——**兩個場館一起漂時，收斂關會過但錢
  賺不到**。io 家族現在收斂 98–100% 看起來很好，但那是相對自己的中線。
  **要區分「真收斂」與「跟著漂」需要外部錨**（§1.10 那條腿）。

- [ ] 掃描器加錄逐 dex 的 funding 與 mark-oracle 偏離（純收集端，meta 本來就在讀）
- [ ] 外部錨——沒有它分不出真收斂與一起漂

### 1.11 Bitget rToken：同所現貨-永續（2026-09-04 使用者提問，當場量測）

全文 `arb/docs/RTOKEN.md`。**有幫助，但幫的地方不是「多一個場館」，是三個成本桶
同時消失**：每一檔 rToken 在 Bitget 都有同名股票永續（rNVDA ↔ NVDAUSDT），
**同場館現貨-永續**配對 → 第 5 桶轉移歸零（同一帳戶）、第 3 桶資金由 UTA 跨資產保證金
吸收（rToken 本身可當抵押、折算率最高 95%）、第 7 桶的 USDG 脫鉤歸零（同 USDT 計價）。
**第 3 桶那個「$300 緩衝隱含 size ≥$200」的兩難在這裡不存在**——持倉本身就是保證金。

⚠ **當場擋掉一個假數字**：ticker 說 rSPY 24h 量 **$29.6B**、rNVDA $23.6B，而同交易所
BTCUSDT 現貨只有 $424M——**一檔代幣化 ETF 做 BTC 的 70 倍不可能**，八成是做市激勵刷量。
成交量不可信，只看簿口。實測簿口：

| 標的 | 現貨價差 | 現貨 3bps 深度(買/賣) | 永續 3bps 深度 | 現貨vs永續中價差 |
|---|---|---|---|---|
| rSPY | 22.8 bps | $28k / $48k | $4.6M / $1.5M | **+8.7 bps** |
| rNVDA | 1.7 | **$0.5k** / $98k | $151k / $648k | +4.1 |
| rTSLA | 2.2 | **$0.4k** / $10k | $754k / $315k | +5.3 |
| rAAPL | 21.9 | $0.7k / $1.9k | $269k / $567k | −3.2 |
| rMSTR | 16.9 | **$19** / $1.8k | $42k / $249k | +11.6 |

**三個讀法**：(a) 中價差 3–12 bps 跟現有家族同量級，**沒有比較肥**；(b) **容量由
rToken 那側決定**，現貨深度比永續薄 2–3 個數量級且極度不對稱（rMSTR 買側 $19）；
(c) 現貨自己的買賣價差（rSPY 22.8、rAAPL 21.9）**大於中價差**——**只能掛單不能吃單**。

**收益其實是三層**：基差（要收斂才拿得到）＋資金費＋**股利**（扣 30% 稅後入帳，
加密永續沒有這層）。第三層是這個結構獨有、也最需要查證的：空永續那側要不要付
對應股利，決定它是不是結構性正 carry。

**風險是換形狀不是變小**：兩個場館的風險變成**單一交易所的集中風險**（Bitget／
Reality／UTA 規則一起中，機率低但沒有分散）；官方風險揭露自己寫「週末/盤後價格跳空
可能快速突破追保門檻」與「UTA 跨資產虧損可疊加」——**後者正是 09-03「500 萬 U 一夜
爆倉」的機制**。

- [ ] Bitget 現貨 rToken 接進掃描器（`bitget-spot`），與既有 Bitget 股票永續自動配對，
      走既有凍結指標與**收斂關**
- [ ] 查證：空永續那側要不要付股利（決定第三層收益是否存在）
- [ ] **預期先寫下來**：多半是**結構性偏移**而非可收斂價差（現貨貼著永續下方＝資金
      成本），那樣它就不是套利而是 cash-and-carry 持有型，判準要換一套（§1.07 教訓）

### 1.10 候選賽道：鏈上股票代幣 vs 真實股價（2026-09-04 使用者提案，登記不開工）

全文 `arb/docs/TOKENIZED_EQUITY.md`。**登記為候選、不預註冊、不開時鐘**，因為它有
一個結構性問題必須先回答：**你是不是 AP（授權參與者）**。是 AP（完成 Backed KYC、能按
NAV 申贖）→ 收斂是**合約義務**，那是真套利；不是 AP → 只是押注別人會把它套回去，
跟我們現在做的同一種形狀，而且多了 gas 與 AMM 滑價。**第一步不是寫程式，是問
Backed／Ondo／Dinari 散戶能不能申贖、門檻多少。**

已查證：xStocks 是 SPL 代幣、1:1 真股託管、可向 Backed 按 NAV 贖回但**限 onboarded
KYC 客戶**；流動性集中在 TSLAx／NVDAx／CRCLx，Raydium/Jupiter **建議滑價容忍
0.1–0.5%（10–50 bps）——已經比我們家族的帶寬一個數量級**；美股開盤時價差被壓緊，
**週末盤後才打開**（但那是跨週末持倉風險，不是套利，§1.01 在股票永續上量過同一件事）。

**不是 AP 的退路**：用**股票永續當對沖腿**（那 188 個 TradFi 永續已在掃描器裡），
兩條腿都 24/7、不必等開盤、收斂不依賴發行商。最小第一步是把 **Jupiter swap 報價**
當一個新場館接進掃描器（只讀），自動走既有凍結指標與收斂關，**不需要新判準**。
**先決條件**：`tokens.jup.ag` 今天從本機 DNS 解析失敗——跟 Polymarket 同款嫌疑，
要東京 VPS 才能確認。

- [ ] 問發行商：散戶申贖門檻（這一題的答案決定要不要投工程）
- [ ] Jupiter 報價接進掃描器（要 VPS 才測得了連通性）

### 1.09 未完成工作交接（2026-09-04 收工）

**全文 `arb/docs/NEXT_SESSION.md`**——分成四塊：**A 卡在使用者身上**（7 項：M1 下單、
HL 位址、CEX 唯讀 key、東京 VPS、邀請碼折扣、xyz 返佣談判、保證金是否分帳戶）、
**B 我可以做的 10 項依序**（B1 逐行審現有下單路徑 → B2 憑證缺口 → B3 補 maker 路徑 →
B4 五個 kill switch → B5 控制通道 → B6 儀表板 → B7 成本假設換量測 → B8 掃描器兩個
儀器缺陷 → B9 公告監看 → B10 §1.04 未排期項）、**C 自己會跑的時鐘 8 條**、
**D 已定案不要做的 5 條**。

**下一個 session 的第一件事**：讀 `NEXT_SESSION.md` 的 A 段——那幾件不做，B 的大部分
做不下去。B1（逐行審 `send_taker`，特別是未決成交那段）不需要任何憑證，隨時可開始。

### 1.08 套利·$50 一週小額實盤規格（2026-09-04 凍結，使用者「可以」）＋ 儀表板架構

規格全文 `arb/docs/LIVE_50U_SPEC.md`。**這一週的產出不是損益，是把成本名冊裡的
假設換成量測值**——七件事各一個事前判準（M1 帳單費率／M2 掛單成交率／M3 逆選擇／
M4 兩腿延遲／M5 資料過期就停／M6 存貨漂移／M7 劃轉成本），收工用量到的值重跑
`cost_model.py`，看「量到%」從 43% 升到多少。範圍鎖死：只做 NBIS 與 NVDA_LL 兩個
配對、每所 $50、總曝險 $300、**絕不重平衡**（$1 提幣費攤在 $50 上是 2%）、1× 槓桿
（清算那一桶消失）。最小下單查證：OKX 股票永續 $1.9–7.7、Bitget $5、Binance $5、
HL $10——**$50 開得了單**。

**速度：實測本機到各場館往返（各 5 次中位）**——Lighter 32ms、Lighter-RH 65、
Binance 65、HL 80、Bitget 88、OKX 95。三個結論：(a) 機會壽命是**分鐘**（收斂中位
5–15 分），進場慢 100ms 不會錯過；(b) **真正吃延遲的是撤單**——掛單模式下價格動了
要在被吃掉前撤掉，這正是 M3 要量的逆選擇；(c) **Railway 沒有東京區**（US/EU/新加坡），
而**沒有任何一區同時靠近兩條腿**（新加坡近 HL/OKX、遠 Lighter 系；美國相反）。
→ **執行引擎不放 Railway**，用 $8–20/月的東京 VPS，順便解 Polymarket 的 DNS 封鎖。

**儀表板放 Railway 沒問題，但只顯示不執行**，沿用本 repo 既有的單向流
（`agent-boundary.md`）：東京 VPS 引擎 → 每 30 秒寫 `arb_live_*` 表 → agent-mcp
`/public/arb-live` → jarvis 頁面。產品端一行交易程式都不寫，跟現在的
`/public/arb-status` 完全同款。頁面內容全部是引擎已經在算的東西（帶／扣七桶淨／
深度分級／兩腿時間差／掛單成交撤單／成交後漂移／各所餘額偏斜／守衛紅燈），
公開面規則照舊：只出百分比、方向、時間。

- [ ] 東京 VPS 開機（X1，$8–20/月）——執行引擎與 Polymarket 掃描都卡在這
- [x] **M1 的讀取端已就緒**（`research/arb/fee_receipts.py`，2026-09-04）：讀四個場館
      過去 N 小時的成交回執，算**帳單上實際付的 bps**（分 maker/taker），與 `fees.py`
      並列，差 >0.5 bps 就標「以回執為準改 fees.py」。**這支只讀，沒有任何下單或
      簽單程式碼**——下單是操作者的動作。`HL_ACCOUNT_ADDRESS` 一設就能讀（HL 的
      `userFills` 是公開的，不需要金鑰）；CEX 要唯讀 API key。
- [ ] **M1 執行（2026-09-04 使用者：「今晚我來搞全部交易所註冊帳號」）**：
      清單 `research/arb/M1_CHECKLIST.md`；**交易所端開通步驟 `research/arb/ONBOARDING.md`**
      （每家要錢包還是 API key、批 A 唯讀先做／批 B 下單再開、Lighter 兩鏈要兩個帳號）。
      **§API 隔離規格已凍結**（2026-09-04 使用者要求）：四個維度——帳戶（獨立錢包／
      子帳戶）、金鑰（一把 key 一個用途，**HL 必須用 Agent 錢包不是主私鑰**，永不發
      提幣權限）、程序（交易 key 只在 VPS，儀表板唯讀不碰金鑰，不進 git 不進 Railway）、
      網路（VPS 固定 IP 綁白名單）。附 8 項上線前檢查表，**任何一項打不了勾就不下
      第一筆真單**。——註冊要點（子帳戶、只勾交易不勾提幣、
      HL 用獨立錢包、**Lighter 兩條鏈各一組**）、四筆最小單（OKX $2.3／Bitget $5／
      Binance $5／HL $10）、要貼給我的唯讀憑證，以及**同一晚可以順便做完的四項查證**
      （M7 劃轉成本、第 3 桶保證金率、A7 保證金是否分帳戶、rToken 抵押率）。
      四項一起做會把 `cost_model.py` 的「量到%」從 **43% 推到 ~70%**。
      ⚠ 用邀請碼時記得 §1.11 的教訓：**要問「我自己交易的折扣」（Benefit），不是
      「推薦別人的分成」（Rebate）**——Entropy 那次就是這兩個數字同名，錯了五天。
- [x] **B2 完成（2026-09-04）：兩條 Lighter 鏈各自的憑證**（entropy-arb 本機倉）。
      `_lighter_creds(venue)` 依部署選前綴，`LIGHTER_RH_*` 覆寫、未設則回退到
      `LIGHTER_*`——**既有單腿設定逐位元不變**。已知答案測試三種情境全過：
      兩腿分開（111/222 不同 key）、回退（RH 清空 → 111）、mainnet 腿不受影響。
      `.env.example` 補上新變數與中英說明。
      附註：上游 3 個 `test_engine` 失敗是 **pytest 事件迴圈隔離問題**（單獨跑會過、
      未改動的樹上也失敗），與本次無關，已查證。
- [ ] **憑證缺口（剩下的）**：(a) HL 要用 **API wallet**——能下單撤單
      但**永遠不能提幣**，把 DEX 腿的金鑰風險拉到跟 CEX 一樣（官方文件明載）；
      (b) **兩條 Lighter 鏈現在共用同一組憑證**（`config.py` 兩處都讀同樣的
      `LIGHTER_*`），而 mainnet 與 RH 是不同鏈不同帳號——`NVDA_LL` 兩腿都是 Lighter，
      **上線前必修**，錄價階段看不出來因為它不簽單；(c) OKX／Bitget／Binance 的下單
      認證**尚未實作**（現在只有公開簿口讀取）
- [x] **B1 逐行審下單路徑完成（2026-09-04）**：`research/arb/B1_ENGINE_AUDIT.md`。
      **核心設計是對的**——未決不假設沒成交、對帳只採信鏈上真相**不下修正單**、
      對沖前檢查簿口新鮮度、`reduce_only` 寫死、連續錯誤 HALT、`RECONCILE_GRACE_SEC=5`
      防「剛成交就對帳」的幻影震盪（作者註解直接寫了 Lighter REST 落後 ws）。
      **沒有找到「以為沒成交就重送」的邏輯**——那正是兄弟 $1.1M 的病灶。
      四個缺口：**G1 沒有累積差額硬上限**（兄弟參數裡的 `maxDelta: 800` 沒有對應物，
      現有的 cap_usd／reduce_only／consec_errors 都是間接防護，**這是五個 kill switch
      裡最該先加的**）、**G2** `settle_timeout` 逾時回 `filled_base=0` 是猜測，而
      `_execute` 先用了它才檢查 `unresolved`（對帳會修回來，但那之前本地部位是錯的，
      而 `_scan` 可能下新單）、**G3** 沒有 maker 路徑、**G4** 兩條 Lighter 鏈共用憑證。
- [x] **B4 第一個 kill switch 完成（2026-09-04）：`max_net_base`**（entropy-arb 本機倉
      `5ba9347`）。B1 的 G1——`_hedge` 只縮當下的差額，**沒有任何地方限制差額能長到
      多大**。修法：`risk.max_net_base`（預設 3×`net_tolerance_base`），在
      `_maybe_hedge` **開頭**檢查（因為失效形狀是「對沖一直失敗而差額一直長大」，
      這種必須自己停，不能等人發現），超過即 `halted=True` + critical log，
      **不下任何單、不碰既有部位**；HALT 單向，重啟時 `strict=True` 會重讀真實部位。
      順帶修 **G2**：`unresolved` 時 `filled_base` 是猜測（逾時回 0.0），加 warning
      說明本地部位在對帳前可能是舊的。
      **五種情境已知答案測試全過**：超限 HALT 且零下單／上限內照常對沖（無誤觸）／
      `cap=0` 停用時行為與改動前相同／容差內不動作／預設值＝3×tol。
- [x] **熱路徑審視（2026-09-04 使用者給的八條原則逐條對帳）**：
      `research/arb/HOTPATH_AUDIT.md`。**照第 8 條先量再說**——
      錄價 `writerow+flush` 中位 **6 µs**／p95 12／最大 139；風控 `sum(2 腿)` **0.5 µs**；
      單次 `log.info` **14 µs**（執行路徑上 6+ 個，合計 40–90 µs）。
      **相對 32–95 ms 的網路往返是 0.1%，而我們的決策週期是分鐘**——
      **結論：不需要動大手術**（第 8 條）。
      校準：**我們不是高頻系統**（收斂中位 5–15 分、掃描 180 秒），所以第 1 條
      「更快下單」部分適用——早已量過，吃延遲的是**撤單不是進場**。
      第 5 條**已合格且是設計出來的**：所有守衛都是 O(1)、不做 I/O 不呼叫交易所。
      第 4 條的「延遲預算」我們其實已有，只是刻度是秒（`staleness_sec=10`／
      `settle_timeout`／`reconcile_sec`／`venue_probe_sec`）；maker 上線要新增
      **撤單預算**（作者 repo 用 5 秒）。
      第 6 條有實測反例：flush 中位 6 µs 但**最大 139 µs（23 倍）**、OKX 中位 95 ms
      **最慢 161 ms**——**這正是 `staleness_sec` 這種絕對時限比平均延遲更該存在的理由**。
      **新缺口 G5**：錄價器與引擎**共用事件迴圈**且失敗路徑未驗證——asyncio task
      的例外若無人 await 會靜默死亡，引擎會繼續交易並以為錄價還在跑
      （mistake.md 2026-04-22「進程活著、功能死亡」那一族）。修法：done-callback
      設存活旗標並印在狀態行，**不停交易**（旁路壞不該停熱路徑）但要看得見。
- [x] **G5 ＋ B4 日損上限完成（2026-09-04，entropy-arb `248ff6b`）**：
      **G5**（原則 7 降級）錄價器 done-callback → critical log ＋ `_recorder_dead`
      旗標印在狀態行；**不停交易**（旁路壞不該停熱路徑），關機的正常取消不誤報。
      **B4 日損上限** `risk.max_daily_loss_usd`：session MTM 跌破即 HALT。
      所有硬停改走統一的 `_risk_halt()`，訊息格式一致、都會觸發對帳。
      **原則 5 已實測驗證**：兩個守衛合計 **1.9 µs**（p95 2.5），只做兩腿加總，
      不查 DB／不寫檔／不呼叫交易所。
      **六種情境已知答案測試全過**：跌破日損 HALT 零下單／未跌破不誤觸／停用時不動作／
      差額上限回歸／錄價拋例外升旗但**交易續行**／關機取消不誤報。
- [x] **B4 完結（2026-09-04，entropy-arb `2eb5d54`）**：`max_gross_usd`（兩腿名目
      合計的絕對上限，兜底設定打錯）、`max_consecutive_stale`（連續 N 次盤口過期即停機
      ——沒有這條「資料源死掉」和「行情安靜」長得一模一樣）、**HALT 語意改正**：
      原本停機後連對沖也停，斷腿的裸露會留在原地，而 `max_net_base` 那條特別自相矛盾
      （因為兩腿分家而停，然後停止把它們合回去）。改成 **HALT 只擋開新套利、
      仍允許 reduce-only 平掉既有裸露**，次數上限 `halt_flatten_attempts`（預設 3）
      避免失敗的對沖無限迴圈；設 0 = 完全凍結（可選）。
      五種情境測試全過：名目超標即停（差額為 0 也擋）／正常不誤觸／停機後平裸露三次
      即止／`flatten=0` 完全凍結／差額上限回歸。
- [x] **極端行情與斷腿的應變寫成文件**：`research/arb/CONTINGENCY.md`——逐情境寫
      **程式現在的實際行為**（不是設計意圖）＋11 條守衛全表＋$50 規模下的最壞情況。
      **誠實標出最弱的一環：沒有波動熔斷**（價格劇烈跳動但兩邊 book 都新鮮時照常交易），
      在 $50、1× 槓桿下不致命，**但放大前必須補**。
- [ ] **引擎缺的那一半：maker 路徑**（2026-09-04 查證）——錄價程式的
      `venue_hl.send_taker` / `venue_lighter.send_taker` 是完整的（IOC、reduce_only、
      成交輪詢、簽章），但**只有吃單**。而成本分析的結論是掛單才活得下來，所以引擎
      要補 post-only 下單＋撤單＋5 秒逾時。這是新的、碰錢的程式碼 → 上線前逐行審
      （CLAUDE.md「下單程式碼要逐行審」）
- [ ] `arb_live_*` 表與 `/public/arb-live` 端點（引擎有資料之後才做，避免空殼）
- [ ] 儀表板頁（jarvis）——端點有資料之後；**唯讀、一顆按鈕都沒有**
- [ ] 控制通道：TG 四個動詞 `pause`／`resume`／`flat`／`size`，冪等、不經儀表板、
      儀表板掛掉仍可用（規格 §6；理由是 mistake.md 2026-06-07 的 GET heal 疤）

### 1.07 套利·持有收租 vs 回半帶就平（2026-09-04 預註冊，使用者裁決「#8 可以試試看」；先 commit 再跑）

凍結規則與三個預測寫在 `research/arb/hold_carry.py` 檔頭：進場同計分器；資金費差對我們
有利時**續抱**，直到溢價穿過中線（吃整個偏離而非一半）、或 carry 翻向、或 24h 上限；
損益＝抓到的偏離＋收到的 carry −一趟來回費用。P1 ≥4 個配對持有優於基準、P2 拿掉最好
的一個仍成立、P3 持有中位 <8h（否則它是基差交易，要另外登記）。選擇窗探索，不是判決。

**結果（2026-09-04）：三關都過，但過的原因不是登記的那個——不開時鐘。**

| 配對 | n | 持有觸發 | 基準 | 持有 | 差 | 持有中位 |
|---|---|---|---|---|---|---|
| SNDK 賣 | 213 | 29 | −5.78 | −5.63 | +0.14 | 0.10h |
| NBIS 賣 | 22 | 3 | −2.40 | −0.99 | +1.41 | 0.20h |
| ANTH 買 | 6 | 1 | −0.96 | +0.04 | +1.00 | 0.23h |
| HYPE 買 | 12 | 9 | −6.80 | −5.49 | +1.32 | 0.19h |
| ZEC 賣 | 56 | 13 | −6.98 | −6.38 | +0.61 | 0.23h |
| BTC／NEAR／NVDA_LL | — | — | — | — | +0.17～+0.74 | 0.08–0.10h |

8/8 配對持有優於基準、拿掉最好的仍 ≥4、持有中位 **0.08h**——三關全過。**但持有中位
5–14 分鐘，這段時間的資金費幾乎是零**，「收租」不可能是增益的來源。照 2026-08-02
的規矩（符合預期的漂亮結果更要查儀器）做消融：**carry 歸零、出場規則不變**——

| | 全規則 | carry 歸零 |
|---|---|---|
| SNDK | +0.14 | +0.14 |
| NBIS | +1.41 | +0.88 |
| ANTH | +1.00 | +0.97 |
| HYPE | +1.32 | +1.29 |
| ZEC | +0.61 | +0.60 |

**增益幾乎全部來自「出場改在穿過中線」（吃整個偏離而非一半），不是 carry**。
carry 只在 NBIS 貢獻了 0.5 bps。所以登記的機制（持有收租）**沒有被支持**；被支持的是
另一件事：出場目標從半帶改成中線，值 +0.1～+1.4 bps，而且**沒有改變任何配對的正負**
（基準 −6～−8 的還是負的）。另註：資金費欄位 09-01 才開始錄，之前的事件全走基準，
所以「兩半」那一格第一半全是 0，沒有資訊。

**處置**：不開「持有收租」的前瞻時鐘（機制不對）。「出場改中線」若要用，另外登記成
出場變體並從零起算——它是執行細節，不是收益軸。使用者的「#8 試試看」試完了：
**在現在的持有時間量級下，carry 進不了帳**；要讓 carry 有意義，持有要以天計，那就是
基差交易，風險簿完全不同，要另立一節才能談。

- [x] 跑（2026-09-04）：三關過但消融證明驅動是出場目標不是 carry → 不開時鐘
- [ ] 若要「出場改中線」：另登記出場變體，從零起算

**同日順帶：#3 的 $300 緩衝跟 size 的關係**（`cost_model.py`，掛單無返佣）：緩衝是
固定金額，所以 size 越小它佔的比例越大——$50 時小所緩衝＝6× 名目，第 7 桶每筆
21–56 bps；$200 時 5.6／5.0／3.8；$300 時 3.9／3.5／2.6。**$300 緩衝隱含每筆 size 至少
$200 才攤得開**，而 S4 說 size 不能超過薄的那一側的深度（家族小所頂檔 $19–420）。
兩個約束夾起來：這個緩衝只在 NBIS（$135–420）與 CEX 腿配對上用得起，SNDK/ANTH 用不起。
**這不是緩衝定錯，是它把「哪些配對值得做」講清楚了。**

### 1.06 套利·成本函數的七個桶（2026-09-04 使用者拆解，`research/arb/cost_model.py`）

**使用者的拆解，逐項變成算式**（每筆來回 bps；`fees.py` 只是第 1 桶）：

| 桶 | 算式 | 現在的輸入來源 |
|---|---|---|
| 1 顯性手續費 | maker/taker × 兩腿 × 進出 | `fees.py`（VERIFIED，除 binance 未查證） |
| 2 滑價 | size 走過頂檔→1bps 內→3bps 內三段深度；超過 3bps 深度＝吃掉整個帶（inf） | 掃描 v5 的累積深度（MEASURED）；錄製家族只有頂檔，×4.5（§1.00 v5 量到的中位比，ASSUMED） |
| 3 資金成本 | (兩腿保證金 + 兩邊閒置緩衝) × 門檻利率 × 持有時間 | 持有時間＝收斂中位分鐘（MEASURED）；保證金率、5% 門檻、1× 閒置緩衝（ASSUMED） |
| 4 持有成本 | −資金費率差 × 持有時間/8h | 錄價 `fund_diff`（MEASURED） |
| 5 轉移成本 | 每次重平衡（手續費 + σ√t 的裸腿風險費）× 每筆分攤次數 | 全部 ASSUMED（2 bps、20 分鐘、每 20 筆一次、σ 60%） |
| 6 營運成本 | P(任一腿失敗 ∪ 停機命中持有期) × 損失 | 失敗率 6%＝掃描器每輪 2975/3164 報得到價（MEASURED）；46 分鐘停機年化（MEASURED）；損失 5 bps（ASSUMED） |
| 7 尾部 | Σ p_i × L_i 年化拖累 × 鎖住資本 ÷ 每年交易次數 | 全部 ASSUMED；每年 <12 筆的配對不攤（會落在一筆上讀成 −800，改標「尾未攤」） |

**跑出來（帶＝凍結判決的 p90；毛＝帶÷2）**：

| 情境 | SNDK | NBIS | ANTH | 零費 GOLD_LL/NVDA_LL |
|---|---|---|---|---|
| 吃單 $200 含返佣：只扣費 → 扣七桶 | −2.8 → **−9.2** | +0.3 → **−5.9** | +1.1 → **−5.1** | +2.6/+3.4 → ≈0 |
| 掛單 $50 無返佣：只扣費 → 扣七桶 | +1.0 → −5.5 | +4.1 → **−2.1** | +4.8 → **−0.3** | 同上 |

**三件從表裡讀出來的事**：
1. **「只扣費」到「扣七桶」差 5–6 bps，而那 5–6 bps 幾乎全是假設**（第 5 桶 2.0、第 7 桶
   2.6–3.9、第 6 桶 0.6）。滑價、資金、持有在現在的規模與持有時間下都是 0——
   **不是它們不重要，是我們現在小到量不出來**（$200 低於頂檔、持有 5–15 分鐘）。
2. **第 7 桶的假設等於每年吃掉鎖住資本的 8.4%**（小所違約 5%×50% + USDG 脫鉤 10%×3%
   + 基差爆開 10%×10%）。這一個桶就決定家族正負，而它一個數字都沒量過。
   要動它只有兩條路：找到公開的違約／脫鉤基率，或降低鎖住資本（第 3 桶的閒置緩衝 1×
   是它的乘數）。
3. **量到的比例 43%（3/7 桶）**。每換掉一個假設就多量到一桶——優先序：第 5 桶（開一次
   真實劃轉就有數字）＞第 3 桶保證金率（各場館文件查得到）＞第 7 桶（最貴、最不可能
   量準，但影響最大）。

**這條線現在的誠實狀態**：以凍結的帶、七桶悲觀假設，**家族在任何情境下都不是正的**；
最接近零的是 ANTH 掛單無返佣（−0.3）與零費對照組（≈0，帶本身只有 5–7）。
把它翻正的不是再找一個桶，是**帶要變寬**——這把問題交回 §1.05（交叉上市窗口）
與 §1.03 的結論（有小所參與的配對才有帶）。

- [x] 七桶成本函數落地、家族逐配對報告（2026-09-04）
- [x] **成本名冊 `research/arb/COST_INVENTORY.md`**（2026-09-04，使用者：「把所有會用到的
      成本列出來，我來想解套方案」）：七桶＋固定成本共 **47 項**，每項標現值（量到／假設）、
      來源、性質代碼（帳／結／工／規／時／率／固）；每桶最大的一項與桶間抵換關係寫在末尾。
      這裡不解，只列——解法由使用者提。
- [ ] 第 5 桶：做一次真實劃轉（HL→Lighter、CEX→HL），記費用與到帳分鐘
- [ ] 第 3 桶：各場館初始保證金率查文件替換 ASSUMED
- [ ] 第 2 桶：錄製家族補錄 1bps/3bps 累積深度（現在只有頂檔，靠 ×4.5 假設）
- [ ] 判決程式加一行「扣七桶淨值」報告列（不動凍結判準）

### 1.05 套利·交叉上市窗口（2026-09-04 預註冊；使用者：「新交易所上了新商品而其他所本來就有，這時候就有套利機會」）

**為什麼是這個問題**：§1.03 殺掉「換大所」（1.07×）、§1.01 殺掉「換時段」——兩者
都在穩態市場裡找**常設**的邊，答案一致：穩態下價差被套到只剩交易成本。那對兄弟
的文章寫的正是他們的邊從來不在「哪裡」，在「**什麼時候**」——場館或市場剛上線的
前幾天、資本還沒到之前。CoinGecko 報告從另一側說同一件事：「後進場的交易所報價
偏高，因為套利資金流進去得少……每有新交易所上線，這種缺口就會在早期重現」。

**所以假設不是 where 是 when**：一個配對的帶是「該配對誕生（第二個場館掛上該標的）
以來的時間」的遞減函數。

**誠實的出處**：這個想法是看 listings 檔看出來的——PONS 08-31 hl_core → 09-01
lighter-rh → 09-02 lighter，然後它是全掃描裡帶最寬的零費配對（45.95 bps）。**看過
資料才有的假設**，所以本節的回測切片只是探索、只能決定要不要開前瞻時鐘；確認要靠
登記之後發生的上市。跟 §0.94 變體 M 同一條規矩。

**先修儀器（2026-09-04，entropy-arb `6a2936d`）**：2,023 筆「listed」事件裡 **2,006 筆
是假的**——某場館 universe 抓取失敗，下一輪整份清單回來，集合差把每一個都叫成新
上市。真的那 17 筆被埋在底下。偵測器改成：前一份快照沒有的場館一律 `seen_at_start`；
同場館同輪 ≥5 個同向變動標 `mass_reappear`／`mass_vanish`；不刪任何列。五個已知答案
測試通過；掃描器已重啟（看門狗順手又驗了一次）。

**凍結（`research/arb/listing_window.py`）**：P1 同一配對 <24h 的帶 ≥ 2× >72h 的帶
（配對內比較，跨標的差異做不出來）；P2 對照組（整段都在的配對，同一把尺）無此衰減
（<1.3×）；P3 ≥60% 的事件同向。全過才開前瞻時鐘＋上市監看，不授權交易。

**回測切片結果（2026-09-04）：跑不出來，不是被否證。** 17 筆真上市對應到 20 個配對，
**沒有任何一個同時有 <24h 與 >72h 的足夠樣本**——掃描窗只有 4 天、多數上市在最後
一天。另外對照組比值 0.55（後段比前段寬）暴露了第二個問題：**掃描器在窗口中途換
過版本**（09-03 v4 加 CEX 腿、v5 加累積深度），前後段不是同一把尺。所以這個窗口
**根本不能用來回答這題**；前瞻版才乾淨——從穩定的掃描器起算。

**公告領先市場出現的時間（同日量到，這是「事件捕捉」的核心數字）**：三家的公告端點
都是公開的（OKX `support/announcements`、Bitget `public/annoucements`、Binance cms），
而且抓到的正是我們偵測到的那批事件——

| 場館 | 標的 | 公告 | 市場出現 | **領先** |
|---|---|---|---|---|
| OKX | PYPL／DDOG／IONQ | 09-03 03:30Z | 09:05／09:29／09:31Z | **5.6–6.0 h** |
| Bitget | GPRO | 09-03 14:00Z | 14:24Z | 0.4 h |

公告不只早，還告訴你**哪個標的會出現在哪裡**——這是能預先把錄價／掛單準備好的
那個窗口。OKX 給六小時，Bitget 給二十幾分鐘。

**同日的實例（使用者轉來 yourQuantGuy 貼文）**：**Polymarket 09-04 上線永續**，
67 個市場（36 股票、24 加密、4 商品、3 指數；含三星、SK 海力士、美光、黃金、白銀、
WTI、布蘭特、S&P500、Nasdaq100）——**一次開出幾十個「新所有、別所也有」的配對**。
費率 taker 4 bps／maker 1.25 bps（30 天量階梯到 2 / 0.5 rebate），**掛單流動性獎勵
每天 $75k**；貼文作者說 BTC/ETH/GOLD/SP500 買一賣一貼著、各幾千美元。
Polymarket 掛單獎勵是「成本」那一軌的直接候選：它不是折扣，是**倒貼**。

- [x] 儀器：上市偵測修正（2026-09-04）
- [x] 回測切片：樣本不足＋儀器中途換版，不能判（2026-09-04）
- [ ] **前瞻時鐘**：從 2026-09-04 起每一筆真上市自動進本節四關；≥20 個事件各自
      有 >72h 追蹤時第一次讀（約 09-18）
- [ ] 公告監看：三家公告端點每 5 分鐘輪詢，公告→市場出現的領先時間逐筆記錄
- [x] **BWEnews（t.me/BWEnews）評估（2026-09-04，使用者：「我有一個新聞機器人消息超快」）**：
      它是交易所公告的**轉發器**（雙語標題＋來源 URL，時間戳 UTC），涵蓋 Binance／
      Upbit／Bithumb／Schwab 等。拿它對 Binance 期貨上市**逐筆對時**（Binance 的
      `onboardDate` 是合約開始交易的時刻）：

      | 事件 | 合約開始交易 | BWE 貼出 | BWE 相對開盤 |
      |---|---|---|---|
      | GPROUSDT | 09-03 13:45Z | 20:30Z | **晚 6.75h** |
      | 9/2 那批 TradFi（NVDL/TSLL/DDOG/…）| 10:00–10:30Z | 16:45Z | 晚 ~6h |
      | 8/28 那批（TEM/MRK/IONQ/MARA/PDD）| 09:00–09:20Z | 15:30Z | 晚 ~6h |

      **對「新所上了別所有的商品」這個用途，BWE 比直接輪詢交易所 API 慢六小時**——
      合約已經交易半天它才貼。它快的是**韓所現貨上幣**（Upbit／Bithumb KRW），那是
      「上幣拉盤」的交易，不是跨場館永續價差。沒查證的一點：Binance 自己的公告時刻
      抓不到（cms 端點回非 JSON），所以「BWE 相對 Binance 公告」的延遲未量。
      **結論**：事件源以交易所自己的公告端點（OKX 領先 6h、Bitget 24 分鐘）＋
      `exchangeInfo` 輪詢為主；BWE 當第三方對照，不當主要訊號。另查 Binance
      `PENDING_TRADING` 狀態：只有一筆陳舊殘留，**不能**當開盤前訊號。
- [ ] Polymarket 永續接進掃描器——**本機連不到**（2026-09-04 查證）：`polymarket.com`、
      `docs.polymarket.com`、`api.perpetuals.polymarket.com` 三個主機全部解析到同一個
      IP `182.173.0.181`，連線逾時——這是 **ISP 層的 DNS 黑洞**（台灣把它當博弈站擋），
      不是 Polymarket 自己的地區限制（那個只擋美國下單）。後果：(a) 掃描它需要一台
      在台灣以外的機器——**這是東京那台機器第一個具體的、非延遲的理由**；(b) 就算
      掃到了，下單前要先確認帳戶地區可下單。API 形狀（公開文件）：
      `https://api.perpetuals.polymarket.com/v1/`，instruments／ticker（index_price、
      mark_price、funding_rate、next_funding）／book（bids/asks、sequence），有 SDK
      `fetch_perps_book(instrument_id, depth)`。

### 1.04 套利·待辦清單（2026-09-04 使用者：「其他的列入待辦事項」）

排序依「回答一個能關掉整條線的問題」的成本，最便宜的在上面。**每一項都要先
預註冊再跑**；沒做的原因也寫下來，不要讓它們變成默默消失的想法。

- [ ] **一筆 $10 測試單讀回執**（最便宜、最決定性）：確認 Entropy 腿的帳單上
      是不是 3.4 bps（25% 折扣）。這是唯一能把費率從「頁面寫的」變成「帳上
      發生的」的方法。**不下這筆單，所有淨值算式都掛在一張截圖上。**
- [ ] **帶改用費率推導而非 p90**：現在的帶是分佈的 p90（依定義 10% 的分鐘在
      帶外），改成「帶 = 打平成本 × 2」。已量過的形狀：IO 掛單門檻 4.5 → 帶 9 bps
      時 SNDK 0.5 次/天、NBIS 4.6、ANTH 8.3，收斂 97-100%。**這不是門檻掃描**
      （帶由成本決定不由結果決定），但要當報告列加進判決程式，不動凍結判準。
- [ ] **掛單成交率**：所有「掛單模式為正」的算式都假設掛得到、成交得了。
      逆選擇的量在這個專案有前例（V7 的 maker 掛開火價死於逆選擇）。要小額
      實盤才量得到。
- [ ] **開盤兩小時 1 秒取樣錄一週**：§1.01 量到開盤時段分鐘內幅度是收盤的
      3 倍（9.7 vs 3.1 bps），代表比 p90 更寬的瞬間存在而我們看不見。改取樣層、
      資料量 60 倍。**先決定要不要，再動工。**
- [ ] **「HIP-3 vs 真實市價」那條腿**（那對兄弟做的那件事）：先用免費延遲報價
      當粗略錨只錄不做，回答「偏離有多大、多常、收不收斂」。看得到 50-300 bps
      才談 IBKR 帳戶與資金。§1.03 已證明「小所彼此」這條路的帶就是 10 bps 級。
- [ ] **兩腿保證金是不是分開帳戶**（HIP-3 dex 之間、HL 主場 vs dex）：決定
      斷腿風險與資金效率，升格前必查。
- [ ] **xyz/HL 的部署者分成返佣能不能談到**（**優先序 2026-09-04 調降**：量化後
      返佣 25%→50% 只多 +0.75 bps，而吃單→掛單差 4.5 bps，**執行方式的槓桿是返佣的
      6 倍**；且「全部 dex 25%」對現有家族**零影響**——另一腿 Lighter 本來就 0 費、
      IO 已經是 25%。要談就談 50%，而且只對 xyz 的商品配對有意義）：B 情境下商品配對掛單
      全部翻正。這是談判不是研究。
- [ ] **`GOLD` 資產別名要拆開**（§1.03 查到）：PAXG／XAUT／XAU 是三個不同發行
      商的產品，掃描器現在把它們併成一個 `GOLD` 互相配對，量到的是發行商基差。
      修法是別名表拆成 `GOLD_PAXG`／`GOLD_XAUT`／`GOLD_IDX`，或乾脆只認同代號。
- [ ] **掃描器的同場館配對**（§1.03 查到）：`GOLD@bitget-bitget` 這種兩腿在同
      一個場館的列不該存在，加一道 `leg_a != leg_b` 的守衛。

**~~不做的~~ → 改列必做（2026-09-04 當天改正）**：我先前寫「Binance 的 USDT 永續沒有
真股票」是**錯的**——我的篩選只認 `contractType == "PERPETUAL"`，而 Binance 的股票
永續是 **`TRADIFI_PERPETUAL`**（`GPROUSDT`／`DDOGUSDT`／`IONQUSDT`／`SAMSUNGEMUSDT`／
`KODEX200USDT`／`HK0625USDT`…，`underlyingType` 標 KR_EQUITY／HK_EQUITY／CN_EQUITY／
EQUITY）。近 21 天 Binance 上了 **34 個**這類合約。所以 CoinGecko 那個「Binance vs
Hyperliquid 三星 0.93%」的配對**可以重現**，而且 Binance 是本線缺的最大一個場館。
教訓同 mistake.md 2026-09-03 Bitget 單位那條：**「查了沒有」的查詢要先問它看不看
得到目標**——我用一個把它排除在外的篩子去找它。
- [x] **Binance 接進掃描器（2026-09-04，entropy-arb 本機倉）**：714 個市場，其中
      **188 個 TradFi 永續**（AAPL/AMZN/ASML/ANTHROPIC/SAMSUNGEM/KODEX200/HK0625…）。
      簿口單位以真實簿驗過：BTCUSDT 頂檔 **$1.02M**（最深的簿，合理）；新股票永續
      很薄——GPRO $113、SAMSUNGEM $230、KODEX200 $11。踩到一個小坑：`depth` 的 limit
      只收 {5,10,20,50,…}，我們的 25 會回錯誤 JSON、看起來像「沒簿」——修成取
      ≥25 的最小合法值再截斷。掃描器已重啟；§1.03 的 BIG 分級加入 binance。
      費率先用標準 5/2 bps、無返佣、標未查證。

### 1.03 套利·大所 vs 小所（2026-09-04 預註冊，使用者：「先做這個」）——外部報告的可檢驗版本

**觸發**：CoinGecko 2026 代幣化股票報告寫 Binance 的三星永續平均比 Hyperliquid
高 **0.93%**（SK 海力士 1.03%、最寬 2.3%），並解釋成「**後進場的交易所報價偏高，
因為流進去的套利資金少**」。我們家族量到的是 2–15 bps——而**我們兩條腿都是後
進場的小所**。如果那個機制成立，帶窄的原因是配對選錯了，不是方法錯了。

**查證（先於任何 band 計算）**：Binance 的 USDT 永續**沒有真股票**（`SPXUSDT`
是迷因幣、`MUSDT` 是迷因幣、`XAUTUSDT` 是代幣化黃金），所以報告的「Binance 腿」
是別的產品。**不需要它**：OKX（SAMSUNG／SKHYNIX／SKHY／KR200／NVDA／TSLA／
AAPL／SPY／QQQ／MSTR／COIN／XAG）與 Bitget（SAMSUNG／SKHYNIX／KR200／NVDA／
TSLA／AAPL／XAUT／PAXG／COPPER／NDX100）掛的是同一批標的，xyz 也有
（`xyz:SKHX`／`xyz:SKHY`／`xyz:KR200`）——**掃描器已經錄了 4 天，這個比較現在
就做得了，零新整合。**

**凍結（`research/arb/venue_size.py`，先 commit 再跑）**：場館分級**按它是什麼
分，不按它的數字分**——BIG＝okx／bitget，SMALL＝所有 HL builder dex＋HL 主場＋
Lighter 兩鏈。
- **P1**：股票／商品類，BIG-SMALL 的帶中位 ≥ **3×** SMALL-SMALL
  （報告的量級是 10×+；3× 是「足以改變我們配對選擇」的門檻，事前定）
- **P2 對照組**：BIG-BIG（okx↔bitget）必須是三者中**最窄**的——若不是，這個
  分級量到的是別的東西，P1 就沒有意義
- **P3**（方向性報告，不擋 P1）：同樣的順序在加密標的上是否成立

**排除（事前寫死）**：槓桿包裝（CSOP…2L 這類，2 倍產品不是同一個標的）、
報價數 <200 的配對；帶用**凍結的排名指標**（正向可成交空間 p90），不另發明數字。

**這一節只決定一件事**：要不要把錄製家族重新指向 BIG-SMALL 那一格。不是判決、
不開時鐘。

**結果（2026-09-04）：P1 失敗，比值 1.07×——「大所 vs 小所寬一個數量級」在我們
量得到的範圍內不成立。P2 對照組通過。**

| 股票/商品 | n 配對 | 帶中位 | 帶 p75 | 深度中位 |
|---|---|---|---|---|
| BIG-BIG（okx↔bitget）| 29 | **3.24** | 5.66 | $378 |
| BIG-SMALL | 140 | 11.60 | 14.75 | $328 |
| SMALL-SMALL | 71 | 10.81 | 14.27 | $367 |

**BIG-SMALL ÷ SMALL-SMALL ＝ 1.07×**（加密類 1.17×，P3 同向但一樣微弱）。

**P2 通過而且通過得很乾脆**：兩個大所之間只有 **3.24 bps**，是三格中最窄的
不到三分之一——**分級是有效的**，它確實量到了「場館成熟度」，只是那個效應
**發生在 BIG-BIG 那一格**（大所彼此已經被套利到很平），而不是「大所對小所
特別寬」。真正的分界線是「**有沒有小所參與**」，不是「大小差距」。

**兩個儀器缺陷，照 2026-08-02 規矩先查再解讀，v2 同一批資料重跑（v1 判準與
判決不動）**：(a) GOLD 配對把 **PAXG／XAUT／XAU** 混在一起——三種不同發行商的
代幣化黃金加一個黃金指數，PAXG vs XAUT 是**發行商基差**不是場館價差（錄價程式
作者自己的頭號警告，§1.00 記過）；(b) `GOLD@bitget-bitget`、`GOLD@lighter-lighter`
**兩條腿在同一個場館**，根本不是跨場館套利，而它們就坐在 P2 依賴的 BIG-BIG 格裡。
v2 剔除同場館與跨代號配對後：比值 **1.08×**（股票）／1.17×（加密），BIG-BIG
3.17 bps 仍最窄——**修完儀器結論一字不變**。

**因此**：不因為那份外部報告改變配對選擇。報告的 0.93%／1.03% 是「**平均價差**」，
我們的是「**p90 可成交空間**」（已扣掉要穿越的買賣價差）——兩個不同的量，那個
數字不能拿來當我們的期望值。要驗它必須自己錄同一個口徑，而我們現在錄的
GOLD_LL／NVDA_LL（§1.02）正是這件事的乾淨版本。

**順帶記下的事實**（不是判準）：韓股三個場館都有——OKX `SAMSUNG`/`SKHYNIX`/
`SKHY`/`KR200`、Bitget 同名、xyz `xyz:SKHX`/`xyz:SKHY`/`xyz:KR200`。
`KR200@xyz-okx` 帶 23.25 bps 但**深度中位 $1**——這正是「帶寬不等於錢」的
教科書例子。Binance 的 USDT 永續**沒有真股票**（`SPXUSDT` 是迷因幣），所以
報告的 Binance 腿是別的產品，我們沒有它也不需要它。

- [x] 跑（2026-09-04）：P1 FAIL 1.07×、P2 PASS；v2 修儀器後 1.08×，結論不變

### 1.02 套利·零費對照配對：兩腿都在費率表為 0 的場館（2026-09-04 登記，使用者：「加進去然後其他能做的研究繼續做」）

**為什麼**：IO 返佣查證是 25% 不是 100% 之後，家族在吃單模式下全負。「返佣以外
的方法」裡最乾淨的一條是**挑兩腿費率表本身就是 0 的場館對**——Lighter 與
Lighter-RH 都是結構性 0 maker / 0 taker（費率表，不是促銷）。掃描裡有 42 個同一
標的兩邊都掛的配對；帶寬前段：NVDA 8.0、GOOGL 7.1、GOLD(XAU) 5.1（頂檔 $557）、
SILVER 3.3（$816）。在這種配對上，**費率變數是 0、門檻是 0，能殺它的只剩兩樣：
收斂（USDC vs USDG 穩定幣基差偽裝成價差）與深度**。

**登記**：GOLD_LL（lighter XAU ↔ lighter-rh XAU；**刻意不用 PAXG**，底層資產要
完全一致）、NVDA_LL（NVDA ↔ NVDA）。各自 7 天時鐘、同一套四關＋收斂、
`premium_verdict` 逐配對報告不挑。它們是「零費對照」這個獨立問題，**不走
掃描排名的升格**（升格是在找肉，這是在問一個機制問題）。

**工程（entropy-arb 本機補丁，本機倉 commit）**：錄價程式腿 A 原本寫死是 HL；
加 `entropy.venue: lighter|lighter-rh`（預設 hl，既有設定逐位元不變、config_BTC
載入結果相同）、`entropy.symbol` 別名、資金費率輪詢在腿 A 為 Lighter 時改抓該鏈。
兩支各自 config／bat（CRLF 驗過）／log 目錄／開機啟動項；看門狗成員 8 → 10；
freshness 兩列；判決程式 PAIRS 與 VENUE_KEYS 登記。

**兩個先寫下的預期**：(a) Lighter 頂檔深度嚴重低估（§1.00 v5：BTC 頂檔 $25、
1 bps 內 $37k），所以「肥刻深度」欄在這兩支上會偏低，判決時要看累積深度；
(b) 若帶寬主要是 USDG 基差，收斂關會擋下來——BTC@lighter-lighter-rh 帶只有
0.8 bps，代表純基差很窄，XAU/NVDA 若寬得多就是別的東西。

- [ ] 錄滿 7 天（~09-11）出判決；期中不讀

### 1.01 套利·時段條件化——溢價是不是美股開盤的事件？（2026-09-04 預註冊，先 commit 再跑）

**觸發**：使用者問「無返佣要突破什麼」。按小時切 SNDK/NBIS 的 p90 帶，13–15 UTC
比其他時段寬 3–5 倍，ANTH（私募永續、沒有現貨開盤）沒有這個形狀——符合本線
登記時寫的機制（「盤後預言機制度不同」是股票永續溢價的來源）。**這是看過資料
才有的假設**，所以照 §0.94 的規矩：現有資料是選擇窗、只用來決定要不要開時鐘；
從本節登記時刻起錄的列才是驗證窗。

**凍結（寫死在 `research/arb/session_window.py`）**：窗口按機制定：OPEN
13:30–15:30 UTC（美股開盤前兩小時）／RTH／PRE／OFF，**不是表上最好的那個小時**。
四個預測（P1 OPEN>2×OFF、P2 ANTH 對照 <1.5×、P3 窗內偏離仍收斂 ≥70%、
P4 至少 4 天成立）全過才登記變體 W；缺一就死、不開時鐘。費率一律用**無返佣**
費率表（問題本來就是「沒有促銷活不活」）。經濟量：只做 OPEN 窗、一腿掛單、
無返佣，每筆淨 bps × 每天收斂次數 × 肥刻深度。

**結果（2026-09-04，`research/results/arb_session_window.json`）：凍結四關 → 陣亡，不開時鐘。**

| 關 | 結果 | 數字 |
|---|---|---|
| P1 SNDK 與 NBIS 的 OPEN 帶 > 2× OFF | ❌ | SNDK **2.17×**、NBIS **1.36×** |
| P2 ANTH 對照組 < 1.5× | ✅ | 0.94×（沒有現貨開盤就沒有這個形狀，機制對）|
| P3 窗內偏離收斂 ≥70% | ❌（**無資訊，不是失敗**，見下）| 0–1 次偏離 |
| P4 ≥4 天成立 | ❌ | SNDK 4/7、NBIS 2/4 |

**跑完發現兩個儀器缺陷，照 2026-08-02 的規矩先查儀器再解讀，另跑 v2（同一批
選擇資料，v1 判準與判決不動）**：(a) OPEN/OFF 把週六日混進去了——機制是美股
開盤，週末沒有開盤，那兩天 13:30 那格依定義就是 OFF，是**窗口定義錯**不是
發現；(b) P3 的帶取的是可成交空間的窗內 p90（6–20 bps），但凍結的 `convergence`
是在 premium_mean 對中線的偏離上偵測事件——尺度不同，判決帶（2–15）有幾百
個事件、窗內帶只有 0–1 個，所以 P3 是**沒量到**。v2 改成只算平日、P3 改問
「判決自己的偏離事件裡，起始在 OPEN 的那些收不收斂」：

| | 平日 OPEN/OFF | 逐日 >2× | OPEN 起始的偏離 | 收斂 |
|---|---|---|---|---|
| SNDK | **2.19×** | **4/5** | 23/194（12%）| **23/23** |
| NBIS | 1.37× | 2/4 | 22/95（23%）| 22/22 |
| ANTH | 0.99× | 0/4 | 0 | — |

**讀法**：開盤效應**只在 SNDK 上成立**，NBIS 沒有——它的寬帶在 PRE（09–12 UTC，
含一個 62.8 bps 的 10 UTC 格，尚未查是不是單一天）和一個買側持續偏移（OFF 買側
p90 14.9，跟 OPEN 差不多）。兩檔股票永續只有一檔有這個形狀，登記的判準是
「兩檔都要」，所以**修完儀器還是陣亡**，這不是差一點。而且就算只看 SNDK：
OPEN 起始的偏離只佔全部的 12%（每天 ~5 次），肥刻深度中位 **$100**——
只做窗口 × 掛單 × 無返佣算出來每天 **$0.0x**。**深度把它殺掉了，跟時段無關。**

**沒查的（照十道檢查明寫）**：~~NBIS 10 UTC 的 62.8 是不是一天撐出來的~~
→ **查了，是一天**（09-03 一天 102 分鐘 ≥20 bps、其餘三天 09–13 UTC p90 只有
−1～+3）——單一事件，不是時段結構，NBIS 的「PRE 窗很寬」到此作廢；
分鐘內幅度在 OPEN 是 OFF 的 3 倍（9.7 vs 3.1 bps）——代表 1 秒取樣會看到
更寬的可成交空間，但這批資料量不到；夏令時間沒處理（全樣本在 EDT 內）。

**這條研究方向下一步在哪**：機制在 SNDK 上活著、在 IO↔Lighter 這個場館對上
沒有肉。開盤效應＋深度同時存在的地方是 **xyz/CEX 的股票與商品配對**
（SPY@lighter-okx $500k、SILVER@xyz-okx $224k）——09-06 `scan_rank` 出名單、
升格、錄 7 天，**那時再用本節凍結的四關（含 v2 的兩個儀器修正）跑一次**，
不要現在就拿快照猜。

- [x] 跑（2026-09-04）：v1 陣亡；v2 儀器修正後 SNDK 有形狀、NBIS 沒有、深度 $100 → 仍不開時鐘
- [ ] CEX/xyz 股票商品配對升格錄滿 7 天後，用本節四關重跑（窗口定義：平日 13:30–15:30 UTC）

---

### 0.91 站內資金費收租（2026-09-02 登記，產品端請求；**判決 09-30**）

> 產品端請求書 `../jarvis/產品端請求_站內資金費收租_20260902.md`；
> 研究端回覆 `../jarvis/研究端回覆_站內資金費收租登記_20260902.md`。
> 產品端建議 §0.80，**該號已被 08-30「每根 Strong 都進」佔用**，改 §0.91。

**問題**：Bitget 站內「現貨多＋永續空」收資金費，BTC/ETH 的年化分佈長什麼
樣——中位多少、翻負佔比多少。原本只有 08-20 一次抽樣（BTC +10.9%）。

**凍結判準（一字未改產品端提案）**：窗口 09-02 → **09-30**；BTC 與 ETH
**各自**滿足「結算年化中位 ≥8%」∧「翻負 <25%」，且**前後兩半各自成立**。
任一不過 → 關線，產品端不寫現貨 adapter。8% 的推導（對投入資金約 6.7%、
扣一輪 6–12bp 與翻負期）站得住；**在登記當下把門檻挪到感覺比較好的數字，
正是預註冊要擋的事**。

**⚠ 先驗（判準凍結後才拉的 180 天回填，每幣 270 次結算）**：

| | 中位年化 | 翻負 | p10/p90 |
|---|---|---|---|
| BTC | **+4.05%** | 21.5% | −3.9 / **+10.9** |
| ETH | **+3.67%** | 21.5% | −2.4 / +10.9 |

**中位只有門檻的一半，而且 08-20 那次促成提案的 +10.9% 正好是分佈的
p90**——動機來自一次尾部抽樣。**不改判準、不提前關線**（回填是背景不是
判決），但期望值已經講清楚：**這條線目前看起來會 FAIL**。

**已落地（與登記同 commit —— §0.44 的教訓：只寫在文件裡的預註冊等於沒有）**：
`basis_recorder.py`（10 分鐘一輪，BTC/ETH 判決＋8 個山寨背景；每列含
fundInterval **不寫死 8**、mark/index → 溢價 bp）、`basis_verdict.py`
（期限前拒判；結算次數以 (symbol, funding 週期) 去重，用取樣次數當分母
會讓安靜期灌爆分佈）、班車、看板 0.91、freshness（1 小時上限）。
**不擴充 `funding_collector`**：`funding_rates` 沒有 mark/index，而溢價
是這條線的另一半肉。

**兩個產品端問題的裁示**：
1. **對沖 vs 獨立**：兩者都是，**分開判、同一批資料**。獨立照 8%；對沖的
   門檻低得多（任何正 carry 都抵銷網格庫存成本）但**規模被網格庫存綁死**。
   對沖列為附錄讀數，**永遠不能拿來救一個 FAIL 的獨立判決**。
2. **hedge 觸發單四條規格**採為底線，但**判決前不實作**。

**同日自踩一坑並修**：09-01 把錄價輪替改成時間戳之後，`prereg_publish`
的 `_n_arb` 只認字面 `.old`，看板數字從 ~6,000 掉到 **914**——
**mistake.md 08-29 一字不差的重演，而且發生在修好輪替的同一個 session**。
已改 glob 所有 `*.old`（並補上 HYPE）。教訓再寫一次：**輪替一份檔案時，
枚舉每一個讀它的人；計數類下游的錯法不是壞掉，是從零重數。**

---

### 0.87 套利看板的三個缺口（2026-09-01，使用者：「套利的數據網站也沒更新」）

網站的進度條其實有在動（14:06 更新），但查下去有三個真缺口，前兩個是
§0.86 的同族——**東西在本機是對的，只是從來沒到達外面**：

1. **判決檔沒有任何排程**。`premium_verdict.py` 只有人跑才更新，
   `arb_premium_verdict.json` 因此**停在 08-30**（兩天前），而它正是
   09-04 判決要用的那個檔案。已掛進每小時班車（bat 末行已驗）。
2. **看板只數 SNDK**。08-30 登記的家族（NBIS/ANTH/BTC/ZEC/NEAR）在
   `_n_arb` 裡不存在 → 從網站看，那五個錄價器等於沒發生。已改：閘門
   仍以 SNDK 為準（它起算最早、09-04 判決），家族數與 BTC 對照組寫進
   note，看板不再低報這條線。
3. **「可捕獲美元」指標把結構性偏移算成錢——對照組抓到的**。
   BTC（兩邊深簿零費率、帶應≈0）報出全家族最高的 **$97.8/天**。資料
   是真的：HL 的 BTC 持續比 Robinhood 鏈貴 3-5 bps、**100% 的分鐘為正**。
   但它的收斂欄是 `episodes: 0`——溢價從不偏離自己的中線，**因為它就是
   中線**：結構性偏移，不是會回來的價差。鎖進去永遠拿不到錢。
   **判決閘門是對的**（收斂關過不了就不過閘）；**錯的是我加的報告欄**，
   band × fires × depth 沒有考慮收斂。已改成以收斂為前提，不收斂記 0
   並印出理由。修正後同時把 NBIS buy 與 NEAR sell 兩個假數字也歸零。

**這是對照組第一次真正發揮作用，而它抓到的是「我自己的儀器」**——
與 mistake.md 2026-07-29「新診斷先在答案已知的資料上跑」同一條規矩，
只是這次那個已知答案是「BTC 不該有肉」。若當初沒有把 BTC 放進家族，
NBIS/NEAR 的假數字會一路帶到判決日。

### 0.88 套利第二條收益軸：資金費率 carry（2026-09-01 開錄，使用者定調「不嚕空投、要靠套利獲利」）

**觸發**：使用者轉來 MOB (mob.exchange) 的空投教學。讀完後判斷：教學本身
是**刷積分**（MOB＋Ondo 對鎖賺三家積分，收益＝未來空投，EV 不可算），
與本線無關；但平台的 Delta-Neutral 策略卡自己寫著 **「The funding spread
is the return」**，兩腿是 **Hyperliquid ↔ Lighter RH**——**正是我們已在錄
的那兩條腿**。使用者定調不做空投、要靠套利獲利，於是只取這一條情報。

**兩種收益要分開記**：價差要**收斂**才付錢（現有四關管的就是它）；
資金費率是**持有**就付錢。前者已錄兩天到四天，後者連資料都沒有。

**已上線**：
- `entropy_arb/funding.py`（本機補丁）：每 5 分鐘拉兩邊費率。
  **單位是這裡唯一不能錯的東西**——HL 是**每小時**（142/233 幣停在
  1.25e-5 基準）、Lighter 是**每 8 小時**（錨定：同端點 `exchange:
  "binance"` 的列群聚在 1e-4 ＝幣安 0.01%/8h）。全部正規化成 **bps/8h**，
  `diff = hedge − entropy`（正＝多 entropy／空 hedge 收錢）。
- 錄價每分鐘多寫四欄（entropy/hedge/diff/age）；poller 掛掉留空白，
  **永不為 funding 掉一分鐘**。
- **HYPE 加入家族（第七條）**：09-01 截面費率差最大（+10.95%/年）。
- `premium_verdict` 加 `funding_stats`（中位／p10-p90／同號比例／年化）
  ——**報告用不進判準**。08-28 凍結的四關是關於價差的，不因為出現第二種
  收益就事後擴充（那正是預註冊要擋的事）。

**修掉一個會吃資料的 bug**：schema 輪替原本是 `os.replace(path,
path+".old")`——**第二次輪替會覆蓋第一次**，08-28 那 257 分鐘會被靜靜
吃掉。已改時間戳輪替，判決端改 glob 所有 `*.old`。驗證：輪替後分鐘數
連續（SNDK 6031、其餘 2909，與輪替前對得上）。這是 mistake.md 08-29
「輪替要枚舉每個消費者」的延伸——**這次的消費者是未來的自己**。

**截面 vs 序列（為什麼要錄）**：09-01 兩次量測相隔一小時就不同
（HYPE 的 Lighter 側 0 → 0.32、BTC 5.6e-5 → 4.8e-5）。**截面不能下注**，
一週後才知道那個差是穩定 carry 還是每天翻。判決仍照 09-04 / 09-06 的
價差四關；carry 是獨立的第二張表，屆時並陳。
