> **給新 session 的第一句話（2026-09-04 晚）**：這條線現在是**自己的 repo**
> （`Desktop/flowbot/arb`，GitHub `rfobelieve-crypto/flowbot-arb`，私有），
> 使用者決定**套利用獨立 session、量化線用另一個**。先讀 `README.md`，再讀本檔。
> flow_system 那邊只剩兩座唯讀顯示橋（`research/arb_home.py` 是唯一知道
> 這裡在哪的檔案）——**本 repo 永不讀 flow_system、永不碰它的 MySQL**。
>
> **今天落地的判決**：SNDK 錄滿 7.03 天、過閘——但只在零費率假設下。
> 實測帶 2.06 bps、收斂 98%（中位 6 分鐘）；費率表要求 13.5 bps → 扣費後
> 每筆為負。判決文原話：「判準沒被改，是它的前提被推翻了。要救這條線只有
> 兩條路：查證返佣，或改掛單執行。」其餘配對 09-06（NBIS/ANTH/BTC/ZEC/NEAR）
> 到 09-10（GOLD/NVDA）到期，看 `results/arb_premium_verdict.json`。
>
> **2026-09-04 晚：那四件都做完了**（B3 掛單路徑＋B1 級自審、波動熔斷、
> 把 `risk:` 數字填進 NBIS/NVDA_LL、B5 控制通道）。引擎現在沒有已知缺口。
> **接下來卡在使用者身上的是 M1**（`M1_CHECKLIST.md`）：拿到成交回執之前
> `maker_fee_bps` 沒有依據，而沒有那個數字就不該把 `mode` 從 taker 改成
> maker——掛單路徑存在的全部理由就是那個費率。之後才是 B6 面板。
>
> **⚠ 實盤前的硬條件（2026-09-04 晚查證）**：Lighter mainnet 帳號 `743078`
> 目前餘額 **$18.89**，而 `config_NBIS.yaml` 的風控數字是照**每所 $50、1×
> 槓桿**推的（`max_position_usd: 40`）。**在補到 $50 之前不要開實盤**——
> $40 的部位上限配 $18.89 等於默許 2 倍槓桿，`LIVE_50U_SPEC` §0 的「1× 槓桿
> → 清算風險消失」在那個餘額上不成立。使用者已表示後續會補到 $50。
> **HL 這邊還多一層**：2026-09-04 查證時 $11.87 **全部在 spot 帳戶**，
> 而 perp（core）與 **io dex 的清算所都是 $0**——HIP-3 dex 各有各的清算所，
> **io dex 與 HL 主場是分開的清算所，錢要分開轉進去**（使用者 2026-09-04
> 確認，也是 `engine/README.md` 的原話「Fund the dex-specific clearinghouses
> you trade」）。沒轉之前引擎連單都下不出去。
> 兩邊介面上都沒找到 spot → io dex 的內部轉帳鈕；最可能跟帳戶類型有關
> （目前是 Unified Account，而 HL 自己說 `Manual` 才是「各 DEX 餘額分開、
> 給自動化交易者用」）。**這件事在補資金＋M1 下第一筆單時一起解決，不要猜。**
> 若決定就用小額，改法在 `docs/RISK_NUMBERS.md` §3：前五列隨帳戶大小線性縮放。
>
> **⏰ HL agent wallet 到期日：2026-12-03 13:27 UTC**（90 天，鏈上
> `extraAgents` 的 `validUntil`）。到期後簽單直接失效，而且**不會有預警**——
> 引擎只會看到下單被拒。到期前要在 <https://app.hyperliquid.xyz/API> 重新
> 產生並授權，然後換掉 `.env` 的 `HL_PRIVATE_KEY`。
>
> **📅 Lighter-RH 排程維護：2026-09-05 12:00 UTC，停機 <5 分鐘**（官網公告，
> 09-04 讀到）。公告原話：維護期間**無法下單**。影響 `GOLD_LL` 與 `NVDA_LL`
> 兩個錄價器——那段時間的簿口會斷，**分鐘資料出現缺口是預期的，不是故障**。
> 引擎的 `venue_down` + 探測回復本來就涵蓋這個情境。
>
> 運維：10 個成員用 `--record-only` 跑在 `engine/`，看門狗 `EntropyArbWatchdog`
> 每 5 分鐘；判斷活著看 `engine/logs/*/minutes.csv` 的 `os.stat` mtime
> （PowerShell 目錄列表的 mtime 對長期開著的檔案會延遲，不準）。
> 引擎測試 `cd engine && python -m pytest -q`（**116 passed**，anaconda 3.9）。

# 未完成工作交接（2026-09-04 收工，給下一個 session）

> 讀法：先讀本檔的「A 卡在人身上」——那幾件不做，B 的大部分做不下去。
> 每一項都標了**卡在誰**、**產出判準**、**相關檔案**。
> 判準已凍結的不要重寫；沒凍結的一律先預註冊再跑（`.claude/rules/factor-research.md`）。

**要接的交易所清單見 `research/arb/VENUES.md`**（三層：現在只讀行情的 10 個、
$50 實盤要簽單的 4 個、之後才碰的）。

## A. 卡在使用者身上（研究端做不了，做了也不算數）

| # | 事情 | 為什麼只有你能做 | 完成的樣子 |
|---|---|---|---|
| A1 | **M1：每所手動下一筆最小單** OKX $1.9–7.7／Bitget $5／Binance $5／HL $10 | 下單是資金動作 | 跑 `research/arb/fee_receipts.py`，四個場館出「帳單 bps vs fees.py」對照；差 >0.5 bps 就以回執改 `fees.py` |
| A2 | `.env` 補 `HL_ACCOUNT_ADDRESS` | HL `userFills` 公開，不需金鑰 | A1 的 HL 那列有數字 |
| A3 | CEX 唯讀 API key（OKX/Bitget/Binance） | 帳戶憑證 | A1 的三列有數字 |
| A4 | **東京 VPS**（$8–20/月） | 要開帳單 | 引擎與 Polymarket 掃描的前提（§1.08 §3 實測：沒有任何 Railway 區同時靠近兩條腿） |
| A5 | 各所邀請碼談到的實際折扣 | 帳戶條款 | 給我數字 → 更新 `fees.py` |
| A6 | **xyz／HL 部署者分成返佣**能不能談到 | 對外談判 | 談到 50% → 商品配對掛單全部翻正（§1.04 B 情境） |
| A7 | 兩腿**保證金是不是分開帳戶** | 要登入看 | 決定 `cost_model.py` 第 3 桶要放幾份錢 |

## B. 我可以做，依序（前面是後面的前提）

### B1 ✅ 完成（2026-09-04）——`research/arb/B1_ENGINE_AUDIT.md`

**核心設計是對的**：未決不假設沒成交、對帳只採信真相不下修正單、對沖前檢查簿口
新鮮度。**沒有找到「以為沒成交就重送」的邏輯**（那是最擔心的一個）。
找到 4 個缺口：**G1 沒有累積差額硬上限**（兄弟的 `maxDelta` 沒有對應物，
**最該先補**）、G2 逾時回 `filled_base=0` 但上層先用了它才檢查 unresolved、
G3 沒有 maker 路徑、G4 兩條 Lighter 鏈共用憑證。
→ 順序因此確定：**B2（G4）→ B4 的 `max_net_base`（G1）→ B3 maker（G3）**。

### B1（原文，已完成）逐行審現有下單路徑
`../entropy-arb/entropy_arb/venue_hl.py` / `venue_lighter.py` 的 `send_taker`。
重點看**未決成交**（unresolved → `orderStatus` 輪詢）那一段：那對兄弟 $1.1M
的病灶就是「以為沒成交所以補單」。CLAUDE.md 寫死「下單程式碼要逐行審」。
**產出**：一份逐段註解 + 找到的問題清單，不改程式。

### B2 憑證缺口修正（規格 §7）
- **兩條 Lighter 鏈共用同一組憑證**（`config.py` 兩處都讀同樣的 `LIGHTER_*`）——
  mainnet 與 RH 是不同鏈不同帳號，而 `NVDA_LL` 兩腿都是 Lighter，**現在根本簽不了**。
  加 `LIGHTER_RH_*` 環境變數。
- HL 改用 **API wallet**（能下單撤單、永不能提幣）。
**產出**：設定載入測試證明兩腿拿到不同憑證；既有配對載入結果逐位元不變。

### B3 ✅ 完成（2026-09-04）——`docs/B3_MAKER_AUDIT.md`

三條「必須」都落在 `entropy_arb/maker.py`（沒有 I/O、沒有自己的時鐘，
所以每一條都能單獨測）。規格點名的四個情境都有測試，其中
「掛→撤單輸掉競速仍成交」直接斷言**那筆成交仍然被對沖**——XEMM 會漏掉的
就是它。自審過程改了四處，兩處是真缺口：送單成功會重置錯誤計數器
（會讓「掛單一直成功、對沖一直失敗」永遠累積不到停機門檻）、拿不到編號的
掛單原本會永遠空轉（現在直接停機並要人工去交易所確認）。
**沒跑過的部分明寫在 §5**：兩個簽章 SDK 本機都沒裝，所以簽章那一層是照
官方原始碼寫的不是跑過的，第一次實盤前要人工確認三件事。

### B3（原文，已完成）補 maker 路徑 —— 引擎缺的那一半
現有只有 `send_taker`。要加 post-only 下單、撤單、5 秒逾時（作者另一個 repo
`cross-exchange-arbitrage` 的形狀：掛 A → 成交才吃 B → 未成交撤單）。
**這是新的、碰錢的程式碼**，寫完要 B1 同等級的審查。

讀 Hummingbot XEMM 執行器原始碼之後補三條**必須**（它兩條都踩了）：
1. **撤單前不得清掉本地訂單狀態。** 訂單只能由「交易所確認已撤」或「成交
   回報」移除；送出撤單後進入 `cancelling` 而不是消失。XEMM 是
   `cancel(...)` 下一行就 `maker_order = None`，撤單輸掉競速時成交事件
   找不到訂單 → **對沖腿永遠不會送出**。
2. **必須處理部分成交**：成交多少對沖多少，不等全部成交。
   `resting` 目前被當成 `unresolved`（對 IOC 正確、對 maker 錯誤），先修語意。
   XEMM v2 全檔零處理 fill 事件（舊的 v1 有 11 處，重寫時弄丟了）。
3. **撤單延遲要有自己的預算格**，與 `staleness_sec` 並列；逾時未確認撤單
   一律**當作可能已成交**（悲觀），不是當作已撤（樂觀）。

**產出**：mock 場館下的單元測試涵蓋「掛→成交」「掛→逾時撤」「掛→部分成交」
「掛→撤單輸掉競速仍成交」。

### B4 引擎的 kill switch（寫死在程式，不是儀表板開關）—— **已完成 2026-09-04**
`max_net_base`（兩腿差額）／`max_gross_usd`（毛曝險）／`max_daily_loss_usd`
（日損）／`max_consecutive_stale`（簿口持續過期）／`max_edge_bps`（邊際太好＝
簿口壞了）／停機後 reduce-only 自救（有進展就重置預算、用完改慢速重試、
**永不永久放棄**）。每一個都有故意觸發它的已知答案測試。

**✅ 已武裝（2026-09-04 晚，`docs/RISK_NUMBERS.md`）**：`config_NBIS.yaml`
與 `config_NVDA_LL.yaml`——規格點名的兩個實盤配對——的 `risk:` 數字全部從
錄價器自己的分鐘資料算出來。其餘七個**刻意維持未武裝**：它們是錄價器，
而規格寫死「只做兩個」，所以用實盤模式啟動它們會被拒絕。這是預期行為。

**⚠ 波動熔斷的門檻要在實盤第一週重調**：`vol_max_move_bps` 起於實測
|1 分鐘變動| p99，再乘 1.6（引擎量的是波峰-波谷幅度，我量到的是首尾位移）。
**那個 1.6 是估計不是量到的**，狀態行會印 `vol trips N`，拿真實觸發次數回來校。

### B4b 波動熔斷 —— **已完成 2026-09-04**
`entropy_arb/volatility.py`。唯一會自動恢復的開關（PAUSE 不是 HALT），
永不暫停對沖與平倉，斷線缺口不算波動。`vol_max_move_bps` 進了
`REQUIRED_RISK`：實盤沒填就拒絕啟動。

### B5 ✅ 完成（2026-09-04）——`entropy_arb/control.py`
`pause`／`resume`／`flat`／`status`，全部冪等，走 Telegram ＋ 落地命令檔
（`echo flat > control.cmd`，讀完即清空）。**`size` 沒做**：同一份規格的
分層表寫的是「控制：不做參數微調」，跟動詞清單自相矛盾，選了限制較嚴的那條。

三件刻意做不到：下單、改參數、**解除 HALT**（halt 單向，因為重啟才會用
strict 重讀真實部位）。`flat` 先對帳核實真實持倉再平——這是 mistake.md
2026-06-07 那道 GET heal 疤的直接對策，測試斷言它平掉的是鏈上的數字而不是
本地相信的數字。CRITICAL 日誌轉發到同一個通道。

### B8 ✅ 完成（2026-09-04）——掃描器兩個儀器缺陷
同場館配對（缺 `va != vb` 守衛，`GOLD@bitget-bitget` 這種）與黃金三個發行商
被併成一個資產（`XAU`/`XAUT`/`PAXG` 全部 → `GOLD`，量到的是託管方基差）。
拆成 `GOLD_IDX`/`GOLD_XAUT`/`GOLD_PAXG`；配對規則抽成 `pair_up()` 才測得了。
**掃描排名的分子先前被污染過，重跑一次才知道差多少。**

### B11 ✅ 完成（2026-09-04）——上線前的三件基礎設施
- **`--shadow`**：策略全跑、一張單都不送，決定寫進 `shadow.csv`。
  `--record-only` 與真錢之間原本是懸崖。**不是 paper mode**：不模擬成交、
  不虛構部位、不宣稱損益（測試直接斷言 position/cash/trades 為 0）。
- **故障注入**（`tests/test_fault_injection.py`，12 案例）：未決、逾時、拒單、
  限流、簿口過期、feed 死掉、場館 down、adapter 拋例外、對沖失敗、差額超上限。
- **P99 延遲**（`entropy_arb/metrics.py`）：撤單來回就是逆選擇成本（M3）。
  平均 80 ms 而 p99 3 秒的系統，是每百張報價賠一次而儀表板全綠。

### B6 `arb_live_*` 表 → `/public/arb-live` → jarvis 儀表板
**唯讀、一顆按鈕都沒有**。引擎有資料之後才做，避免空殼。公開面規則照舊
（只出百分比、方向、時間）。

### B7 成本模型的假設換成量測值
- 第 2 桶：錄製家族補錄 1bps/3bps 累積深度（現在只有頂檔靠 ×4.5 假設）
- 第 3 桶：各場館初始保證金率查文件
- 第 5 桶：一次真實劃轉記費用與到帳分鐘（可與 A1 同一天做）
- **已量到兩筆（2026-09-04）**：
  - HL 費率 **4.5 bps 吃單 / 1.5 bps 掛單**，返佣與質押折扣皆為 0
    （`userFees` 端點，本帳戶）。`config_NBIS.yaml` 原本寫 0.0，已更正。
  - HL **首次出金動作收一次性 $1 帳戶啟用費**（entropy.io 的 Deposit
    彈窗自己寫的）。$50 規模下那是 **2%**，要進第 5 桶。
- 判決程式加一行「扣七桶淨值」報告列（**不動凍結判準**）
**產出**：`cost_model.py` 的「量到%」從 43% 往上。

### B8 掃描器兩個儀器缺陷（§1.03 查到）
- `GOLD` 把 PAXG／XAUT／XAU 併成一個資產互相配對 → 量到的是**發行商基差**。
  拆成 `GOLD_PAXG`／`GOLD_XAUT`／`GOLD_IDX` 或只認同代號。
- 同場館配對（`GOLD@bitget-bitget`）不該存在 → 加 `leg_a != leg_b` 守衛。

### B9 公告監看接進掃描器
三家端點已驗證可用（OKX `support/announcements` 領先市場 6h、Bitget
`public/annoucements` 24 分鐘、Binance cms）。每 5 分鐘輪詢，逐筆記「公告→市場
出現」的領先時間。**這是 §1.05 前瞻時鐘的輸入。**

### B10 §1.04 裡還沒排期的
- 帶改用**費率推導**而非 p90（帶 = 打平成本 × 2）當報告列
- 開盤兩小時 **1 秒取樣**錄一週（改取樣層，資料量 60×）
- **「HIP-3 vs 真實市價」那條腿**——先用免費延遲報價當粗略錨只錄不做，回答
  「偏離有多大、多常、收不收斂」。§1.03 已證明「小所彼此」這條路的帶就是 10 bps 級

## C. 自己會跑的時鐘（不用動，到期再讀）

| 時鐘 | 現況 | 到期 |
|---|---|---|
| §0.75 錄製家族（9 配對） | SNDK 6.5/7 天 | **SNDK 09-04**、其餘 09-06～09-08 |
| §1.02 零費對照 GOLD_LL／NVDA_LL | 0.3/7 天 | ~09-11 |
| §1.05 交叉上市窗口前瞻 | 從 09-04 起算 | ~09-18（≥20 事件各有 >72h 追蹤） |
| §1.03 大所 vs 小所（含 Binance） | Binance 09-04 才進掃描 | ~09-07 樣本夠時重跑 |
| §1.00 掃描排名 | 每小時自動 | ~09-06 看非加密配對能否進前段 |
| Gate F 變體 A | 292/1400 | ~2027-01 |
| 變體 M | 3/400 | ~2027-02 |
| §0.91 資金費收租 | 96/8064 | 09-30 |

## C2. V7 現況（2026-09-04 查證，免得下次重查）

訊號層**健康**：21 天 Strong 開火 DOWN 22 / UP 20（單邊 52%，告警線 85%）、
生產模型 pred 均值 +0.000086（G2 要求 <0.0004）、模型 08-08 部署 27 天（10-07 到期）。
近 30 天 Strong 勝率 56.4%（n=55），樣本太小不能下結論。地形扳機 **56/60，
gap 4.5pp vs 門檻 8pp** → 到期時大概率「不採用」。

**但 V7 現在沒有任何真錢執行**：OKX 帳戶 08-18 被提空（階躍＝提領，非虧損），
Bitget 的 V7Bot **使用者還沒開**（09-04 親口確認）。所以 `v7_product_trades` 0 列是
選擇不是故障，**不要去挖 skip 原因**。Gate B 凍在 21 筆，在 Bitget 開跑前不會動。

**一個未決事項**：OKX executor 仍 `OKX_EXECUTOR_ENABLED=1`（Railway「輸出圖表」服務），
帳戶 $0 而基準 $274 → CAP-4 每小時 DEMOTE、已 185 筆，**kill log 因此失去資訊量**。
關掉它要一起做三件事（改 env、移除 freshness 的 `okx balance snapshots` 列否則它
永遠紅、更新 CLAUDE.md）——**等使用者決定，不要自己動**。

## D. 不要做的（已定案，免得重想）

- 不加 Binance 以外的新場館「因為它看起來有機會」——先讓現有 10 個場館的
  時鐘跑完
- 不做「持有收租」的前瞻時鐘（§1.07：消融證明驅動是出場目標不是 carry）
- 不因為外部報告的 0.93% 改變配對選擇（§1.03：實測 1.07×，而且那是「平均價差」
  不是「p90 可成交空間」，兩個不同的量）
- 不在儀表板放任何按鈕
- 不把套利接進 V7／獵取的任何資料流或資金
